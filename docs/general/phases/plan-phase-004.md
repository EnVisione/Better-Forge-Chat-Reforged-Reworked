# Phase 004 Execution Plan

> **Plan ID:** PLAN-PHASE-004
> **Phase ID:** SEFAUD-PHASE-004
> **Owner:** GUI presentation
> **Classification:** MANDATORY
> **Master plan:** [plan.md](../plan.md)
> **Phase sequence:** 004 of 007
> **Diagnostics contract:** 2

## Purpose and Ownership

This phase delivers the complete `SEFAUD-REQ-005` closure for graphical and textual operator UI after Phase 003 has stabilized command semantics, permissions, effects, feedback meaning, audit identities, and intentionally unavailable states. It audits, remediates, and verifies every in-scope enhanced screen, container screen, HUD, pause entry, workflow, panel, picker, Fancy Tags presentation, and administrator command feedback surface. The measurable outcome is a responsive, accessible, state-clear, privacy-safe, server-authoritative operator experience with semantic equivalence between enhanced and command fallback clients, portable Minecraft Java matching-client proof through the client fixture, and no separate operating system client evidence is required. All three Minecraft Java targets remain supported.

The master plan owns global scope, decisions, compatibility boundaries, phase order, the completion defect boundary, and the plan-wide Definition of Done. This file owns only the detailed execution of `SEFAUD-PHASE-004`. It does not authorize product expansion, platform changes, publication, production mutation, or work assigned to later phases.

## Evidence-Based Entry State

| Evidence class | Area | Finding | Source or command | Freshness condition |
|---|---|---|---|---|
| OBSERVED | Phase authority | Phase 004 owns `SEFAUD-REQ-005`, depends on `SEFAUD-PHASE-003`, and transitions only to `SEFAUD-PHASE-005`. | `docs/general/plan.md`, Sections 12 through 18 | Invalidated by any authorized master-plan revision affecting Phase 004 scope, dependencies, acceptance criteria, or decisions. |
| OBSERVED | Compatibility boundary | The target remains Minecraft 1.21.1, NeoForge 21.1.235, Java 21, Parchment 2024.11.17, the checked-in Gradle wrapper, optional enhanced protocol, fallback clients, current optional-integration contracts, and Linux, macOS, and Windows Minecraft Java support. | `docs/general/plan.md`, `DEC-004`, `DEC-009` | Invalidated by an authorized compatibility decision. A local dependency or platform change is not self-authorizing. Conditional host evidence is required only for changed or exercised host-specific paths. |
| OBSERVED | Enhanced screen family | `SefScreen` suppresses the vanilla background pass. Current direct subclasses are `FancyTagStudioScreen`, `SefControlEditorScreen`, `SefItemPickerScreen`, `SefPanelScreen`, `SefPlayerPickerScreen`, `SefSuggestionPickerScreen`, and `SefWorkflowScreen`. | `src/main/java/com/enviouse/sef/gui/client/SefScreen.java`; CodeGraph exploration; `src/test/java/com/enviouse/sef/gui/client/SefScreenTest.java` | Invalidated by adding, removing, renaming, or changing inheritance or background behavior for an enhanced screen. |
| OBSERVED | Other graphical UI | `SefInvSeeScreen` is a separate `ContainerScreen`; `SefClientEvents` owns pause-menu insertion, HUD rendering, screen invalidation dispatch, InvSee replacement, Fancy Tags rendering hooks, and logout cleanup. | `src/main/java/com/enviouse/sef/gui/client/SefInvSeeScreen.java`, `src/main/java/com/enviouse/sef/gui/client/SefClientEvents.java`; CodeGraph exploration | Invalidated by screen registration, event routing, menu, HUD, or lifecycle changes. |
| OBSERVED | Shared presentation | `SefScreenBackground` and `SefVanillaTheme` provide shared screen background and panel primitives. Existing screen implementations use fixed panel dimensions and page sizes, then center content from current width and height. | `src/main/java/com/enviouse/sef/gui/client/SefScreenBackground.java`, `src/main/java/com/enviouse/sef/gui/client/SefVanillaTheme.java`; CodeGraph exploration of screen classes | Invalidated by layout or theme changes. Fixed dimensions are an audit risk, not proof of failure. |
| OBSERVED | Workflow UI | `SefWorkflowScreen` owns typed fields, variants, pagination, preview, confirmation-pending presentation, execution, progress, results, invalidation, and player, item, and suggestion picker transitions. | `src/main/java/com/enviouse/sef/gui/client/SefWorkflowScreen.java`; CodeGraph exploration | Invalidated by workflow payload, compiler, screen, or canonical route changes. |
| OBSERVED | Player and item selection | `SefPlayerPickerScreen`, `SefSuggestionPickerScreen`, and `SefItemPickerScreen` implement bounded pages and selection flows; CodeGraph reports no direct covering tests for the player and item picker classes. | CodeGraph exploration of picker classes | Invalidated by picker source or test additions. The coverage warning is a planning input, not a confirmed product defect. |
| OBSERVED | Fancy Tags UI | `FancyTagStudioScreen` owns gallery, local, manager, and editor presentation. `FancyTagLocalOverlay`, `FancyTagWorldRenderer`, `FancyTagGlyphBridge`, and `FancyTagClientCache` contribute HUD, nameplate, chat, texture, and client-cache presentation. | `src/main/java/com/enviouse/sef/gui/client/FancyTagStudioScreen.java`, `FancyTagLocalOverlay.java`, `FancyTagWorldRenderer.java`, `FancyTagGlyphBridge.java`, `FancyTagClientCache.java`; CodeGraph exploration | Invalidated by Fancy Tags screen, cache, glyph, renderer, payload, policy, or lifecycle changes. |
| OBSERVED | Protocol authority | `SefNetwork`, `SefSessionManager`, `SefGuiServer`, `GuiWorkflowService`, `PanelActionValidator`, typed payloads, and server-projected action sets form the server-authoritative enhanced UI boundary. Current validation includes session, sequence, feature, panel, record, target, policy, and permission state. | `src/main/java/com/enviouse/sef/gui/protocol`; CodeGraph exploration; `DOCUMENTATION.md` | Invalidated by protocol, session, projection, action, permission, revision, or module changes. |
| OBSERVED | Existing automated coverage | Existing tests cover screen background suppression, workflow compilation, panel validation, session negotiation, request guards, payload codecs, HUD contracts and deltas, identity projection, catalog coverage, GUI preferences, and selected Fancy Tags cache behavior. | `src/test/java/com/enviouse/sef/gui`, `src/test/java/com/enviouse/sef/gui/client`, `src/test/java/com/enviouse/sef/gui/protocol` | Invalidated by source or test changes. Existing tests do not substitute for required graphical, narration, or real-client evidence. |
| OBSERVED | Direct screen coverage gap | The current client test package contains `SefScreenTest` and `FancyTagClientCacheTest`, while the concrete panel, workflow, control-editor, player-picker, suggestion-picker, item-picker, Fancy Tags studio, and InvSee screen classes do not each have a direct class-named test. | `src/main/java/com/enviouse/sef/gui/client`; `src/test/java/com/enviouse/sef/gui/client` | This is a coverage risk, not proof of a defect. `P004-TASK-008` must add deterministic coverage where layout, focus, state, or interaction can be isolated and retain real-client proof for rendering and input. |
| OBSERVED | Prior interactive evidence | Historical evidence opened the pause button and dashboard at 854 by 480 and after resize, but the current acceptance ledger still requires renewed scale, layout, interaction, reconnect, mixed-client, and accessibility proof. | `sef2.md`, Phase 9 and Phase 10 status; `docs/SEF2_ACCEPTANCE.md`; `test.md` | Historical evidence becomes usable only when rerun at the Phase 004 integrated revision in the required environments. |
| OBSERVED | Administrator feedback breadth | Command registrars construct feedback through `Component`, `TextFormatter`, domain helpers, `MessageService`, and shared kernel failures. The Phase 003 universal action matrix is the authoritative enumeration for Phase 004 feedback review. | CodeGraph exploration of `KernelCommandExecutor`, `MessageService`, and command registrars; Phase 003 completion packet | Invalidated by a command, action, feedback, message template, translation, permission, or fallback change. |
| UNKNOWN | Client evidence availability | `EXT-001` is currently unknown. No Phase 004 run has yet proved matching-client startup, every required visual state, mouse and keyboard input, live resize, narration, privacy, authority, revocation, command fallback, feedback, and enhanced/fallback equivalence at one integrated revision. The Minecraft Java client fixture is the client evidence surface. No operating system specific client evidence is required. MacOS or Windows evidence is conditional only for changed or exercised non-client host-specific paths. | `docs/general/plan.md`, `DEC-009`, `EXT-001`; Phase 004 execution evidence is not yet produced. | Remains unknown until `P004-TASK-009` completes the Minecraft Java client fixture rows. An unavailable conditional host leaves only its affected non-client row open and never narrows support or declares Windows unsupported. |
| OBSERVED | Dependency prerequisite inheritance | `EXT-002` is mandatory in-plan closure owned initially by Phase 001 and revalidated later by Phases 005 through 007. Phase 004 UI work does not need a second native runtime or a dependency change. | Master plan Sections 10, 13, and 18; Phase 001 completion packet | Any Phase 004 dependency, packaging, native API, or runtime-supply change reopens the affected `EXT-002` and Phase 001 rows before Phase 004 can close. An unchanged dependency boundary is recorded as inherited evidence, never as an external blocker or a waived prerequisite. |

No evidence in this table is labeled `VERIFIED` for phase completion. Phase 000 and Phase 003 outputs become trusted inputs only after their integration, candidate-branch verification, and signed phase tags pass.

## Scope Boundaries

### Included Scope

- `SEFAUD-REQ-005`: inventory, polish, remediate, and verify every in-scope graphical, HUD, workflow, fallback, and administrator-feedback UI for layout, responsiveness, accessibility, state clarity, safe projection, and server-side authorization.
- Every current direct `SefScreen` subclass: `FancyTagStudioScreen`, `SefControlEditorScreen`, `SefItemPickerScreen`, `SefPanelScreen`, `SefPlayerPickerScreen`, `SefSuggestionPickerScreen`, and `SefWorkflowScreen`.
- `SefInvSeeScreen`, including enhanced target presentation over the same server-authoritative container used by fallback clients.
- The dashboard and every panel, category, detail, search, pagination, typed form, preview, confirmation-pending state, progress, success, failure, empty, loading, and unavailable view emitted through `SefPanelScreen`, `SefControlEditorScreen`, and `SefWorkflowScreen`.
- Player, multi-player, server-authoritative suggestion, registry item, and other typed picker paths, including vanish-safe and permission-filtered records, modded registry content, native item icons, and one bounded native tooltip at a time.
- Fancy Tags gallery, local project, management, editor, upload and transfer state, local provenance, chat glyph, nameplate, HUD, missing texture, cache, and error presentation that exists in the current product.
- `SefClientEvents` pause-menu button, HUD tiles, progress surfaces, local overlay, identity and disguise-facing presentation hooks, screen invalidation routing, disconnect cleanup, and coexistence with vanilla and supported overlay regions.
- Administrator command feedback for every Phase 003 executable administrator action and every intentionally unavailable negative row, including headers, labels, values, paging, success, denial, validation, partial completion, recovery, degraded, unavailable, and failure states.
- Enhanced and fallback semantic equivalence: the same domain result, canonical action id, permission and policy outcome, audit identity, and safe operator meaning, while presentation may differ by negotiated capability.
 - Portable Minecraft Java client fixture runtime evidence for matching enhanced-client startup and navigation, every required visual state, mouse and keyboard input, focus, live resize, narration, privacy, server authority, permission revocation, administrator feedback, command fallback, and enhanced/fallback equivalence. No operating system specific client captures or repeats are required.
- Remediation of every confirmed in-scope blocking or materially misleading UI defect and regression proof at its real boundary.
- Phase-owned tests, documentation updates, sanitized screenshots and recordings, phase evidence, and repository phase integration.

### Explicit Exclusions

- `FUT-001` and `NG-001`: do not implement the sixteen intentionally unavailable control-family runtimes. Their UI must remain clearly unavailable, unreachable, permission-safe, side-effect-free, and negatively tested.
- `FUT-002`: do not add unrelated commands, screens, HUDs, controls, integrations, or gameplay features.
- `FUT-003`: do not perform broad UI framework replacement, architecture modernization, or class decomposition beyond confirmed-defect remediation or mandatory evidence enablement.
- `NG-002`: do not change Minecraft, NeoForge, mappings, Java, Gradle, loader, or protocol boundaries.
- `NG-003` and `DEC-005`: do not publish, deploy, mutate production systems, use production worlds, or use real credentials or personal data. All destructive or privacy-sensitive workflows use disposable synthetic fixtures.
- Phase 005 backend lifecycle closure, Phase 006 full clean-checkout release matrix, and Phase 007 final documentation and endpoint audit remain owned by their phases. Phase 004 must still run the focused lifecycle, build, server, client, fallback, and mixed-client checks needed to prove its own changes.
- New custom fonts, textures, shaders, browser panels, resource-pack requirements, or artwork outside the existing bounded Fancy Tags content model are not authorized.
- An unavailable Linux, macOS, or Windows host is not an exclusion and does not authorize a reduced support claim. It leaves affected host-specific runtime rows open through conditional `EXT-001`; portable Linux work may continue.
- `EXT-001` and `EXT-002` are available in-plan work, not permanent external blockers. Phase 004 closes its canonical Linux client-fixture portion of `EXT-001`, closes any conditional host row created by a changed or exercised non-client path, and consumes or reopens the applicable `EXT-002` packet if its own changes alter dependencies, packaging, or native-runtime supply.

## Phase Contract

### SEFAUD-PHASE-004 — Canonical Owner GUI and Operator Presentation

**Objective:** Deliver and prove a complete, responsive, accessible, privacy-safe, state-clear, server-authoritative UI and administrator-feedback system with no known blocking or materially misleading defect.
**Owner:** GUI presentation
**Dependencies:** SEFAUD-PHASE-003, SEFAUD-REQ-004, DEC-009
**Canonical requirements:** SEFAUD-REQ-005
**Documentation and release impact:** Update `README.md`, `DOCUMENTATION.md`, `docs/PHASE_9_TESTS.md`, `docs/PHASE_10_TESTS.md`, `docs/SEF2_ACCEPTANCE.md`, `docs/COMPATIBILITY_MATRIX.md`, `test.md`, generated command or UI references when their live source changes, and any existing affected Fancy Tags, protocol, command, accessibility, troubleshooting, or operator documentation found by the Phase 000 documentation inventory. Release readiness remains incomplete and publication is not authorized.
**Next transition:** `SEFAUD-PHASE-005`

**Entry criteria**

- Phase 003 is merged through the repository pull-request workflow, all required checks and review gates pass, the resulting candidate branch is verified, and its signed phase tag exists.
- The Phase 000 UI, command-feedback, protocol, HUD, permission, documentation, test, and evidence inventories are available and have no missing or duplicate mandatory owner.
- The Phase 003 completion packet provides the frozen executable action set, intentionally unavailable negative rows, canonical action ids, command and permission references, source and target rules, mutation effects, feedback expectations, and redacted audit evidence.
- The working branch is created from the latest approved candidate state according to repository rules, unrelated user changes are preserved, and the baseline commit, environment, dirty state, and approved evidence root are recorded.
 - A Minecraft Java client fixture and a disposable shared Linux runtime satisfy the canonical Phase 004 portion of `EXT-001` for the exact candidate revision and artifact, with Java 21, Minecraft 1.21.1, NeoForge 21.1.235, matching enhanced clients, fallback clients, capture facilities, and sanitized environment manifests. Client behavior is verified through the Minecraft Java client fixture. Add a macOS or Windows environment only for changed or exercised non-client host-specific paths. If a conditional host is unavailable, record the exact gap and keep only affected rows open; do not call that operating system unsupported.
- The exact enhanced, fallback, incompatible-protocol, mixed-client, synthetic-player, long-locale, registry-content, and disposable-world fixtures required by this plan are available through the Minecraft Java client fixture and on each affected host-specific non-client path. A missing fixture leaves its affected row open and cannot be replaced with lower-fidelity or another-host proof.

**Implementation scope**

- Execute `SEFAUD-REQ-005` completely across every Phase 000 graphical and textual operator UI row.
- Execute `P004-TASK-001` through `P004-TASK-010` in dependency order. This work is governed by `SEFAUD-REQ-005`.
- Audit first, classify each row against fixed criteria, remediate confirmed failures, and rerun every affected row at the repaired revision. This work is governed by `SEFAUD-REQ-005`.
- Preserve server authority. The client may render, navigate, filter already-authorized projections, and retain safe local drafts or preferences, but it may not create permission, identity, target, revision, confirmation, action, publication, audit, or mutation authority. This work is governed by `SEFAUD-REQ-005`.
- Preserve the current vanilla-native visual system and optional-client contract. Enhanced presentation cannot become required for command access or connection eligibility. This work is governed by `SEFAUD-REQ-005`.
 - Apply `DEC-009` to runtime proof. Platform-independent automated evidence may be shared only where the invariant and code path are demonstrably identical. Canonical Minecraft Java client fixture evidence covers all portable client behavior, including matching-client startup, rendering, mouse and keyboard input, live resize, narration, command fallback, and presentation. `EXT-001` adds macOS or Windows evidence only for changed or exercised non-client host-specific paths. This work is governed by `SEFAUD-REQ-005`.

**Execution order**

1. `P004-TASK-001` freezes the Phase 004 surface, state, feedback, and evidence matrices from the integrated Phase 000 and Phase 003 outputs. This advances `SEFAUD-REQ-005`.
2. `P004-TASK-002` defines measurable presentation and accessibility criteria and remediates shared screen, background, theme, layout, focus, narration, tooltip, announcement, and resize primitives. This advances `SEFAUD-REQ-005`.
3. `P004-TASK-003` audits and remediates panels, typed workflows, confirmation, player and suggestion pickers, item browsing, control editing, and InvSee. This advances `SEFAUD-REQ-005`.
4. `P004-TASK-004` audits and remediates Fancy Tags studio, editor, gallery, upload and transfer state, local provenance, glyph, nameplate, and texture presentation. This advances `SEFAUD-REQ-005`.
5. `P004-TASK-005` audits and remediates HUD, pause entry, overlay coexistence, safe regions, reduced-motion behavior, and lifecycle cleanup. This advances `SEFAUD-REQ-005`.
6. `P004-TASK-006` closes privacy, permission, server-authority, unavailable-control, stale-state, revision, module, disconnect, reconnect, dimension, and fallback-equivalence behavior across every graphical surface. This advances `SEFAUD-REQ-005`.
7. `P004-TASK-007` audits and remediates administrator command feedback using the Phase 003 universal action matrix. This advances `SEFAUD-REQ-005`.
8. `P004-TASK-008` expands deterministic automated layout, workflow, session, codec, payload, permission, performance, and regression coverage. This advances `SEFAUD-REQ-005`.
9. `P004-TASK-009` executes the full real-client visual, accessibility, interaction, dynamic-state, administrator-feedback, and enhanced-versus-fallback evidence-capture matrix on the Minecraft Java client fixture. No separate operating system client evidence is required under `DEC-009` or `EXT-001`. This advances `SEFAUD-REQ-005`.
10. `P004-TASK-010` reconciles documentation, runs focused final verification, audits evidence invalidation, prepares the completion packet, and integrates the phase. This advances `SEFAUD-REQ-005`.

Tasks 003, 004, 005, and 007 may prepare independent audit rows after Task 001, but shared primitive changes from Task 002 must integrate before their final remediation and verification. Tasks 006 through 010 are dependency-ordered and may not be bypassed.

**Required evidence**

- One machine-checked UI inventory with one owned row for every Phase 000 graphical, HUD, pause, workflow, picker, Fancy Tags, InvSee, and administrator-feedback surface.
- Automated screen, layout helper, workflow compiler, payload codec, session, stale-state, permission, privacy, and enhanced-versus-fallback equivalence results.
 - A canonical Minecraft Java client screenshot manifest for every screen class and distinct required state at GUI scales 1 through 4, 854 by 480, 1280 by 720, representative narrow and wide aspect ratios, and live resize where the state is interactive. No separate operating system client evidence is required.
 - Canonical Minecraft Java client recordings for matching-client startup and navigation, keyboard-only navigation, focus order, mouse behavior, live resize, narration, tooltip behavior, validation and error announcement, progress and animation, privacy filtering, authority rejection, permission revocation, stale selection, reconnect, disconnect, dimension change, module disablement, command fallback, and administrator feedback. No separate operating system client evidence is required.
- HUD and overlay captures proving safe-region placement and coexistence with hearts, armor, hunger, air, experience, crosshair, selected-item text, chat, subtitles, boss bars, player list, scoreboard, action bar, nickname, Fancy Tags, and disguise presentation where applicable.
 - Through the Minecraft Java client fixture for client behavior, paired matching enhanced-client and command-fallback comparisons show the same authoritative result, canonical action id, permission and policy result, safe feedback meaning, zero unauthorized mutation, and redacted audit record for each representative mutation and failure class. No separate operating system client evidence is required.
 - `EXT-001` environment manifests and sanitized capture indexes for portable Minecraft Java client fixture behavior and any affected macOS or Windows non-client host-specific path, tied to the exact candidate commit and artifact. Missing or failed host evidence is recorded as open for that path and cannot be replaced by another operating system, a mocked operating-system name, a headless widget test, or a support-scope reduction.
- Before-and-after evidence and focused regression tests for every remediated defect.
- Updated documentation and an evidence index tied to the exact integrated commit, client and server environment, artifact, resource set, locale fixture, and capture settings.

**Exit criteria**

- No known mandatory phase-owned defect remains.
- Every Phase 000 UI and administrator-feedback row has one owner, a complete purpose and state hierarchy, a passed matrix result, and current evidence. No row is missing, duplicated, stale, blocked, or represented by a lower-fidelity substitute.
- Every required control and content region remains readable, reachable, and nonoverlapping at GUI scales 1 through 4, 854 by 480, 1280 by 720, representative narrow and wide ratios, and live resize.
- Mouse, keyboard-only, focus order, escape and back behavior, narration, tooltips, announcements, long localized strings, long values, search, pagination, loading, empty, validation, error, success, progress, and rapid refresh states pass.
- Unauthorized entries and private data are absent rather than disabled. Permission loss, stale selection, revision change, module disablement, disconnect, reconnect, dimension change, and server switch close or refresh affected surfaces before mutation.
- Enhanced GUI and fallback commands reach equivalent authoritative outcomes and audit action ids. GUI preferences do not change a complete command's semantics.
 - Matching-client startup and navigation, every required visual, mouse and keyboard input, focus, live-resize, narration, privacy, authority, revocation, fallback, and administrator-feedback row pass through the Minecraft Java client fixture at the integrated revision. No separate operating system client evidence is required.
- The sixteen intentionally unavailable families remain visibly unavailable, cannot preview or appear ready, cannot mutate, and are not advertised as implemented.
- Every confirmed phase-owned defect is repaired with regression proof. No known UI-blocking or materially misleading defect remains.
 - Focused unit, GameTest where applicable, `./gradlew test`, `./gradlew runGameTestServer`, `./gradlew build`, `./gradlew compileFallbackRuntimeJava`, dedicated-server classloading smoke, matching enhanced-client smoke, command-fallback smoke, and mixed-client workflows pass after the last Phase 004 product change. The Minecraft Java client fixture rows pass, and any changed or exercised macOS or Windows non-client host-specific rows pass, using the exact Java 21 environments and task parameters recorded by Phase 000 and `EXT-001`.
- No exercised runtime row is failed, incomplete, stale, or represented as passed by another host. An unavailable conditional host leaves only its affected row open and never changes the supported Linux, macOS, or Windows Minecraft Java claim.
- Documentation, captures, test ledgers, and generated references match the integrated code and artifact. The complete diff contains no secrets, personal data, host-specific private paths, generated runtime output, cache, world data, or unrelated edits.
- The phase branch is reviewed and merged through GitHub with required checks, the resulting candidate branch is verified, and the required signed Phase 004 tag is pushed before Phase 005 begins.

## Inputs and Upstream Contracts

| Input or contract | Provider | Required state | Validation | Failure behavior |
|---|---|---|---|---|
| Frozen UI and feedback inventories | `SEFAUD-PHASE-000` | Every graphical, HUD, workflow, fallback, and textual operator surface has a stable row and owner. | Reconcile inventory against current CodeGraph, source sets, generated references, and Phase 004 branch diff. | Reopen the Phase 000 gate for missing, duplicate, or unowned surfaces and invalidate affected downstream rows. |
| Universal administrator action matrix | `SEFAUD-PHASE-003` | Every executable action and unavailable family has a canonical action id, authority contract, effect, failure behavior, feedback meaning, audit class, and fallback route. | Verify the Phase 003 completion packet, integrated commit, signed tag, live dispatcher and sealed catalog counts. | Stop Phase 004 execution if command semantics are incomplete or unstable. Do not invent UI semantics. |
| Security and privacy closure | `SEFAUD-PHASE-001` | Permission, redaction, sensitive-data, payload, filesystem, and authority boundaries required by presentation are closed. | Check current security evidence and rerun affected tests after any UI projection or payload change. | Route a newly discovered security finding back to its owner, block the affected UI row, and invalidate dependent proof. |
| Dependency and native-runtime closure | `SEFAUD-PHASE-001`, `EXT-002` | Candidate dependency ownership, JNA compile-only supply, platform provenance, alert dispositions, and no-duplicate-native-runtime evidence are closed at the inherited revision. | Compare Phase 004 dependency declarations, resolved graph inputs, and packaged entries with the Phase 001 packet. | If Phase 004 changes dependencies, packaging, or native API reachability, reopen the mapped Phase 001 and `EXT-002` rows and rerun them before closure. Do not label the work externally blocked. |
| Persistence integrity closure | `SEFAUD-PHASE-002` | GUI preferences, offline actions, Fancy Tags client state where applicable, and other UI-facing durable state have declared recovery behavior. | Verify Phase 002 packet and targeted persistence tests for any changed UI-owned durable data. | Do not hide recovery or unsupported states. Block mutation and route the defect to the persistence owner. |
| Optional enhanced protocol | `SefNetwork`, `SefSessionManager`, `SefGuiServer`, typed payloads | Negotiated capabilities, bounded payloads, monotonic sequences, revisioned sessions, server-projected choices, and fallback on incompatibility. | Codec, negotiation, request-guard, replay, permission, stale-state, and mixed-client tests. | Reject or close stale or unauthorized enhanced state without affecting fallback command access. |
| Visual language | `SefScreenBackground`, `SefVanillaTheme`, existing vanilla widgets | Vanilla-native chrome, sharp transparent world background, consistent panel semantics, no resource pack or custom UI texture dependency. | Shared primitive tests, screenshot review, and every-screen inheritance or equivalent behavior checks. | Repair the shared primitive or isolate a justified screen-specific correction; never add a new visual framework to bypass a defect. |
 | Cross-platform runtime evidence | `DEC-009`, `EXT-001` | The Minecraft Java client fixture runs the exact candidate artifact with matching enhanced and fallback clients and retains sanitized manifests and captures. No macOS or Windows client evidence is required. Add macOS or Windows hosts only when changed or exercised non-client paths are host-specific. | Verify operating-system version, architecture, filesystem, Java 21, Minecraft 1.21.1, NeoForge 21.1.235, artifact digests, mod list, client mode, capture settings, and fixture identity before each applicable host matrix. | Mark only unavailable or failed affected host rows open and obtain or repair the required host before closing that affected row. Do not substitute another host or declare Windows or another supported operating-system target unsupported. |
| Synthetic fixtures and capture environment | Phase 000 evidence contract | Disposable worlds, synthetic identities, long locale, item registry fixtures, enhanced and fallback clients, stable capture metadata, and sanitized storage exist through the Minecraft Java client fixture and on each affected host-specific non-client path. | Fixture identity, path, version, locale, resolution, GUI scale, mod list, client mode, and operating-system preflight. | Mark the row blocked and obtain the correct fixture. Do not use production data, another host, or lower-fidelity evidence. |

## Outputs and Downstream Contracts

| Output or contract | Consumer | Guaranteed state | Compatibility or versioning | Evidence |
|---|---|---|---|---|
| Integrated UI surface matrix | `SEFAUD-PHASE-005`, Phase 006, Phase 007 | Every surface and state has an owner, passed criteria, capture, invalidation rules, and current result. | Stable row ids inherit Phase 000 identity; additions or removals require inventory reconciliation. | Machine-checked matrix and sanitized evidence index at the integrated Phase 004 commit. |
 | Remediated enhanced UI | `SEFAUD-PHASE-005`, operators | Current screens, workflows, pickers, Fancy Tags UI, InvSee, HUDs, and pause entry satisfy layout, input, accessibility, state, and privacy contracts on supported Minecraft Java environments. The Minecraft Java client fixture is canonical for all portable client behavior. No macOS or Windows client evidence is required. Direct macOS or Windows evidence is limited to changed or exercised non-client host-specific paths. | Preserve optional protocol and current public identifiers; wire or serialized changes require compatible handling or the smallest versioned change. | Automated tests plus Minecraft Java client fixture captures and recordings, with non-client host evidence only when applicable, and diff review. |
 | Administrator feedback contract | Phase 005, Phase 006, Phase 007 | Every Phase 003 action exposes structured, actionable, privacy-safe success and failure meaning across supported sources, with every Phase 004 client feedback row proved through the Minecraft Java client fixture and non-client host-specific rows proved only when applicable. | Preserve action ids, permission ids, routes, audit identities, and documented fallback semantics. | Feedback matrix, Minecraft Java client fixture component captures, applicable non-client host results, source-class workflows, and redacted audit comparison. |
 | Enhanced/fallback equivalence ledger | Phase 005 and Phase 006 | Capability changes presentation only; domain effect, authorization, action id, feedback meaning, and audit result remain equivalent through the Minecraft Java client fixture. | Matching enhanced, fallback, GUI-off, and incompatible-protocol clients remain supported on Linux, macOS, and Windows; client evidence is the portable Minecraft Java client fixture. | Minecraft Java client fixture paired command and GUI results plus non-client host results when applicable, state assertions, and audit correlations. |
| Invalidation and lifecycle presentation contract | `SEFAUD-PHASE-005` | Permission, revision, module, target, disconnect, server switch, and dimension changes close, clear, or refresh presentation before mutation. | No stale session or confirmation token survives its owning lifecycle. | Automated stale-state tests and recorded dynamic workflows. |
| Phase completion packet | `SEFAUD-PHASE-005`, repository operators | Exact commits, checks, captures, documentation, issues, pull request state, integration proof, signed tag, and invalidation map. | Evidence is bound to the integrated Phase 004 commit and environment. | Packet described in this file's completion section. |

## Work Packages

| Task ID | Requirement IDs | Work | Inputs and dependencies | Outputs | Affected components or interfaces | Verification |
|---|---|---|---|---|---|---|
 | `P004-TASK-001` | `SEFAUD-REQ-005`, `SEFAUD-REQ-001`, `DEC-008`, `DEC-009`, `EXT-001` | Freeze the complete Phase 004 matrix. For every graphical and textual surface, record purpose, audience, entry routes, state hierarchy, primary and destructive actions, navigation, loading, empty, validation, error, success, progress, fallback, privacy class, permission, revision, invalidation, resolution and scale coverage, Minecraft Java client fixture evidence, and evidence route. Reconcile all `SefScreen` subclasses, InvSee, pause and HUD events, Fancy Tags render paths, descriptors, workflow payload types, and every Phase 003 feedback row. Add macOS or Windows rows only for changed or exercised non-client host-specific paths. Record `EXT-001` gaps without excluding any supported operating system. | Integrated Phase 000 inventory, Phase 003 completion packet, `DEC-009`, and `EXT-001` availability matrix. | Machine-checked UI and feedback matrix with no missing, duplicate, or unowned row; Minecraft Java client fixture rows, applicable non-client host rows, support matrix, baseline captures, and known-gap ledger. | `com.enviouse.sef.gui`, `com.enviouse.sef.gui.client`, `com.enviouse.sef.gui.protocol`, Phase 003 action matrix, current command registrars | Inventory drift check fails when a screen, view type, HUD descriptor, payload, command feedback row, unavailable family, or required Minecraft Java client fixture or applicable non-client evidence row lacks ownership. Baseline captures record but do not excuse failures. |
| `P004-TASK-002` | `SEFAUD-REQ-005` | Convert the master acceptance criteria into shared deterministic layout and accessibility rules. Remediate shared background, theme, panel sizing, safe-area, truncation or wrapping, scroll or pagination, focus, narration, tooltip, announcement, destructive emphasis, unavailable-state, and resize behavior. Preserve sharp live world rendering and vanilla-native chrome. | `P004-TASK-001`; current shared client primitives. | Tested shared presentation primitives and a design-system ledger defining spacing, hierarchy, contrast, badges, labels, values, focus, narration, and state semantics. | `SefScreen`, `SefScreenBackground`, `SefVanillaTheme`, shared client helpers, vanilla widgets | Unit or harness tests for bounds and state; every enhanced screen suppresses the unwanted background pass; screenshots show consistent chrome and no double darkening, blur, overlap, clipping, or color-only meaning. |
| `P004-TASK-003` | `SEFAUD-REQ-005`, `SEFAUD-REQ-004`, `DEC-009` | Audit and remediate all panel and workflow surfaces. Cover category and detail pages, panel controls, control editor, typed variants and fields, preview, confirmation-pending state, progress, result, back and close, player and multi-player selection, server suggestions, item browser, search, filters, pages, modded items, native tooltips, stale selection, and InvSee read-only or mutable state. Each field must expose clear labels, bounds, dependencies, errors, and authoritative suggestions without accepting raw command authority. | Tasks 001 and 002; Phase 003 command semantics; server descriptors and workflow compiler. | Passing per-screen and per-state rows for `SefPanelScreen`, `SefControlEditorScreen`, `SefWorkflowScreen`, `SefPlayerPickerScreen`, `SefSuggestionPickerScreen`, `SefItemPickerScreen`, and `SefInvSeeScreen`. | Exact screen paths under `src/main/java/com/enviouse/sef/gui/client`; `UniversalGuiCatalog`, `VanillaDescriptorLinter`, `GuiWorkflowCompiler`, `GuiWorkflowService`, `SefGuiServer`, typed workflow payloads | Automated field, layout, search, pagination, focus, tooltip, suggestion, codec, stale-state, and mutation tests plus real Minecraft Java client fixture captures at every required scale and viewport. Direct host evidence is limited to changed or exercised non-client paths. Equivalent explicit commands prove domain and audit parity on the affected operating system when its path is exercised. |
 | `P004-TASK-004` | `SEFAUD-REQ-005`, `DEC-005`, `DEC-009` | Audit and remediate existing Fancy Tags presentation. Cover gallery, local projects, server manager, editor canvas, tools, palette, layers, undo and redo, local preview provenance, save and export status, upload eligibility, leases, revision conflict, transfer progress, missing or corrupt texture, cache state, chat glyph wrapping, nameplate spacing, HUD placement, server switch, permission loss, disconnect, and unsaved local work preservation. Keep user artwork visually separate from trusted chrome and server-approved status. | Tasks 001 and 002; security and persistence closures; current Fancy Tags protocol and client cache contracts. | Passing Fancy Tags screen and rendering matrix with safe recovery and no authority or provenance ambiguity. | `FancyTagStudioScreen`, `FancyTagLocalOverlay`, `FancyTagWorldRenderer`, `FancyTagGlyphBridge`, `FancyTagClientCache`, `FancyTagProject`, `FancyTagProjectStore`, relevant protocol projections | Automated project, cache, revision, transfer, bounds, teardown, and provenance tests; Minecraft Java client fixture screenshots and recordings only for client behavior; macOS or Windows evidence is limited to changed or exercised non-client paths; server switch and permission-loss workflows; no artwork bytes, paths, hidden identity, or private metadata in broad evidence. |
 | `P004-TASK-005` | `SEFAUD-REQ-005`, `DEC-008`, `DEC-009` | Audit and remediate HUDs, pause entry, overlays, and dynamic presentation. Define priority, caps, safe anchors, label and value hierarchy, non-color cues, reduced-motion behavior, coalesced updates, and coexistence. Verify pause placement never overlaps or replaces vanilla controls and disappears on negotiation or permission loss. Verify HUD state is private, viewer-filtered, bounded, revisioned, and cleared on logout or server switch. | Tasks 001 and 002; Phase 003 active-state semantics; HUD contracts and projections. | Passed pause, HUD, overlay, identity, Fancy Tags, and disguise-facing presentation rows. | `SefClientEvents`, `SefClientModEvents`, `HudContracts`, `HudDeltaComposer`, `IdentityProjectionComposer`, `ClientProtocolState`, `FancyTagLocalOverlay`, `FancyTagWorldRenderer` | HUD contract and delta tests; permission and revision tests; Minecraft Java client fixture screenshots and recordings for client behavior; macOS or Windows evidence is limited to changed or exercised non-client paths; recordings for progress, rapid updates, animation, reduced motion, permission loss, disconnect, and server switch. |
| `P004-TASK-006` | `SEFAUD-REQ-005`, `SEFAUD-REQ-002`, `SEFAUD-REQ-004`, `DEC-002`, `DEC-009`, `EXT-001` | Close authority, privacy, invalidation, unavailable-state, and fallback equivalence for every surface. Omit unauthorized rows and hidden identities rather than disabling them. Revalidate actor, source, feature, permission, target, hierarchy, exemption, revision, record, policy, confirmation, module, and session immediately before mutation. Exercise stale selections, changed permissions, module disablement, revision changes, disconnect, reconnect, dimension changes, incompatible protocol, and forged or replayed payloads. Make all sixteen unavailable families visibly unavailable without a ready or success affordance. | Tasks 003 through 005; Phase 001 security closure; Phase 003 negative rows; `EXT-001` host matrix. | Passing authority and privacy matrix, invalidation map, unavailable-control negative proof, and paired enhanced/fallback equivalence ledger for canonical Linux and any affected host-specific paths. | `SefGuiServer`, `SefSessionManager`, `SessionRequestGuard`, `PanelActionValidator`, `ClientProtocolState`, payload records and codecs, screen invalidation handlers, command fallback paths | Unit, codec, session, replay, permission, hidden-identity, target-swap, stale-revision, module, disconnect, dimension, and negative mutation tests, plus Minecraft Java client fixture privacy, authority, revocation, zero-mutation, command-fallback, state, and audit evidence and no separate operating system client evidence; use Minecraft Java client fixture evidence and direct macOS or Windows evidence only for changed or exercised non-client paths. |
| `P004-TASK-007` | `SEFAUD-REQ-005`, `SEFAUD-REQ-004`, `DEC-008`, `DEC-009` | Audit and remediate administrator command feedback for every Phase 003 row. Establish consistent purpose and state hierarchy, titles, section labels, field labels, values, counts, paging, next action, success, no-change, partial completion, validation, denial, unavailable, recovery, degraded, and fatal states. Ensure feedback remains usable for player, console, RCON, command block, function, scheduled, alias, bundle, sudo, panel, and server-profile sources where supported. Long values must be bounded or paged; failure must be actionable without revealing secrets, raw paths, hidden identities, private messages, or unredacted arguments. | Task 001; Phase 003 action, feedback, source, and audit matrices; shared message and component boundaries. | One passed feedback row for every executable administrator action and unavailable family; remediated shared or domain feedback components; documentation-ready examples. | `KernelCommandExecutor`, `TextFormatter`, `MessageService`, domain command registrars, command feedback components, generated command reference | Component-level tests and source-class workflows capture exact hierarchy and meaning. Through the Minecraft Java client fixture, client behavior is verified without separate macOS or Windows client evidence. Matching-client and command-fallback captures prove equivalent feedback meaning for every Phase 004 runtime feedback row, including required success, denial, validation, revocation, unavailable, degraded, recovery, and failure classes. Phase 003 domain assertions and audit ids remain unchanged. Long-string, pagination, empty, recovery, hidden-data, redaction, and unavailable tests pass. |
| `P004-TASK-008` | `SEFAUD-REQ-005` | Build and maintain deterministic automated proof for layout calculations, screen inventory, field types, workflow compilation, search and pagination, focus and narration metadata, tooltip ownership, UI state machines, payload bounds and codecs, session and permission invalidation, hidden projection, unavailable controls, enhanced/fallback action parity, and performance. Add focused GameTests only where world or server authority is required. | Tasks 002 through 007. | Maintained regression suite and failure-injection fixtures tied to stable matrix rows. | Existing GUI, client, protocol, command, Fancy Tags, inventory, and GameTest source sets | `./gradlew test` and applicable `./gradlew runGameTestServer` pass. Tests fail on clipping math, missing surface ownership, unsafe projection, stale mutation, semantic divergence, or unbounded search, render, payload, or refresh work. |
 | `P004-TASK-009` | `SEFAUD-REQ-005`, `DEC-004`, `DEC-005`, `DEC-009`, `EXT-001` | Execute the real-client matrix after the last UI change on the Minecraft Java client fixture. No separate operating system client evidence is required. Run matching-client startup and navigation and capture every screen class and distinct supported state at GUI scales 1 through 4, 854 by 480, 1280 by 720, narrow and wide ratios, and live resize. Exercise mouse, keyboard-only, focus, escape and back, narration, tooltips, announcements, long locale strings, long values, search, pagination, empty, loading, error, success, progress, rapid refresh, privacy filtering, authority rejection, permission revocation, stale selection, reconnect, disconnect, dimension change, module disablement, command fallback, incompatible, and mixed-client flows. Pair enhanced and fallback routes by canonical action id, authoritative result, feedback meaning, and redacted audit record. | Tasks 006 through 008; verified Minecraft Java client fixture and capture environment, plus macOS and Windows non-client fixtures only when applicable through `EXT-001`. | Minecraft Java client screenshot and recording manifests, accessibility ledger, privacy and authority ledger, revocation ledger, administrator-feedback ledger, enhanced/fallback comparison, and defect dispositions. | Minecraft Java client fixture matching-client runtime, fallback runtime, dedicated servers, mixed multiplayer fixtures, approved sanitized evidence location | Every client matrix row has Minecraft Java client fixture identity, expected and actual result, environment, commit, artifact digest, capture id, and pass or fail. Any failure opens remediation and invalidates affected captures. Any unavailable non-client host or fixture records a conditional `EXT-001` gap, keeps only the affected row open, and does not change the supported-platform contract. |
| `P004-TASK-010` | `SEFAUD-REQ-005`, `DEC-009`, `EXT-001`, `EXT-002` | Reconcile implementation, diagnostics contract version 2, docs, generated references, tests, and evidence. Require one portable Minecraft Java client fixture evidence set and explicit Linux, macOS, and Windows support claims without a separate operating-system client matrix. Validate canonical Linux and any conditional changed or exercised non-client host rows, prove the inherited `EXT-002` dependency and packaging boundary is unchanged or rerun its affected closure, inspect the complete diff and JAR client boundaries, verify audit-final cleanup, resolve phase issues and review findings, merge through the required pull-request workflow, verify the resulting candidate branch, and create the signed Phase 004 tag. | All prior tasks; applicable `EXT-001` rows; inherited or reopened `EXT-002` packet; repository phase workflow. | Updated documentation and support procedure, diagnostics packet, zero unexplained generated drift, final Minecraft Java client fixture verification ledger, applicable non-client host ledger, `EXT-002` comparison, cleanup record, reviewed pull request, verified integration, signed tag, Phase 004 completion packet, and Phase 005 handoff. | `README.md`, `DOCUMENTATION.md`, `test.md`, existing phase test ledgers, acceptance and compatibility docs, generated references, build and artifact outputs, repository workflow | Required local and remote checks and every Minecraft Java client fixture visual, input, resize, narration, privacy, authority, revocation, fallback, feedback, diagnostic, and cleanup gate pass. Applicable non-client host checks pass when exercised; dependency and packaging ownership remains closed; no stale evidence remains; client classes do not load on dedicated-server paths; artifact and diff scans are clean; candidate verification matches the completion packet. |

For every task, record defect ids, affected matrix rows, root cause, smallest in-scope remedy, tests, captures, invalidation, and rollback. A failed shared primitive invalidates every dependent screen capture. A protocol or projection change invalidates codec, session, privacy, enhanced, fallback, mixed-client, and affected visual evidence. A feedback change invalidates its action rows and generated or documented examples.

## Architecture and Implementation Boundaries

### Ownership and dependency direction

- The logical server owns identity, visibility, permissions, feature state, target selection, suggestions, page contents, action availability, preview, confirmation, revisions, and mutation. Client screens own only presentation, navigation, safe filtering of already-authorized rows, unsent bounded drafts, and local preferences.
- `UniversalGuiCatalog`, `GuiWorkflowCompiler`, and server domain owners describe semantic actions and typed fields. They must not provide executable callbacks, raw command strings, arbitrary client classes, pixel coordinates, or trusted client-supplied authority.
- `SefGuiServer`, `GuiWorkflowService`, `SefSessionManager`, `SessionRequestGuard`, `PanelActionValidator`, and typed payload codecs form the server trust boundary. Every privileged request is bounded, session-bound, sequenced, capability-gated, revision-bound, permission-checked, and revalidated before mutation.
- `ClientProtocolState` is a presentation cache. It cannot become durable authority. Disconnect, server switch, incompatible negotiation, module disablement, permission loss, and relevant revisions clear or invalidate dependent presentation.
- `SefScreen`, `SefScreenBackground`, and `SefVanillaTheme` own shared visual behavior. Screen-specific code may specialize content, but must not silently diverge from background, focus, contrast, tooltip, narration, resize, and state semantics.
- `SefInvSeeScreen` remains a presentation of the same authoritative menu and click validation used by fallback clients. Enhanced layout cannot introduce an alternate inventory mutation protocol.
- Fancy Tags local projects and local preview remain client-owned and visibly local. Server publication, assignment, permissions, revisions, canonical image facts, visibility, audit actor, and object authorization remain server-owned. Local artwork must never appear server-approved without explicit provenance.
- Administrator feedback must preserve the Phase 003 action and audit contract. Presentation remediation may restructure components, labels, and pagination, but cannot change authorization, effect, route, action id, redaction, or source semantics without reopening the owning earlier phase.

### Visual design system

- Use vanilla-native widgets, fonts, item rendering, tooltips, focus treatment, and restrained colors. Do not require custom textures, fonts, shaders, browser surfaces, or a resource pack. Fancy Tags artwork is bounded user content, never general UI chrome.
- Each surface declares a title, purpose, primary status, content region, primary action, secondary navigation, destructive action treatment, and location for validation or result announcements. Meaning cannot depend on color alone.
- Labels and values are distinct. Truncation may protect bounded layout only when the complete value is available through an accessible tooltip, detail view, wrapping, or paging and disclosure is authorized.
- Destructive actions state object, scope, consequence, and irreversibility or recovery. Confirmation must describe the exact server-bound preview and cannot be reused after a relevant change.
- Loading, empty, no-match, unavailable, denied, validation, stale, degraded, error, partial success, success, and closed states are not interchangeable. Each state provides the safest useful next action.
- Unavailable controls remain discoverably unavailable only where the current permission-filtered contract allows discovery. They cannot look ready, preview successfully, or expose a mutation affordance.

### Layout, input, and accessibility

- Layout must adapt to actual GUI-space dimensions rather than assume that a centered fixed panel fits. Required content must remain reachable at scales 1 through 4, 854 by 480, 1280 by 720, narrow and wide representative ratios, and during live resize.
 - Matching-client rendering, mouse input, keyboard-only input, focus traversal, escape and back behavior, live resize, narration, tooltips, announcements, and window interaction require direct Minecraft Java client fixture evidence. No separate operating system client evidence is required.
- When all content cannot fit simultaneously, use bounded scrolling, paging, wrapping, or a compact state-specific layout with visible position and navigation. Do not clip required controls, hide them below the window, overlap them, or rely on mouse wheel access without keyboard equivalence.
- Define deterministic focus order from title or first actionable control through content, pagination, primary action, and back or close. Rebuild and resize preserve the nearest safe logical focus without restoring stale authority.
- Escape and back return to the correct parent without double-sending close requests, losing permitted unsent local work, or retaining server-side session state. Disconnect and invalidation close to a safe vanilla or parent surface.
- Narration names the screen, current state, focused control, required field, validation error, destructive consequence, progress update, and final result without narrating hidden data. Repeated rapid updates are coalesced so narration remains usable.
- Tooltips belong to one hovered or focused control, stay within the viewport, expose full authorized content, and never cover the action being described without a keyboard-accessible equivalent.
- Long localized strings, long player or registry names, and long authorized values must not break layout. Use at least one deterministic long-string locale fixture, not ad hoc manual text edits.

### State, privacy, compatibility, and invalidation

- Unauthorized entries, controls, targets, identities, records, and fields are omitted from server projection. Disabling a leaked private row is a failure.
- Unknown and unauthorized players use the same safe unavailable meaning where distinction would disclose vanish, membership, or private state. Search and counts must not leak filtered records.
- Suggestions and item or player pages are server-authoritative where permissions or privacy apply. Client search may narrow an authorized page but cannot infer or request arbitrary hidden records.
- Permission, hierarchy, exemption, feature, module, policy, command definition, target, record, inventory, provider, configuration, and session revisions invalidate only the dependent state, but always before a privileged mutation.
- Enhanced, fallback, GUI-off, and incompatible clients preserve command access and equivalent domain semantics. No enhanced payload is sent to a connection that did not negotiate the feature.
 - Through the Minecraft Java client fixture, the matching enhanced client and command fallback must be paired against the same server-side fixture. Each pair must prove the same authorization decision, canonical action id, authoritative mutation or zero mutation, safe feedback meaning, and redacted audit result. Presentation differences may not become semantic differences.
- Wire shapes, action ids, serialized fields, permission ids, translation keys, and routes remain stable unless the smallest compatible or versioned repair is necessary. Unsupported newer state fails explicitly.

### Concurrency, threading, and performance

- Client widget construction and render-state mutation run on the client thread. Server mutations run on the logical server thread. Background image, cache, search, or persistence work may publish only immutable validated results to the owning thread.
- Render, tick, narration, and input paths perform no filesystem or network access, blocking waits, unbounded registry scans, or repeated full-domain recomputation. Item and player lists, Fancy Tags textures, HUD deltas, and search results remain bounded and paginated or indexed.
- Rapid search replacement, progress updates, permission refresh, config reload, mass invalidation, HUD delta bursts, and texture availability changes cannot reorder stale state over a newer revision.
- Screen close, disconnect, server switch, and module disablement release sessions, transfers, textures, overlays, drafts according to ownership. Local unsaved Fancy Tags work may survive only under the documented local recovery contract.
- Evidence capture must not materially change timing or hide performance failures. Record capture tooling and repeat performance-sensitive rows without capture overhead.

## Diagnostics and Debugging

**Requirement IDs:** `SEFAUD-REQ-005`
**Task IDs:** `P004-TASK-001`, `P004-TASK-002`, `P004-TASK-003`, `P004-TASK-004`, `P004-TASK-005`, `P004-TASK-006`, `P004-TASK-007`, `P004-TASK-008`, `P004-TASK-009`, `P004-TASK-010`
**Controls:** Reuse the Phase 000 server-authoritative controls `/sef debug on <category> [target] [seconds] [sample_limit]`, `/sef debug status`, and `/sef debug off`; the owned server console uses the same commands without the leading slash. Only the console or a source with `sef.commands.sef.debug` may operate them. Phase 004 uses `ui`, `protocol`, `authority`, `feedback`, `hud`, and `fancy_tags` categories with an explicit synthetic player or component target. Unknown categories, absent or unauthorized targets, ambiguous identities, and remote client attempts fail without enabling capture. `off` is idempotent, and timeout, target removal, disconnect, reload, server stop, and restart reset applicable state. Client-only observations use bounded fixture-controlled client logs, narration capture, and targeted visual recording joined to the server `capture_id`; no client-authoritative debug or mutation command is added. Missing inherited controls at entry reopens the Phase 000 diagnostic gate.
**Signals:** Emit diagnostics contract version 2 records for capture identity, candidate commit and artifact hashes, sequence and correlation, logical side, screen or HUD surface id, state, GUI scale, logical and framebuffer dimensions, focused control, input method, narration and announcement event, tooltip bounds, authorized row counts, session and relevant revisions, canonical action, expected and actual decision, reason code, render or handler duration in milliseconds, payload bytes, queue depth, dropped and truncated counters, protocol state, and cleanup state. Values are typed, bounded, sanitized, and separated into server-authoritative state, client-received state, and observed presentation. The signal table below defines sources, units, and failure indicators.
**Collection procedure:** Follow the numbered Phase 004 collection runbook below. It freezes one UI row and candidate, registers disposable resources, starts the canonical Linux server and silent client fixture only when the claim requires it, enables one diagnostic category and target for a bounded window, performs one deterministic input and state sequence, captures event-driven logs and only the visuals needed for presentation claims, disables capture, compares server, client, visual, domain, and audit results, retains a sanitized packet, and completes cleanup on every used host.
**Headless verification:** On `node-1` or the Linux laptop, first inspect the Gradle task graph and prove it starts no client, renderer, or display. Use focused JUnit tests and `./gradlew test` for layout arithmetic, inventory drift, field and pagination state, focus metadata, narration metadata, payload bounds, session state, projections, privacy, feedback components, and lifecycle invalidation. Use `./gradlew runGameTestServer` only for actual server or world authority paths, and run `./gradlew build compileFallbackRuntimeJava` plus a no-GUI dedicated-server classloading smoke after Java or resource changes. These checks prove deterministic logic, server authority, packaging, and classloading only. They cannot prove pixels, mouse or keyboard input, actual focus traversal, narration playback, live resize, tooltips, HUD placement, client receipt, or reconnect presentation.
**Client verification:** The residual client claims are every graphical screen and state, actual mouse and keyboard-only interaction, focus traversal, escape and back behavior, narration and announcements, tooltips, GUI scales and viewports, live resize, HUD and overlay coexistence, Fancy Tags visual provenance, client receipt and clearing of authorized projections, permission revocation, reconnect, and enhanced versus fallback presentation. Run one portable Minecraft Java client evidence set on the owner Linux laptop against the same canonical Linux candidate server. Use event and client logs before visual sampling, capture screenshots only for static presentation, and record short bounded video or narration capture only for dynamic or auditory accessibility claims. No macOS or Windows Minecraft client is launched or required.
**Client audio isolation:** Before launch, create a disposable 1.21.1 client directory and set that instance's discovered pinned-version master audio option to zero without touching a personal instance. After launch, query `hyprctl clients -j` and bind the exact owned Hyprland window address, class, title, and PID to the launched candidate process. Verify the candidate identity, active desktop, and discrete RTX renderer. Correlate only that PID and verified descendants with its PipeWire or PulseAudio application stream, apply `wpctl set-mute <discovered-node-id> 1` or `pactl set-sink-input-mute <discovered-input-id> 1`, and verify the matched application stream reports muted before any acceptance action. Keep a bounded owned watcher and repeat identity and mute verification after resource reload, device change, reconnect, restart, or every stream recreation. If window, renderer, process-tree, or mute proof fails, stop that client and leave only its client rows unverified. Teardown stops the exact client and watcher, verifies processes and streams disappeared, and removes disposable audio, routing, and instance state. Never mute the default sink, system-wide output, microphone, unrelated application, or user-owned client. Narration evidence is captured through an inaudible disposable route or metadata rather than audible playback.
**Budgets and privacy:** Diagnostics are off by default. Default duration is 60 seconds and maximum duration is 300 seconds; default sample limit is 1,000 and maximum is 10,000 events; maximum output is 4 MiB; maximum accepted rate is 100 events per second per capture. Client capture is limited to the rows named before launch, one required static image per stable visual state and one shortest useful recording per dynamic state. Disabled diagnostics perform no expensive formatting, traversal, packet, render, or filesystem work. Enabled diagnostics build records lazily, coalesce refresh and narration events, bound per-tick work and buffers, expose dropped and truncated counters, and never block render, tick, input, packet, or server threads. Redact credentials, private messages, raw or linkable addresses, hidden identities, restricted arguments, artwork bytes, server and private absolute paths, unrelated chat, and personal desktop content before any sink. Diagnostics cannot grant permission, reveal filtered rows, alter layout or gameplay, persist enablement, or upload evidence.
**Regression and support:** Test enable, status, disable, idempotent disable, console and permission authorization, unknown category, absent and removed target, timeout, disconnect, reload, stop, restart, schema fields, ordering, correlation, rate and output limits, dropped and truncated counters, privacy filtering, disabled-mode overhead, bounded enabled overhead, and unchanged domain and presentation outcomes. Each repaired UI defect adds a row-specific signal assertion and a capture-without-capture comparison for timing-sensitive behavior. `P004-TASK-010` updates the `UI diagnostics and accessibility capture` procedure in `DOCUMENTATION.md`, the executable Phase 004 workflow in `test.md`, and links the support entry from `README.md`. The support packet contains candidate and environment identity, minimal sanitized configuration, exact reproduction and input path, expected and actual state, capture id and window, relevant bounded server and client logs, required visual or narration evidence, test summary, disposition, and cleanup result.

### Phase 004 Diagnostic Signals

| Signal or record | Source and unit | Expected observation | Failure indicator |
|---|---|---|---|
| Capture and candidate identity | Shared diagnostic service and evidence manifest; opaque ids, schema integer, sequence, hashes, seconds, events, bytes | Unique `capture_id`, schema version 2, stable `correlation_id`, exact commit and SHA-256 and SHA-512 artifact identity, truthful remaining budgets, and no reuse after reset | Missing or stale identity, nonmonotonic sequence, silent truncation, capture beyond limits, or evidence bound to another build |
| Surface and layout state | Client screen, HUD, and capture harness; stable surface and state ids, GUI scale, logical pixels, framebuffer pixels, rectangles | Correct screen or HUD state, every required rectangle inside the viewport and safe region, no prohibited overlap, and stable state-specific hierarchy at each required scale and resize | Clipped or unreachable control, overlap, double background, hidden state, stale geometry, color-only meaning, or capture lacking scale and dimensions |
| Input, focus, narration, and tooltip | Client widget and accessibility hooks; input enum, focused-control id, ordered event ids, bounded text length, viewport rectangle | Mouse and keyboard reach equivalent actions, deterministic focus order, correct escape and back target, coalesced meaningful narration, and one in-bounds authorized tooltip | Mouse-only action, lost or stale focus, double close or submit, narration flood, hidden data narration, tooltip offscreen, or inaccessible full value |
| Projection and authority | `SefGuiServer`, session and request guards, panel validator, payload handlers; ids, revisions, counts, decision enum | Client rows are a subset of current server-authorized projection, revisions match, the canonical action is revalidated before mutation, and denial or revocation clears private state | Unauthorized row or count leak, hidden identity, stale or replayed action accepted, client-selected authority, mutation after denial, or payload sent without negotiation |
| Feedback semantics | Command and GUI result components plus audit; canonical action id, result class, labeled counts and values, correlation id | Enhanced and fallback routes report equivalent success, no-change, denial, validation, partial, degraded, recovery, unavailable, or failure meaning and the same audit action id | False success, unlabeled value, hidden partial result, unsafe detail, different action id, or feedback whose meaning changes by client capability |
| HUD, overlay, and Fancy Tags presentation | `SefClientEvents`, HUD composers, Fancy Tags cache and renderers; surface ids, anchors, priorities, tile counts, revision, provenance enum | Bounded viewer-filtered state uses safe regions, preserves critical vanilla UI, marks local versus server-approved content, coalesces updates, and clears on lifecycle invalidation | Overlay collision, unbounded tiles, stale state, private projection, ambiguous artwork provenance, missing-texture failure blocking unrelated UI, or state surviving server switch |
| Timing and cleanup | Client and server diagnostic owners, capture harness, process and stream registry; milliseconds, queue depth, process and path counts | Render, input, search, refresh, texture, and narration work stays within measured baseline budgets; drops are explicit; capture stops; owned processes and streams exit; disposable paths are absent | Blocking work, unbounded queue or scan, events after disable, capture materially hiding a regression, orphan process or stream, or retained disposable state |

### Diagnostic Collection Runbook

1. Select one Phase 004 matrix row and its owning `P004-TASK-###`. Record the full candidate commit, candidate JAR SHA-256 and SHA-512, Java 21, Minecraft 1.21.1, NeoForge 21.1.235, exact screen or feedback state, expected result, server and client configuration, locale, GUI scale, logical and framebuffer dimensions, synthetic actors, host, runtime directories, evidence destination, and every owned path, process, playback stream, watcher, and capture resource before creation. Use fresh unique disposable roots and discover any unknown laptop path or launcher property before launch.
2. Use headless tests when the claim is layout arithmetic, state transition, payload, projection, authority, feedback structure, or server behavior. For a residual client claim, use the canonical owner Linux laptop. Configure only the disposable server runtime with one effective `eula=true`, read it back, start the no-GUI candidate server, verify the exact candidate ready event and console ownership, then apply the full silent-client preflight before client launch.
3. Start `sef debug on <category> <target> <seconds> <sample_limit>` from the owned server console and record the returned `capture_id`, expiry, budgets, and output. Start the bounded client log, narration, screenshot, or recording capture for the named row only. Verify the intended synthetic player joins the exact server in both server evidence and client world state before in-world assertions. A title screen or server-list entry is not a successful join.
4. Execute the declared input path through actual mouse, keyboard, resize, narration, command, reconnect, permission, module, dimension, or server-switch behavior. Use server console only for deterministic fixture setup and state transitions that do not bypass the behavior or permissions being tested. Capture the minimum event-driven evidence for every required stable or dynamic state; do not poll screenshots for facts available in logs.
5. Run `sef debug status` and join server events, client logs, visual or narration captures, domain before-and-after state, and audit results by `capture_id`, `correlation_id`, canonical action, session and revisions, candidate commit, and artifact hashes. Verify layout bounds, focus, input equivalence, announcements, privacy omission, current authority, feedback meaning, dropped and truncated counters, and zero unauthorized mutation. Separate server state, client-received state, and actual pixels rather than inferring one from another.
6. Run `sef debug off` twice and verify idempotent disabled status, zero later matching events, and reset on timeout, disconnect, reload, stop, and restart as applicable. Retain only the minimal sanitized support packet in the Phase 000 approved evidence location. No automatic external upload, whole-runtime copy, unbounded log, or personal desktop capture is authorized.
7. On success, failure, timeout, cancellation, or launch failure, gracefully stop only the exact owned clients, capture tools, audio watchers, temporary routes, and server; wait for exit; verify application streams disappeared; preserve only required sanitized evidence; re-resolve registered cleanup targets without following symlinks; remove only test-created worlds, logs, screenshots, recordings, crash reports, configuration, client and server instances, and disposable build output; and verify absence on every used host. Record acceptance and cleanup separately. Any remaining owned process, stream, or disposable path keeps the workflow cleanup-incomplete and must be reconciled before another run.

## Runtime Host, Client, and Cleanup Contract

| Workload | Execution host and runtime | Required identity and evidence | Client rationale |
|---|---|---|---|
| Source inventory, deterministic unit tests, build, fallback compilation, artifact inspection, and verified server-only GameTests | `node-1` or Linux laptop in a fresh disposable checkout or worktree using Java 21 and the checked-in wrapper | Record checkout, commit, task graph, command, Java and Gradle versions, report path, result, test-created resources, and cleanup. On `node-1`, prove no task launches a client, renderer, or display. | No client is needed for inventories, arithmetic, component structure, codec, projection, authority, server-state, packaging, or classloading assertions. These results never close visual or interaction rows. |
| Canonical Phase 004 server and Minecraft Java client fixture | Owner Linux laptop with active desktop and verified discrete RTX renderer; a discovered disposable no-GUI server directory and separate disposable enhanced, fallback, and incompatible client directories | Record exact source commit, candidate hashes, Minecraft and NeoForge versions, mod lists, server and client runtime paths, loopback or existing authorized private endpoint, `eula=true` readback, server readiness, console control, intended joined players on both sides, window and PID identities, renderer, per-stream mute states, capture ids, results, and cleanup | A real client is mandatory for actual rendering, mouse and keyboard input, focus, narration, resize, HUD placement, tooltips, client receipt, reconnect, and mixed-client presentation. One Linux evidence set proves the portable Minecraft Java client contract. |
| macOS or Windows non-client path changed or exercised by Phase 004 | Matching disposable host runtime for only the affected filesystem, native, startup, process-control, dependency-loading, recovery, or packaging path | Record source-to-artifact reachability trigger, host and filesystem, commit and hashes, installed runtime, exact command, result, evidence, and cleanup. Otherwise record `NOT_EXERCISED` with reachability proof. | No Minecraft client is launched. There is no separate macOS or Windows client, screen, narration, input, resize, or visual matrix. |

Before a real-client run, discover and verify the current repository run definitions. The observed build exposes `runCandidateServer` with `-PsefCandidateServerGameDirectory=<disposable-server-dir>`, `runClient` with `-PsefClientGameDirectory=<disposable-client-dir>` and `-PsefClientQuickPlayServer=<verified-endpoint>`, and `runFallbackClient` with the corresponding fallback properties. Recheck these live definitions before execution rather than assuming they remain unchanged. Use the exact candidate server artifact and bind client source outputs or installed artifact to the same commit and hashes. Do not alter a personal server list or instance.

Create and verify the disposable server's `eula.txt` before launch, start the server without a GUI, and confirm readiness rather than relying on an open port. Preconfigure each isolated client's master output to zero, launch one owned client at a time until its exact Hyprland window and process tree are bound to a muted application stream, and keep all simultaneously required client streams independently identified and muted. Connect automatically through the verified quick-play property when supported; otherwise use existing authorized desktop control without changing a personal saved list. Verify the intended player joined and entered the correct server world before assertions. Recheck every stream after reconnect, resource reload, or recreation. If desktop, renderer, connection, identity, or mute evidence is unavailable, stop the affected client, keep its rows unverified, and continue independent headless work. Never move the client to `node-1`, use a virtual display or software renderer, weaken authentication, expose a port publicly, change firewall rules, or request credentials.

Singleplayer is excluded because Phase 004 acceptance exercises shared server authority, fallback equivalence, reconnect, and mixed-client behavior. It becomes permissible only for a specifically identified singleplayer or integrated-server defect with a named requirement id and reproduction rationale; missing connectivity or convenience is not an exception. Server-observable rows use console, bounded diagnostics, and real server assertions without requiring EnVy to join. Console setup never substitutes for natural client input or visual presentation.

Every Phase 004 test and audit registers teardown before execution. Prefer unique disposable directories and a disposable checkout so build output, client and server runtimes, worlds, options, logs, crash reports, captures, and generated configuration can be removed after the final evidence consumer. Preserve tracked source and fixtures, required sanitized evidence, shared dependency caches, personal instances and worlds, unrelated worktrees, and unrelated processes. Never use blanket `git clean`, broad process kills, broad globs, repository-root or workspace-root deletion, or cleanup that follows symlinks. A read-only audit that creates no resources records that fact; a passed test with unverified cleanup remains incomplete.

## Failure, Recovery, and Edge Cases

| Scenario | Detection | Required behavior | Recovery or rollback | Regression proof |
|---|---|---|---|---|
| Viewport or scale is too small for current fixed dimensions | Bounds assertion, clipped capture, unreachable focus, or overlap | Reflow, page, wrap, or scroll within the shared visual system. Preserve every required action and state. | Return to previous safe screen if no valid composition exists; do not submit hidden controls. | Automated layout bounds plus captures at every required scale and viewport. |
| Live resize occurs during edit or confirmation | Resize event and screen rebuild | Preserve safe field drafts and logical focus; invalidate pixel positions; never preserve stale confirmation authority. | Recompute layout and require a new preview when semantic inputs or binding changed. | Resize recording with fields, focus, picker, preview, and confirmation states. |
| Long translation, value, player name, or registry id overflows | Text measurement exceeds assigned region | Wrap, fit with accessible full-value affordance, or page. Do not conceal a destructive target or error. | Use detail or tooltip fallback that remains within viewport and privacy rules. | Long-locale and long-value automated and screenshot tests. |
| Search yields no results or server page becomes empty | Authorized result count is zero | Show explicit no-match or empty state, active query, and safe clear or back action. Do not reveal filtered counts. | Clear or revise search and request a current authorized page. | Player, suggestion, item, panel, and Fancy Tags empty-state tests. |
| Server suggestions arrive late or out of order | Workflow, field, request, or revision mismatch | Ignore stale response; retain current safe draft; never replace newer input. | Submit a new bounded request or continue with validated manual input where allowed. | Out-of-order and rapid-search tests with revision assertions. |
| Permission is revoked while a screen is open | Server revision or feature update, action rejection | Remove private state and close or downgrade before another privileged submission. Announce a safe reason. | Restore permission, renegotiate or reopen, and obtain a new projection. | Recorded revocation plus session, projection, and zero-mutation assertions. |
| Module or global GUI mode is disabled | Effective capability mask changes | Close owned enhanced screens, cancel invalid previews, clear HUD and projections, keep command access. | Re-enable, renegotiate, and reopen from current server state. | Module-disable and GUI-off enhanced/fallback workflow. |
| Selected player, item, record, or panel revision becomes stale | Revision, existence, visibility, registry, or target mismatch | Reject before mutation, remove or refresh the selection, and identify the safe corrective action without leaking hidden state. | Request a new authorized page and preview. | Stale target, entry, item, panel, and workflow tests. |
| Confirmation expires or inputs change | Binding or expiry mismatch | Do not execute. Return to editable state with the exact fields needing new preview. | Preview again and obtain a new confirmation. | Replay, expiry, changed-field, changed-cost, and changed-target tests. |
| Disconnect, reconnect, server switch, or dimension change occurs | Connection and lifecycle events | Close or invalidate dependent screens; clear sessions, sequences, projections, HUD, transfers, and server-scoped caches. Preserve only permitted local preferences or unsent local projects. | Negotiate a new session and request fresh state. | Recorded disconnect, reconnect, server-switch, and dimension workflows. |
| Fancy Tags texture is missing, corrupt, late, or unsupported | Cache verification, decode failure, missing handle | Show bounded alternative, loading, missing, or error state without blocking unrelated UI or exposing paths and hashes broadly. | Re-request only authorized content or clear corrupt server-scoped cache through its safe contract. | Missing, corrupt, cache-hit, cache-miss, and teardown tests and captures. |
| Fancy Tags permission or edit lease is lost | Server projection or upload rejection | Close management authority, cancel uncommitted upload, remove unauthorized projections, preserve local unsaved work with clear local provenance. | Reauthorize and reopen against a fresh revision or continue local editing only. | Lease-loss and permission-loss recordings plus server-side zero-mutation proof. |
| HUD competes with vanilla or another owned surface | Safe-region collision or tile budget breach | Apply priority, cap, coalescing, relocation, or documented fallback without hiding critical vanilla UI. | Disable noncritical local tiles or use command fallback; required state remains understandable. | Overlay coexistence screenshots and tile-budget tests. |
| Narration floods during progress or refresh | Repeated announcements within bounded interval | Coalesce intermediate updates and announce meaningful state transitions and final result. | User can refocus or request current status without stale repetitions. | Narration recording and deterministic coalescing test. |
| Fallback client lacks enhanced capability | Negotiation state or GUI disabled | Send no enhanced payload. Preserve complete authorized command, chat, action-bar, boss-bar, vanilla-menu, or truthful no-visual fallback. | Install a compatible optional client or continue through fallback. | No-SEF, incompatible, GUI-off, and mixed-client comparison. |
| Unavailable control family appears in navigation | Phase 003 negative row and descriptor state | Present only truthful unavailable status where discoverable; no ready, preview, confirmation, or success affordance; zero mutation. | No automatic recovery. Runtime implementation remains future scope. | Sixteen named negative UI and command feedback rows. |
| Administrator feedback includes restricted content | Redaction or privacy scan | Omit or replace with approved safe label. Preserve actionable reason and correlation without private content. | Quarantine unsafe evidence, repair projection, and rerun affected action rows. | Hidden-identity, private-message, path, secret-argument, and audit comparison tests. |
| Mutation partially completes | Domain result and Phase 003 outcome indicate partial completion | State exactly which bounded records succeeded, failed, or remain pending without false global success. | Use documented retry or recovery only for safe independently revalidated records. | Representative batch, queue, storage, and provider partial-result workflows. |
| Client thread, render path, or search stalls | Profiler, frame-time, tick, queue, or timeout budget breach | Move safe work off hot path, bound work, and publish validated immutable results on the owning thread. | Cancel superseded work and restore last valid presentation. | Maximum legal list, rapid search, HUD burst, texture, and mass invalidation performance tests. |

## Verification Matrix

| Requirement or task | Static or unit | Integration | Real workflow or runtime | Negative and recovery | Evidence artifact |
|---|---|---|---|---|---|
| `P004-TASK-001` | Inventory parser, duplicate owner, missing state, and drift checks | Reconcile Phase 000, Phase 003, live catalog, descriptors, payloads, screens, HUDs, commands, and docs | Baseline capture from the integrated candidate | Inject or simulate a missing screen, HUD descriptor, feedback row, and unavailable family | UI and feedback matrix with commit and environment manifest |
| `P004-TASK-002` | Bounds, text-fit, focus-order, narration metadata, tooltip, theme, and background tests | Shared primitives exercised by every screen type | Every screen at required scales, viewports, and resize through the Minecraft Java client fixture, with direct macOS or Windows evidence only when a changed or exercised non-client path is host-specific | Long strings, minimum height and width, missing assets, color-blind states, and rapid rebuild | Canonical and affected-host design-system ledger and shared-primitive screenshot set |
| `P004-TASK-003` | Workflow compiler, field bounds, pages, search, selection, codec, focus, and state tests | Panels, controls, workflows, pickers, and InvSee through server authority | Through the Minecraft Java client fixture for client behavior, run bare command, dashboard, typed fields, preview, confirmation, progress, result, retry, and explicit command | Invalid values, stale selection, hidden target, permission loss, modded item, empty page, out-of-order suggestions | Minecraft Java client fixture screen captures, workflow recordings, state and audit comparison |
| `P004-TASK-004` | Fancy Tags project, cache, texture facts, bounds, lease, revision, and teardown tests | Studio, protocol, cache, glyph, nameplate, HUD, and local overlay | Through the Minecraft Java client fixture for client behavior, run gallery, editor, local preview, save, export, upload, transfer, render, reconnect, and server switch | Missing or corrupt texture, stale lease, permission loss, upload failure, unsupported state, and unsaved-close | Minecraft Java client fixture Fancy Tags screenshot and recording manifest with sanitized fixture ids |
| `P004-TASK-005` | HUD contract, delta, safe-region, priority, cap, reduced-motion, and pause-placement tests | HUD, pause, identity, Fancy Tags, disguise, and vanilla overlays | Through the Minecraft Java client fixture for client behavior, toggle each active state and combine all applicable surfaces at required scales | Revocation, module disable, old delta replay, disconnect, server switch, tile overflow | Minecraft Java client fixture HUD coexistence captures and dynamic recordings |
| `P004-TASK-006` | Session, request guard, panel validator, payload codec, permission, projection, and invalidation tests | Server projection to client close or refresh and fallback path | Minecraft Java client fixture matching enhanced, command fallback, GUI-off, incompatible, and mixed-client action pairs | Forgery, replay, target swap, hidden identity, stale revision, permission revocation, disconnect, dimension, unavailable family | Minecraft Java client fixture privacy and authority matrix, zero-mutation assertions, paired audit records |
| `P004-TASK-007` | Component structure, bounded output, pagination, redaction, and template tests | Feedback through shared kernel and domain registrars across supported sources | Through the Minecraft Java client fixture for client behavior, run representative success, no-change, denial, validation, revocation, partial, degraded, recovery, unavailable, and fallback workflows | Long values, empty state, secret input, hidden identity, raw path, RCON or console width, failed provider | Minecraft Java client fixture administrator feedback ledger and sanitized component captures |
| `P004-TASK-008` | Complete focused unit suite | Applicable GameTests and cross-route parity tests | Headless and graphical harness smoke | Maximum legal lists, rapid search, refresh storms, malformed payloads, stale responses, and thread assertions | Test reports, GameTest reports, performance measurements |
| `P004-TASK-009` | Capture manifest, operating-system coverage, and expected-result schema validation | Minecraft Java client fixture mixed-client and enhanced/fallback comparison | Full matching-client startup, visual, mouse, keyboard, focus, accessibility, narration, resize, privacy, authority, revocation, feedback, fallback, and lifecycle matrix through the Minecraft Java client fixture | Every required failure and recovery state, plus explicit `EXT-001` routing for any unavailable applicable non-client host | Sanitized Minecraft Java client fixture screenshots, recordings, transcripts, and manifest |
| `P004-TASK-010` | Documentation, cross-platform support claim, link, generated reference, diff, secret, and artifact-boundary checks | Build, fallback compilation, dedicated-server classloading, candidate verification | Final matching enhanced-client, command-fallback, and mixed-client smoke through the Minecraft Java client fixture after the last change | Evidence invalidation audit, conditional non-client host gap audit, and rollback rehearsal for phase-owned UI changes | Completion packet, applicable `EXT-001` records, pull request results, merge commit, signed tag, Phase 005 handoff |

### Fixtures and environments

 - Run the Phase 004 real-client matrix in a disposable Minecraft Java client fixture environment for one exact candidate commit and matching artifact digest. Do not add operating system specific client environments. The client fixture manifest records operating-system version, architecture, filesystem, Java 21 vendor and version, Minecraft 1.21.1, NeoForge 21.1.235, mod list, client mode, locale, accessibility settings, capture tool, and fixture identity. Non-client host manifests are added only when a changed or exercised path requires them.
- Use synthetic administrators with explicit grant, explicit deny, revoked, equal-rank, higher-rank, exempt, visible, vanished, online, known-offline, and never-seen identities. Never use real player records.
- Through the Minecraft Java client fixture, use at least one matching enhanced client, one command-fallback client with no SEF classes, one deliberately incompatible enhanced protocol fixture, and enhanced plus fallback clients connected simultaneously. No separate operating system client evidence is required.
- Use disposable worlds and deterministic domain fixtures for every destructive or privileged workflow. Reset from a retained baseline between workflows whose results could interfere.
- Use vanilla and permitted modded registry content for items and other registry-driven fields, including long namespaced identifiers, missing client resources, and a registry mismatch fixture where supported.
- Use English and one deterministic long-string locale fixture. Record locale, GUI scale, logical GUI dimensions, framebuffer dimensions, aspect ratio, resource packs, mod list, accessibility settings, and capture tool.
- Use deterministic Fancy Tags artwork fixtures that are synthetic, non-sensitive, bounded, and safe for repository evidence. Record only fixture ids and authorized alternative text in broad ledgers.
- Raw screenshots, recordings, narration captures, profiles, and runtime logs belong in the Phase 000 approved sanitized evidence location unless a small synthetic test fixture is intentionally tracked. Do not invent a new evidence directory during this phase.
 - If the Minecraft Java client fixture session, input path, narrator, fallback client, or capture facility is unavailable, record the affected client rows as open under conditional `EXT-001`. Do not reuse another operating system evidence, downgrade to headless or mocked proof, or describe Windows or another supported target as unsupported. Non-client host gaps are recorded separately only when those paths are exercised.

### Expected results and rerun order

1. Run inventory and deterministic unit tests before remediation to identify exact failing rows.
2. After each focused repair, rerun its unit tests, affected integration or GameTests, and affected real-client rows.
3. After a shared layout, theme, workflow, protocol, projection, permission, or feedback change, rerun every row named by the invalidation table below.
4. After the last Phase 004 product change, run `./gradlew test`, applicable `./gradlew runGameTestServer`, `./gradlew build`, and `./gradlew compileFallbackRuntimeJava` with the Phase 000 Java 21 environment.
5. Run dedicated-server classloading smoke, then matching enhanced-client startup and navigation smoke, command-fallback smoke, incompatible-protocol behavior, mixed-client equivalence, and the complete visual, input, resize, narration, privacy, authority, revocation, feedback, screenshot, and recording matrix through the Minecraft Java client fixture. Do not repeat client evidence by operating system.
6. Run generated reference comparison if any source-owned reference input changed, inspect the JAR client/common boundaries, scan the complete diff and evidence manifest, then integrate through the repository workflow.

A deterministic test failure is a defect or fixture failure to diagnose. A graphical mismatch is classified against the fixed criteria in this plan, not personal preference. A blocked Minecraft Java client fixture row remains incomplete through `EXT-001`. Historical screenshots, operating system specific client captures, mocked operating-system names, mocked widgets, static source inspection, or headless tests cannot substitute for the required Minecraft Java client workflow.

## Documentation, Operations, and Release

- Update `README.md` when supported enhanced-client behavior, fallback behavior, visible capability, accessibility, compatibility, or known limitation changes.
- Update `DOCUMENTATION.md` with the final screen inventory, layout and state model, focus and narration behavior, permission projection, invalidation lifecycle, enhanced and fallback equivalence, Fancy Tags provenance, HUD ownership, client-thread boundaries, troubleshooting, and exact verification procedures.
- Update `docs/PHASE_9_TESTS.md` for renewed negotiation, pause, HUD, screen, reconnect, mixed-client, and scale evidence. Clearly separate historical evidence from current Phase 004 evidence.
- Update `docs/PHASE_10_TESTS.md` for current universal GUI, picker, typed workflow, panel, control-editor, fallback, narration, focus, resize, and visual results.
- Update `docs/SEF2_ACCEPTANCE.md` and `docs/COMPATIBILITY_MATRIX.md` only with results actually verified at the integrated Phase 004 revision. Do not mark the plan or release complete.
- Keep Linux, macOS, and Windows documented as supported Minecraft Java targets. Report unavailable or failed host proof as a conditional `EXT-001` gap for the affected path and never translate missing evidence into an unsupported-Windows or reduced-platform claim.
- Update `test.md` when exact commands, fixture setup, expected results, evidence locations, or recovery steps change. Preserve it as an executable operator procedure rather than a status diary.
- Update generated command, permission, configuration, or UI references only through their existing generators when their source contracts change. Zero unexplained generated drift is required.
- Update existing Fancy Tags, InvSee, HUD, protocol, accessibility, or troubleshooting topic documents identified by Phase 000. Do not create generic placeholder documentation.
- Record evidence-capture setup, accessibility settings, long-locale fixture, synthetic identities, GUI scale, viewport, client mode, expected result, actual result, commit, and capture id. Keep raw large media outside tracked source unless the repository evidence contract explicitly permits it.
- Document operator recovery for stale screens, permission loss, incompatible clients, missing assets, Fancy Tags cache or transfer errors, reconnect, and command fallback. Recovery instructions must not expose server paths, secrets, hidden identities, or private content.
- No release publication, deployment, marketplace update, production mutation, credential handling, or public capability claim occurs in this phase. The phase records candidate readiness evidence only.

## Risks and Evidence Invalidation

| Risk | Prevention | Detection | Recovery | Evidence invalidated | Reverification |
|---|---|---|---|---|---|
| Fixed layout passes one viewport but clips another | Shared adaptive layout rules and complete viewport matrix | Bounds assertions, screenshots, focus traversal | Repair shared or screen-specific layout | All captures for affected shared primitive or screen | Unit layout tests and every required scale, ratio, and resize capture |
| Visual polish expands into feature work | Bind every change to a failed matrix row and master acceptance criterion | Diff and task-to-defect review | Remove unrelated work and retain it only as excluded future scope | Diff, documentation, and affected test evidence | Focused tests and complete diff audit |
| Unauthorized or hidden data is merely disabled | Server-side omission and per-viewer projection tests | Payload inspection, hidden-target search, count comparison | Repair projection and clear leaked client state | Privacy, picker, panel, HUD, feedback, and mixed-client evidence | Full affected projection, session, visual, and adversarial matrix |
| Shared theme or background change alters every screen | Centralize behavior and inventory all subclasses | Inheritance drift test and screenshot comparison | Restore last valid primitive or repair every dependent screen | Every enhanced screen screenshot and layout result | Complete screen-family visual matrix |
| Workflow or payload repair breaks compatibility | Preserve wire identifiers and use compatible or versioned change | Codec, protocol, incompatible-client, and fallback tests | Revert or add smallest compatible handling | Codec, session, enhanced, fallback, mixed-client, and workflow proof | Full protocol and equivalence matrix |
| Focus or narration works only in English | Deterministic long-locale fixture and semantic labels | Keyboard and narration recordings | Repair label, order, wrapping, or announcement logic | Accessibility captures for affected screens | English and long-locale keyboard, focus, and narration rows |
| HUD obscures critical vanilla UI | Safe anchors, tile caps, priority, coalescing, reduced motion | Overlay coexistence screenshots and bounds tests | Relocate, collapse, or use documented fallback | HUD captures at affected scales and states | Complete HUD and overlay matrix |
| Stale client response overwrites newer state | Request and revision binding, immutable snapshots, owning-thread publication | Out-of-order and rapid-refresh tests | Drop stale response and request current state | Search, page, workflow, permission, and dynamic captures | Affected automated and recorded refresh workflows |
| Enhanced UI and command fallback diverge | Pair every representative workflow by canonical action id and state assertion | Domain and audit comparison | Repair presentation routing without changing action semantics | Enhanced, fallback, command feedback, and audit evidence | Paired route matrix and Phase 003 regression rows |
| Fancy Tags artwork obscures trusted provenance | Separate artwork region, local labels, server-state badges, alternative text | Screenshot and permission review | Remove ambiguous chrome or mark local state clearly | Fancy Tags studio, overlay, glyph, nameplate, and HUD evidence | Complete Fancy Tags visual and lifecycle matrix |
| Command feedback remediation leaks or changes behavior | Use structured components and Phase 003 action contract | Redaction scan, source-class tests, state and audit comparison | Restore semantics, repair projection, quarantine unsafe captures | Affected feedback, security, action, documentation, and evidence rows | Phase 003 regression plus feedback and privacy matrix |
| Client hot path becomes unbounded | Page, index, cache, coalesce, cancel superseded work | Profiler, frame time, queue, payload, and allocation budgets | Bound or move safe work off hot path | Performance and dynamic visual evidence | Maximum legal data, rapid refresh, and uncaptured performance rerun |
| Client class leaks into common or server path | Preserve package and lifecycle isolation | Dedicated-server startup and JAR class-boundary inspection | Remove common reference or isolate client registration | Build, server smoke, JAR, and integration evidence | Unit, build, dedicated-server smoke, and JAR scan |
| Non-client host-specific evidence is unavailable | Preflight `EXT-001` environments and fixtures before runtime execution | Missing environment manifest or native, filesystem, server-lifecycle, dependency, recovery, or packaging capability on an exercised non-client host path | Keep only affected non-client rows open until the required host is available; do not narrow support or declare Windows or another supported target unsupported | Every host-specific claim depending on the missing environment | Run the complete applicable non-client matrix on the restored host |
| Late product fix makes captures stale | Bind every artifact to commit and component invalidation map | Completion audit compares capture commit with integrated commit | Recapture affected and dependent rows | Every artifact linked to changed component or contract | Prescribed focused reruns, then final Phase 004 matrix |
| Evidence contains private or host-specific data | Synthetic fixtures, bounded capture, redaction, approved evidence root | Secret, address, message, path, metadata, and visual inspection | Quarantine and delete unsafe evidence, rotate exposed credentials through their owner if any, recreate sanitized proof | All unsafe evidence and claims derived from it | Evidence scan and affected workflow recapture |

Any defect found in security, persistence, command semantics, or another earlier-phase contract reopens that owning gate and blocks dependent Phase 004 rows. Any Phase 004 change that affects backend lifecycle or cross-channel behavior is explicitly handed to Phase 005 and invalidates its pre-change assumptions.

## Phase Completion Packet

Phase 004 may close only when the packet contains all of the following, stored outside the protected plan set according to the Phase 000 evidence contract:

1. The exact Phase 004 base commit, phase branch commits, integrated merge commit, candidate branch, Minecraft Java client fixture version, architecture, filesystem, Java environment, mod list, locale fixtures, client modes, capture settings, applicable macOS and Windows non-client environment manifests, and artifact identity.
2. The complete UI and administrator-feedback matrix with stable row ids, owners, purposes, state hierarchies, expected results, actual results, defect dispositions, and evidence links.
3. The final list of current graphical classes and protocol or feedback interfaces, including the seven `SefScreen` subclasses, `SefInvSeeScreen`, pause entry, HUD and overlay surfaces, Fancy Tags render surfaces, and Phase 003 action rows.
4. Before-and-after evidence for every remediation, including root cause, changed components, regression tests, screenshots or recordings, rollback, and invalidated evidence that was rerun.
5. Unit and focused integration reports, applicable GameTest results, `./gradlew test`, `./gradlew runGameTestServer`, `./gradlew build`, and `./gradlew compileFallbackRuntimeJava` results after the last product change.
6. Dedicated-server classloading smoke, Minecraft Java client fixture matching enhanced-client startup and navigation smoke, command-fallback smoke, incompatible-protocol behavior, and mixed-client equivalence results. MacOS and Windows server or non-client results are included only when applicable.
7. The complete sanitized Minecraft Java client screenshot manifest for every screen and mandatory state at required GUI scales, viewports, ratios, and resize conditions. This is one portable client evidence set, not separate operating system manifests.
8. The complete Minecraft Java client recording manifest for mouse, keyboard, focus, escape and back, live resize, narration, tooltip, announcements, progress, animation, privacy filtering, server-authority rejection, permission revocation, stale selection, module disable, reconnect, disconnect, server switch, dimension, command-fallback, and administrator-feedback workflows.
9. The Minecraft Java client HUD and overlay coexistence ledger, safe-region proof, reduced-motion results, privacy and permission results, and fallback presentation comparison.
10. The Minecraft Java client enhanced-versus-fallback ledger with canonical action ids, before-and-after domain assertions, authorization results, operator feedback, zero-unauthorized-mutation proof, and redacted audit correlation.
11. The sixteen unavailable-family negative UI and feedback results proving no ready state, preview, confirmation, success affordance, mutation, or false capability claim.
12. Updated `README.md`, `DOCUMENTATION.md`, phase test ledgers, acceptance and compatibility records, `test.md`, affected topic documentation, and generated references, with link and drift results.
13. Complete Git diff and artifact inspection confirming no unrelated changes, secrets, personal data, host paths, runtime worlds, caches, logs, raw oversized evidence, version drift, or dedicated-server client linkage.
14. Issue and milestone state, pull-request link, required check results, private independent-review capability and result disposition, resolution of all findings received from a supported review, merge-commit verification, and signed Phase 004 tag.
15. An evidence invalidation map naming which Phase 005 assumptions and rows must be rerun if any screen, feedback, protocol, projection, permission, action, resource, locale, or client-environment input changes.
16. A Phase 005 handoff stating that security, persistence, command, and UI contracts pass at the integrated revision and identifying any explicit limitations that remain in scope for backend lifecycle and cross-channel integration.
17. Applicable `EXT-001` evidence for the Minecraft Java client fixture and any changed or exercised non-client host-specific path; no macOS or Windows client evidence. An unavailable, failed, incomplete, stale, mocked, or substituted row keeps that affected path open and may not be represented as an unsupported-platform exception.
18. The diagnostics contract version 2 packet with all ten required fields represented, enable, status, disable, authorization, timeout and reset, bounds, drop and truncation, privacy, disabled-overhead, behavior-equivalence, client correlation, support-procedure, and audit-final cleanup results. Every runtime record names the host, runtime directory, exact command or launch configuration, candidate identity, expected result, actual result, evidence location, and teardown state.
19. The inherited `EXT-002` packet identity and a Phase 004 comparison proving dependency, packaging, native API, and runtime-supply inputs did not change, or complete reopened Phase 001 and `EXT-002` closure evidence if they did. `EXT-002` is recorded as available in-plan work and never as a permanent external blocker.

The packet records proof. It does not modify this phase plan, the master plan, or the immutable goal, and it does not authorize release publication.

## Next Transition

After the Phase 004 pull request is merged, verify that the candidate branch contains the merge commit, rerun the required post-merge matching-client and fallback smoke through the Minecraft Java client fixture, confirm the Phase 004 portion of `EXT-001` remains satisfied for Minecraft Java client fixture behavior and any exercised non-client paths, create and push the signed annotated Phase 004 tag, and freeze the completion packet. Then reread the master plan and `phases/plan-phase-005.md` through EOF. Begin `SEFAUD-PHASE-005` only if its entry criteria accept the exact integrated Phase 004 revision and packet.

Do not create or start a Phase 005 branch while the Phase 004 pull request is open, checks are pending or failed, review findings remain unresolved, merge is queued, candidate verification is incomplete, an exercised `EXT-001` row is unavailable or failed, or the signed phase tag is missing. An unexercised conditional host row does not block transition. A Phase 004 defect discovered during transition reopens this phase and invalidates dependent Phase 005 evidence.
