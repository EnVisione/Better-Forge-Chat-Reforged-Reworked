# Phase 001 Security and Supply Chain Closure Execution Plan

> **Plan ID:** PLAN-PHASE-001
> **Phase ID:** SEFAUD-PHASE-001
> **Owner:** Security audit boundary
> **Classification:** MANDATORY
> **Master plan:** [plan.md](../plan.md)
> **Phase sequence:** 001 of 007
> **Diagnostics contract:** 2

## Purpose and Ownership

This phase closes the security, privacy, unintended authority, opened-object writer, and dependency supply chain risks defined by `SEFAUD-REQ-002` and `SEFAUD-REQ-003`. It starts only after `SEFAUD-PHASE-000` has frozen the execution revision and supplied complete trust boundary, data classification, Linux, macOS, Windows, dependency, artifact, and evidence inventories. It ends only when every inventoried boundary and every current remote dependency alert has an evidence based disposition, every confirmed in scope finding has been repaired with regression proof, the native audit writer and runtime dependency contracts pass on canonical Linux and on any changed or exercised macOS or Windows non-client host-specific path, and no known authorization bypass, sensitive data leak, backdoor like route, or applicable critical or high exploitable dependency vulnerability remains. `EXT-001` and `EXT-002` remain separate conditional evidence and provenance records. Upstream-only platform alerts with no direct mod declaration, packaging, or reachable API are recorded as not mod resolvable, while direct mod-owned exposure remains a closure gate.

The master plan owns product scope, decisions, platform pins, phase order, and completion authority. This file owns the dependency ordered execution details, finding rules, evidence expectations, remediation loop, verification gates, and downstream handoff for Phase 001 only. It does not authorize new features, a platform migration, publication, production access, or work owned by later phases.

## Evidence Based Entry State

| Evidence class | Area | Finding | Source or command | Freshness condition |
|---|---|---|---|---|
| OBSERVED | Candidate identity and history | The earlier Phase 001 blueprint recorded `envy/sef2_complete` at `88668786de2bdfbe2699acd1d1307a7351cf5502`. The frozen master preserves the candidate lineage base `0c75bf25c58622096dfa7cc65a5f4b32e6d60ac4`, records the original update-pass start on `envy/phase-001-security` at `e203ece01df76cb076d9112a14a052cac6f4145d`, and observes the contract-amendment branch and remote at `3fd6ce65e7a2610a04b1a0965e89a36cb6905fec`, with Minecraft 1.21.1, NeoForge 21.1.235, Java 21, Parchment 2024.11.17, and product version 2.0.0. | Master plan Sections 1 and 4, `gradle.properties`, `build.gradle`, and `settings.gradle` | Phase 000 must select and freeze the actual execution revision. Any later commit, branch, ancestry, dirty state, wrapper, platform property, manifest, native provider, or dependency declaration change invalidates mapped Phase 001 evidence. |
| OBSERVED | Remote alerts | At authoring, the repository has 26 open Dependabot alerts, comprising twelve high, thirteen medium, and one low severity alert. Candidate applicability and packaged reachability are unresolved. No open code scanning or secret scanning alert was returned by the same preflight. | `SRC-007` and master plan Section 4 | Any alert creation, update, withdrawal, severity change, advisory change, default branch change, or dependency graph change requires a fresh read only snapshot and disposition reconciliation. |
| OBSERVED | Command authority | `KernelServices` assembles the catalog, policy, permission, delegation, alias, bundle, profile, GUI, audit, logging, and domain services. Privileged requests can enter through direct commands and indirect routes. | `src/main/java/com/enviouse/sef/kernel/KernelServices.java`; CodeGraph exploration on 2026-09-01 | Changes to service construction, command registration, policy order, permission providers, source classification, or any indirect execution service invalidate the related traces and adversarial tests. |
| OBSERVED | Permission and delegation | Permission evaluation, dynamic provider behavior, temporary execution grants, hierarchy, cooldown permissions, and delegation are implemented under `com.enviouse.sef.permissions`; sudo, aliases, bundles, and profiles are implemented under `com.enviouse.sef.automation` and `com.enviouse.sef.kernel.command`. | `src/main/java/com/enviouse/sef/permissions`; `src/main/java/com/enviouse/sef/automation`; `src/main/java/com/enviouse/sef/kernel/command`; CodeGraph exploration on 2026-09-01 | A permission identifier, provider rule, wildcard rule, source type, target rule, grant lifetime, profile, compiler, or execution context change invalidates the corresponding authority proof. |
| OBSERVED | GUI and payload boundary | Enhanced UI uses server managed sessions, typed payloads, request guards, panel validation, server projected state, and logical server execution. | `src/main/java/com/enviouse/sef/gui/protocol/SefPayloads.java`, `SefSessionManager.java`, `SessionRequestGuard.java`, `PanelActionValidator.java`, and `SefGuiServer.java`; CodeGraph exploration on 2026-09-01 | Any codec, payload limit, session, sequence, revision, panel, target, action, or handler change invalidates protocol and authority evidence. |
| OBSERVED | Filesystem and content boundary | Shared atomic storage validates managed paths and bounded reads. Fancy Tags owns content addressed image, inbox, export, backup, and restore roots and validates imported content. Configuration owns module files, history, migration, backups, rollback, and watched publication. | `src/main/java/com/enviouse/sef/storage/AtomicFileStore.java`; `src/main/java/com/enviouse/sef/fancytags/FancyTagObjectStore.java`; `FancyTagProjectArchive.java`; `src/main/java/com/enviouse/sef/config/modules/ModuleConfigService.java`; CodeGraph exploration on 2026-09-01 | Any owned root, path check, link policy, archive or image parser, byte or dimension limit, migration rule, backup rule, or publication behavior change invalidates the affected filesystem and content proof. |
| OBSERVED | Audit and privacy | `AuditService` has broad kernel and policy call coverage. Command observation uses `CommandRedactionPolicy`, `CommandEventJournal`, and `FileLogSink`. Current tests cover selected audit, redaction, privacy, path, retention, and export behavior. | `src/main/java/com/enviouse/sef/audit`; `src/main/java/com/enviouse/sef/commandlog`; `src/test/java/com/enviouse/sef/audit/AuditServiceTest.java`; `CommandRedactionPolicyTest.java`; `FileLogSinkTest.java`; CodeGraph exploration on 2026-09-01 | Changes to event schemas, audit callers, correlation, redaction classes, sink filters, retention, export, diagnostics, or failure handling invalidate audit and privacy evidence. |
| OBSERVED | Mixins and access widening | The required Mixin configuration declares common and client injections with `defaultRequire` set to one. The access transformer exposes `ChunkMap.entityMap` and `ChunkMap.TrackedEntity` for verified NeoForge 21.1.235 behavior. | `src/main/resources/sef.mixins.json`; `src/main/resources/META-INF/accesstransformer.cfg` | Any target, injection, accessor, client classification, required count, transformed member, NeoForge pin, or packaged resource change requires a renewed scope, class loading, and artifact review. |
| OBSERVED | Dependency declarations | Runtime platform dependencies come through ModDevGradle and NeoForge. LuckPerms API 5.4, FTB Essentials file 7608733, and Curios file 6529130 are compile only optional integrations. Test dependencies include JUnit 5.10.2, Gson 2.10.1, Mockito 5.12.0, and the JUnit platform launcher. | `build.gradle` | Any repository, plugin, direct dependency, constraint, exclusion, transitivity rule, source set, or generated metadata change invalidates graph and provenance evidence. |
| OBSERVED | Cross-platform native audit writer | The active Phase 001 branch contains Linux and macOS POSIX descriptor handling and Windows handle handling that inspect opened-object identity and link or reparse state. `jna` and `jna-platform` are declared compile only. Linux is the canonical portable evidence host; macOS and Windows runtime proof is required only when the corresponding provider path is changed or exercised. Authoritative NeoForge runtime supply remains separately recorded. | `NativeAuditFileProvider`, `build.gradle`, `SRC-005`, `SRC-010`, and master plan Section 4 | Any provider, native signature, structure layout, flag, identity field, link or reparse check, append, flush, rotation, or runtime dependency change invalidates the affected host path. Compile success alone cannot satisfy an exercised native path. |
| UNKNOWN | Cross-platform runtime prerequisite | The disposable Java 21 and NeoForge 21.1.235 canonical Linux runtime packet is not yet confirmed. macOS and Windows rows are reachability classifications unless Phase 001 changes or exercises a non-client host-specific path, in which case that exact path requires direct evidence on its matching host. | `DEC-009`, `EXT-001`, and the master Goal Creator handoff | Complete the Linux packet in plan. Classify every macOS and Windows path as `NOT_EXERCISED` with source-to-artifact reachability proof or execute its direct host packet. An unavailable required conditional host leaves only its affected row open and never narrows support. |
| UNKNOWN | Platform dependency prerequisite | Authoritative NeoForge artifact provenance, exact runtime supply, advisory applicability, and compatible closure for platform-owned dependencies, including JNA and JNA Platform, are not yet established. | `DEC-009`, `EXT-002`, and the master Goal Creator handoff | Record unresolved platform-owned alerts, missing provenance, missing runtime artifacts, ABI uncertainty, or unavailable compatible remediation in the separate ledger. A finding blocks Phase 001 only when the mod directly declares, packages, or reaches an applicable vulnerable component. Absence from the mod JAR alone is not closure. |
| OBSERVED | Historical security claim | The tracked review reports no unresolved finding in its reviewed scope and lists prior critical, high, and medium repairs, but it is historical evidence rather than proof for the Phase 001 frozen revision. | `docs/SECURITY_REVIEW.md`, `audit.md`, and master plan Section 4 | The review becomes current only after all Phase 001 rows are rerun at the frozen repaired commit and the document is reconciled to that evidence. |
| OBSERVED | Dependency evidence tooling | `build.gradle` declares JNA and JNA Platform 5.14.0 as `compileOnly`, provides `generateAuditDependencyManifest`, verifies runtime ownership through `net.neoforged:minecraft-dependencies:1.21.1`, hashes resolved artifacts, and fails if the candidate JAR embeds JNA classes or native libraries. | `build.gradle`; `src/test/java/com/enviouse/sef/docs/BuildInventoryGenerator.java` | This is evidence enablement, not `EXT-002` closure. A declaration, task, expected owner, or artifact-layout change invalidates the generated manifest and every dependent alert disposition. |
| OBSERVED | Native-writer evidence tooling | `NativeAuditFileProvider` has POSIX descriptor and Windows handle implementations, and `runAuditWriterProbe` executes `AuditRuntimeProbe` against the packaged candidate JAR. | `src/main/java/com/enviouse/sef/audit/NativeAuditFileProvider.java`; `src/test/java/com/enviouse/sef/audit/AuditRuntimeProbe.java`; `build.gradle` | Static source and a probe task do not prove a host path. Linux must execute the canonical packet; macOS or Windows executes only when its non-client provider path changed or was exercised. |
| OBSERVED | Documentation drift risk | Current user and acceptance documents still describe an unconditional Linux, macOS, and Windows pull-request runtime matrix. That evidence wording conflicts with `DEC-009`, which keeps all targets supported but requires direct macOS or Windows runtime evidence only for changed or exercised non-client host-specific paths. | `README.md`, `DOCUMENTATION.md`, `docs/SEF2_ACCEPTANCE.md`, `docs/SECURITY_REVIEW.md`, and `test.md` | `P001-TASK-013` must reconcile affected claims from verified evidence without adding operating-system-specific Minecraft client procedures or declaring a target unsupported. |
| UNKNOWN | Phase 000 outputs | The exact frozen execution commit, artifact inputs, complete trust boundary inventory, data classes, resolved graphs, evidence location, and invalidation manifest do not exist until Phase 000 closes. | `SEFAUD-PHASE-000` completion packet | Phase 001 cannot start until every required Phase 000 output is present, internally consistent, sanitized, and integrated. |

No historical result is `VERIFIED` for Phase 001 merely because it passed at another revision. Each claim must be tied to the frozen execution commit, exact environment, fixture manifest, command or workflow, actual result, and evidence location.

## Scope Boundaries

### Included Scope

- `SEFAUD-REQ-002`. Threat model, trace, inspect, repair, and adversarially verify every Phase 000 authority, command execution, permission, source, target, delegation, GUI, payload, configuration, migration, recovery, persistence security, filesystem, archive, image, reflection, mixin, access transformer, optional integration, log, audit, export, privacy, and packaged artifact boundary.
- `SEFAUD-REQ-002`. Classify protected data and trace it from source through validation, transformation, storage, observation, export, error handling, generated references, and packaging. Any confirmed authorization bypass, sensitive data leak, or backdoor like route is mandatory remediation regardless of severity.
- `SEFAUD-REQ-002`. Maintain a stable finding ledger, prove each disposition, repair every confirmed in scope finding, add regression evidence at the real failure boundary, and rerun every invalidated row.
- `SEFAUD-REQ-002`, `DEC-009`, and `EXT-001`. Audit and remediate every security-sensitive audit writer so validation and mutation remain bound to the same opened descriptor or handle. Prove native ABI, flags, structure layout, opened-object identity, regular-file type, link count or reparse state, object stability, bounded append, flush, rotation, failure preservation, and restart on canonical Linux and on any changed or exercised macOS or Windows provider path. Missing identity metadata or native capability fails closed without a path-only fallback.
- `SEFAUD-REQ-003`. Capture all resolved Gradle compile, runtime, test, tooling, fallback, and packaged graphs; reconcile the twelve high, thirteen medium, and one low alerts present at authoring plus every alert current when execution begins; and determine candidate and artifact applicability.
- `SEFAUD-REQ-003`. Repair every applicable dependency finding through the smallest compatible declaration, constraint, exclusion, or upstream resolution that preserves `DEC-004`; verify optional integration contracts, licenses, provenance, and packaged contents.
- `SEFAUD-REQ-003`, `DEC-009`, and `EXT-002`. Keep JNA and JNA Platform compile only, prove that pinned NeoForge 21.1.235 supplies the exact binary-compatible APIs and native runtime for each exercised provider path, and prove that the mod JAR embeds no duplicate JNA classes or native libraries. Give every platform-owned alert separate candidate graph, packaged-mod-artifact, installed-runtime-artifact, affected-API reachability, authoritative advisory applicability, provenance, and compatible-remediation dispositions. Classify an upstream-only unreachable alert as not mod resolvable; directly mod-owned applicable exposure remains mandatory remediation.
- Phase owned documentation, sanitized evidence, compatibility notes, security review, and repository integration needed to make the repaired security and supply chain state reviewable and reproducible.

### Explicit Exclusions

- `FUT-001` and `NG-001`. Do not implement any of the sixteen intentionally unavailable control families. Their presence in a security trace only requires proof that they remain unavailable, unreachable, permission safe, and side effect free.
- `FUT-002`. Do not add unrelated commands, screens, integrations, controls, or gameplay behavior.
- `FUT-003`. Do not perform broad architecture modernization or class decomposition. A focused refactor is allowed only when it is the smallest safe way to repair a confirmed finding or make mandatory evidence possible.
- `NG-002` and `DEC-004`. Do not change Minecraft 1.21.1, NeoForge 21.1.235, Java 21, Parchment 2024.11.17, the checked in Gradle wrapper, loader, protocol boundary, fallback client contract, or optional integration contract. If an applicable exploitable dependency cannot be repaired inside these pins, record a blocker and request an owner decision instead of silently widening scope.
- `DEC-009`. Do not narrow Linux, macOS, or Windows support, call Windows unsupported, substitute an operating-system name mock for native execution, use a path-only security fallback, embed another JNA runtime, or treat platform ownership or absence from the mod JAR as dependency closure.
- `NG-003` and `DEC-005`. Do not publish, deploy, use production systems, mutate a production world, or run destructive tests outside disposable synthetic staging fixtures.
- `NG-005`. Do not use or preserve real credentials, private messages, personal addresses, production data, or unrelated user information. No task requires reading secret values.
- Phase 002 persistence completeness, Phase 003 universal administrator action execution, Phase 004 UI polish, Phase 005 full lifecycle convergence, Phase 006 final clean checkout matrix, and Phase 007 final documentation closure remain owned by their later phases. Phase 001 must still run the security and compatibility evidence necessary for its own changes and hand off every invalidated downstream row.
- Dismissing remote alerts, publishing advisories, public disclosure of exploit details, release publication, marketplace upload, and deployment are not authorized by this phase.

## Phase Contract

### SEFAUD-PHASE-001 — Security, Privacy, and Supply Chain Closure

**Objective:** At one integrated repaired candidate revision and artifact digest, give every Phase 000 security boundary and every current dependency alert an evidence based disposition, repair every confirmed in scope finding, prove provider-specific or native opened-object audit writing and pinned-runtime native dependency supply on canonical Linux and on any changed or exercised macOS or Windows non-client host-specific path, and prove through adversarial, runtime, scanning, dependency, and artifact evidence that no known authorization bypass, sensitive data leak, backdoor like route, or applicable critical or high exploitable dependency vulnerability remains.
**Owner:** Security audit boundary
**Dependencies:** SEFAUD-PHASE-000, SEFAUD-REQ-001, DEC-009
**Canonical requirements:** SEFAUD-REQ-002, SEFAUD-REQ-003
**Documentation and release impact:** Update the current security review, acceptance evidence, dependency and compatibility facts, operator security and recovery guidance, and release verification instructions when actual behavior changes. Produce no release and make no production change.
**Next transition:** `SEFAUD-PHASE-002`

**Entry criteria**

- `SEFAUD-PHASE-000` is fully integrated through the required pull request workflow, its checks pass, the resulting candidate branch commit is verified, and its signed phase tag exists.
- The Phase 000 completion packet supplies the frozen commit and dirty state, sanitized baseline manifest, trust boundary matrix, source to sink candidates, data classification, permission and source inventories, durable owner and filesystem inventories, dependency graph baseline, remote alert snapshot, artifact input manifest, evidence storage rules, and traceability matrix.
- The Phase 000 matrices contain no missing, duplicated, or unowned mandatory boundary. A Phase 001 discovery that exposes an omission reopens Phase 000 and stops Phase 001 remediation until the baseline is corrected and reintegrated.
- The work begins from the latest approved candidate branch state, preserves unrelated user changes, and uses a sequential `envy` phase branch created only after the prior phase is merged and tagged.
- Disposable synthetic server, client, provider, filesystem, archive, image, migration, and network fixtures are available. A missing required fixture remains a blocker and cannot be replaced with lower fidelity evidence.
- Phase 000 supplies explicit Linux, macOS, and Windows target rows, with Linux canonical and conditional host-specific evidence routing. The current availability of `EXT-001` and `EXT-002` is recorded before work begins. Unknown conditional evidence is not represented as satisfied, but it does not block unaffected portable work.
- The frozen dependency baseline distinguishes candidate declarations, the packaged mod artifact, and the installed NeoForge runtime. JNA and JNA Platform ownership, compile-only declarations, expected runtime provider, native library loading, and duplicate-runtime prohibitions are explicit.

**Implementation scope**

- `SEFAUD-REQ-002` owns the complete threat model, source to sink traces, source and target authority review, indirect execution review, data leak review, trust boundary adversarial tests, mandatory finding remediation, and security evidence.
- `SEFAUD-REQ-002` also owns provider-specific or native opened-object audit writer review and remediation. Canonical Linux receives direct POSIX ABI and runtime proof. macOS may share POSIX implementation structure and Windows may use its handle implementation, but either host receives direct proof only when Phase 001 changes or exercises that non-client host-specific path. Otherwise its row closes as `NOT_EXERCISED` from source-to-artifact reachability evidence. Unsupported identity metadata fails closed and never selects a path-only writer.
- `SEFAUD-REQ-003` owns resolved candidate and installed-runtime graph capture, all current alert dispositions, compatible dependency remediation, packaged and installed reachability, authoritative advisory applicability, license and provenance review, optional integration compatibility, compile-only JNA ownership, duplicate-runtime exclusion, and supply chain evidence.
- All repairs preserve logical server authority, fail closed behavior, audit correlation, bounded resource use, dedicated server class loading safety, save compatibility, and the frozen platform contract. This work is governed jointly by `SEFAUD-REQ-002` and `SEFAUD-REQ-003`.

**Execution order**

1. `P001-TASK-001` validates the Phase 000 completion packet and freezes the Phase 001 evidence context for `SEFAUD-REQ-002` and `SEFAUD-REQ-003`.
2. `P001-TASK-002` builds the threat model, source to sink graph, abuse cases, finding ledger, and severity rules from every Phase 000 boundary for `SEFAUD-REQ-002`.
3. `P001-TASK-003` audits direct and indirect authority across sources, targets, permissions, hierarchy, exemption, delegation, aliases, bundles, profiles, sudo, scheduled work, and GUI convergence for `SEFAUD-REQ-002`.
4. `P001-TASK-004` audits payload, session, GUI projection, codec, replay, stale state, rate, size, and thread boundaries for `SEFAUD-REQ-002`.
5. `P001-TASK-005` audits configuration, migration, recovery, durable authority, deserialization, revision, and time of check behavior for security impact under `SEFAUD-REQ-002`.
6. `P001-TASK-006` audits filesystem roots, links, archives, images, imports, exports, object storage, host path disclosure, and every provider-specific or native opened-object audit writer for `SEFAUD-REQ-002`, `DEC-009`, and `EXT-001`.
7. `P001-TASK-007` audits optional integrations, native API loading, reflection, mixins, access transformers, logical side boundaries, ABI assumptions, and compatibility containment for `SEFAUD-REQ-002`, `SEFAUD-REQ-003`, `DEC-009`, `EXT-001`, and `EXT-002`.
8. `P001-TASK-008` audits sensitive data, logs, audit, observation, privacy projection, exports, exceptions, fixtures, generated references, and evidence custody for `SEFAUD-REQ-002`.
9. `P001-TASK-009` captures all resolved Gradle and installed NeoForge runtime graphs and gives every current dependency alert a candidate, packaged-mod, installed-runtime, reachability, advisory, provenance, and compatible-remediation disposition for `SEFAUD-REQ-003` and `EXT-002`.
10. `P001-TASK-010` implements the mandatory remediation set in severity and dependency order, with a focused regression for every finding and a compatibility or recovery plan for every contract change under `SEFAUD-REQ-002` and `SEFAUD-REQ-003`.
11. `P001-TASK-011` executes the consolidated adversarial, concurrency, interruption, provider, native-writer, performance, and privacy regression matrix against the repaired revision. It completes every unblocked row, routes portable behavior through the Minecraft Java client fixture and canonical Linux, and adds direct macOS or Windows runtime proof through `EXT-001` only for changed or exercised non-client host-specific paths for `SEFAUD-REQ-002` and `SEFAUD-REQ-003`.
12. `P001-TASK-012` runs security scanning, resolved candidate and installed-runtime graph renewal, cross-platform runtime compatibility checks, `EXT-002` provenance and advisory closure, and final source and JAR content, reachability, license, provenance, secret, path, reflection, mixin, access transformer, JNA ownership, native library, and duplicate entry inspection for `SEFAUD-REQ-002` and `SEFAUD-REQ-003`.
13. `P001-TASK-013` reconciles documentation and sanitized evidence and records exact downstream invalidation for `SEFAUD-REQ-002` and `SEFAUD-REQ-003`.
14. `P001-TASK-014` assembles the completion packet and integrates Phase 001 through the repository phase workflow after `SEFAUD-REQ-002` and `SEFAUD-REQ-003` satisfy their phase gates.

Tasks 003 through 009 may be investigated in parallel after Task 002 freezes identifiers and evidence rules. They may not independently alter shared policy or dependency declarations. All confirmed findings converge through Task 010, where remediation order is explicit and overlapping changes are reconciled. Tasks 011 through 014 are sequential because each consumes the final output of the previous gate.

**Required evidence**

- Complete threat boundary and source to sink matrices, stable finding identifiers, severity, affected invariant, exploit precondition, source and sink, disposition, repair commit, regression proof, and invalidation scope.
- Permission, source, target, hierarchy, exemption, delegation, indirect execution, payload, parser, session, stale state, filesystem, archive, image, configuration, migration, recovery, reflection, mixin, access transformer, integration, audit, redaction, privacy, export, and artifact tests at their real boundaries.
- Resolved compile, runtime, fallback, test, tooling, and packaged dependency graphs; one row for every alert current at execution; advisory sources; dependency path; candidate configuration; packaged presence; affected API; reachability; exploit precondition; disposition; and remediation proof.
- Clean compatible builds plus dedicated server, matching enhanced client, fallback client, incompatible protocol, optional integration present and absent, and changed dependency runtime evidence as applicable.
- Read only code scanning, secret scanning, and remote alert snapshots, along with repository and JAR secret shape, private path, unexpected resource, duplicate entry, mixin, access transformer, license, and provenance inspections.
- Updated security and compatibility documentation tied to the repaired commit, with limitations, evidence invalidation, migration or rollback guidance where applicable, and no sensitive exploit payload or secret value.
- An `EXT-001` packet for canonical Linux and any affected macOS or Windows non-client host-specific path tied to one commit and SHA-256 and SHA-512 artifact digest. Each executed host record names operating-system version, architecture, filesystem, Java, NeoForge, resolved runtime, native ABI and symbols, fixture identity, exact workflow, expected and actual result, and sanitized evidence location. It proves safe regular-object append and flush, rotation and restart, symbolic-link or reparse rejection, detectable hard-link rejection, object-swap resistance, metadata-unavailable failure, write and flush failure preservation, and stable identity of the same opened descriptor or handle before and after mutation.
- An `EXT-002` packet with authoritative NeoForge coordinates, repositories, metadata, installed artifacts, licenses or provenance, hashes, compile and runtime paths, JNA and JNA Platform API and binary compatibility, native loading results on canonical Linux and any affected macOS or Windows non-client host-specific paths, mod-JAR duplicate-runtime absence, and separate alert closure gates for candidate graph, packaged mod, installed runtime, affected API reachability, authoritative advisory applicability, and compatible remediation. An upstream-only unreachable alert is recorded as not mod resolvable; direct mod-owned exposure remains mandatory remediation.

**Exit criteria**

- Every Phase 000 trust boundary has a completed source to sink and abuse case disposition. No boundary is missing, duplicated, unowned, or represented only by historical evidence.
- Every current remote alert, including the authoring snapshot of twelve high, thirteen medium, and one low alerts, is mapped to the frozen candidate graph and receives an exact disposition supported by dependency path, configuration, packaged presence, affected API, reachability, advisory evidence, and candidate behavior.
- Every confirmed in scope source or dependency finding is repaired with a focused regression and all invalidated evidence is rerun. No risk acceptance, documentation only workaround, alert dismissal, or lower fidelity result substitutes for remediation.
- No known authorization bypass, sensitive data leak, unintended authority path, or applicable critical or high exploitable vulnerability remains. No other confirmed mandatory Phase 001 finding remains open at any severity.
- Minecraft, NeoForge, Java, Parchment, Gradle wrapper, enhanced protocol, fallback client, and optional integration boundaries still match `DEC-004`.
- The canonical Linux native-writer row passes against the same candidate artifact used by the phase. Every macOS and Windows row is either `NOT_EXERCISED` with frozen source-to-artifact reachability proof or, when its non-client provider path changed or was exercised, passes the equivalent direct host packet. Every executed provider validates and mutates the same opened object, rejects unsafe link or reparse state and substitution, fails closed when identity or type metadata is unavailable, and preserves prior valid state after injected failure. No path-only fallback exists.
- `EXT-001` and `EXT-002` remain separate conditional evidence and provenance records. A missing host leaves only an affected host-specific row open. An unresolved native ABI, absent runtime API, unresolved authoritative provenance, partial platform-owned alert gate, or applicable alert without compatible remediation blocks Phase 001 only when the mod directly declares, packages, or reaches the affected component. Windows and the other supported targets remain supported and are never converted to exclusions.
- JNA and JNA Platform remain compile only, the pinned NeoForge runtime supplies the exact compatible APIs and native runtime for each exercised provider path, and the mod JAR contains no duplicate JNA classes or embedded native implementation.
- Every platform-owned alert separately passes candidate graph, packaged-mod-artifact, installed-runtime-artifact, affected-API reachability, authoritative advisory applicability, provenance, and compatible-remediation gates. Platform ownership, transitivity, absence from the mod JAR, or reduced operating-system scope is not a passing disposition.
- The repaired source and JAR pass security scanning, secret and private path inspection, dependency and duplicate class inspection, mixin and access transformer inspection, license and provenance review, relevant runtime compatibility, and required build and test gates.
- Documentation and sanitized evidence describe only verified Phase 001 behavior. Sensitive findings are routed privately, and no credential, personal data, exploit secret, private host path, or unsafe raw log enters tracked artifacts or public GitHub surfaces.
- The Phase 001 pull request passes all required checks and, after private independent-review capability preflight, either receives a passing supported review or records the explicit optional unsupported disposition. It merges through GitHub with the required merge method, the resulting candidate branch commit is verified, and a signed annotated Phase 001 tag is pushed before Phase 002 begins.
- No known mandatory phase owned defect remains.

## Inputs and Upstream Contracts

| Input or contract | Provider | Required state | Validation | Failure behavior |
|---|---|---|---|---|
| Frozen execution revision | `SEFAUD-PHASE-000` | Exact branch, commit, ancestry, dirty state, remote state, Java, platform, and wrapper identity are recorded. | Compare Git, Gradle properties, generated metadata inputs, and Phase 000 manifest without changing state. | Stop on mismatch. Reopen Phase 000 if the baseline is stale or contaminated. Preserve unrelated user files. |
| Trust boundary and data flow inventory | `SEFAUD-PHASE-000` and `SEFAUD-REQ-001` | Every command, authority, payload, filesystem, configuration, persistence security, integration, observation, export, and artifact boundary has one owner and one stable row. | Deterministic inventory drift check and manual reconciliation to source and runtime entry points. | A missing, duplicate, or unowned row is a Phase 000 failure. Do not continue with a partial threat model. |
| Data classification and evidence rules | `SEFAUD-PHASE-000`, `DEC-005` | Sensitive classes, permitted synthetic markers, raw evidence location, retention, access, sanitization, and deletion rules are explicit. | Review the evidence manifest before fixture creation or capture. | Stop privacy sensitive testing until a safe fixture and evidence route exists. Quarantine unsafe capture without printing its content. |
| Permission, source, target, and indirect route inventory | `SEFAUD-PHASE-000` | All source types, policy checks, target classes, delegation routes, aliases, bundles, profiles, GUI actions, integrations, and lifecycle callbacks are mapped to canonical actions. | Reconcile the live dispatcher, sealed catalog, shortcut set, policy pipeline, and Phase 000 counts. | Any disagreement is a finding or Phase 000 inventory defect. Never infer authority from documentation alone. |
| Filesystem, archive, image, configuration, migration, and recovery inventory | `SEFAUD-PHASE-000` | Every owned root, input parser, output sink, link policy, bound, migration, recovery path, and data class is identified. | Compare the inventory to `AtomicFileStore`, `ModuleConfigService`, Fancy Tags storage and archive components, file logging, exports, and every other Phase 000 owner. | Stop destructive cases if target ownership or disposable fixture identity is ambiguous. Rebaseline missing owners. |
| Resolved dependency and artifact baseline | `SEFAUD-PHASE-000` | Every relevant Gradle configuration, plugin and tool dependency, optional integration, artifact input, repository, and packaged entry is captured. | Reproduce graph reports without dependency declaration changes and bind them to the frozen commit. | A resolution mismatch or unavailable repository is a failed row. Do not reuse cached output as proof without verifying coordinates and checksums. |
| Remote security snapshot | `SRC-007` refreshed through the repository preflight | Open alerts, advisory metadata, code scanning, secret scanning, and repository branch context are captured read only. | Search every current alert and compare its dependency path and branch basis with the candidate graphs. | Keep the alert row open if remote evidence is unavailable or ambiguous. Never dismiss or suppress it to close the phase. |
| Pinned compatibility contract | `DEC-004` | Minecraft 1.21.1, NeoForge 21.1.235, Java 21, checked in wrapper, Parchment 2024.11.17, enhanced and fallback behavior, and optional integration contracts remain fixed. | Compare `gradle.properties`, `build.gradle`, wrapper properties, generated mod metadata, protocol declarations, and compatibility documentation. | Block any repair that requires a pin change and request an owner authorized plan decision. |
| Mandatory cross-platform and native dependency contract | `DEC-009` | Linux, macOS, and Windows remain mandatory. Security-sensitive writes use provider-specific or native opened-object identity. JNA and JNA Platform remain compile only and platform supplied without an embedded duplicate. Platform-owned alerts retain separate closure gates. | Reconcile Phase 000 operating-system and provider rows, build declarations, native API use, artifact inputs, and support claims with the master. | Stop any path-only fallback, unsupported-Windows claim, embedded native runtime, silent platform pin change, or collapsed platform-alert disposition. |
| Disposable native runtime evidence | `EXT-001` | One exact commit and artifact has canonical disposable Java 21 and NeoForge 21.1.235 Linux manifests and native-writer evidence, with macOS or Windows manifests when changed or exercised provider paths require direct proof. | Validate host, architecture, filesystem, Java, platform, resolved runtime, fixture, artifact hashes, exact commands, identity metadata, append, flush, failure, rotation, shutdown, and restart records for each executed path. | Continue unblocked work while unknown. Mark only affected host-specific rows open and prohibit Phase 001 exit for an exercised path until its evidence passes. Never call a missing host unsupported. |
| Authoritative platform dependency evidence | `EXT-002` | Exact NeoForge artifacts, repositories, hashes, licenses or provenance, runtime supply, advisory applicability, and compatible remediation classify every platform-owned alert and JNA runtime dependency. | Compare authoritative metadata with compile and runtime graphs, installed artifacts, affected APIs, JAR entries, canonical Linux native loading, and direct macOS or Windows results only for changed or exercised non-client host-specific paths. | Treat missing ownership, provenance, or reachability proof as ordinary unfinished in-plan work, not a permanent external blocker. Record an upstream-only, nonreachable platform-owned alert as `NOT_MOD_RESOLVABLE` only after all required gates prove it; direct mod-owned applicable exposure remains a Phase 001 blocker. |
| Safe staging contract | `DEC-005` | All destructive, adversarial, privacy sensitive, migration, archive, filesystem, provider, and network tests use disposable synthetic fixtures. | Record fixture identity and path preflight without capturing secret values. | Abort the destructive action when a path, world, account, provider, or data source is not provably disposable. |

## Outputs and Downstream Contracts

| Output or contract | Consumer | Guaranteed state | Compatibility or versioning | Evidence |
|---|---|---|---|---|
| Closed security boundary matrix | `SEFAUD-PHASE-002` through `SEFAUD-PHASE-007` | Every Phase 000 boundary has an owner, trace, abuse cases, disposition, repair if needed, regression proof, and invalidation rules. | Stable boundary and finding IDs survive later reruns. Behavior and public identifiers remain compatible unless a documented minimal repair requires versioning. | Sanitized matrix tied to repaired commit and exact tests. |
| Security finding ledger | All later phases and repository review | Every confirmed finding has severity, invariant, source, sink, preconditions, impact, remediation, regression, disposition, and disclosure routing. No mandatory finding is open. | Findings use `P001-FIND-###`. Merged or duplicate rows retain their identifiers and cross references. | Ledger summary, repair commits, focused tests, and review resolution. |
| Permission and execution assurance | `SEFAUD-PHASE-003` and `SEFAUD-PHASE-005` | Direct and indirect privileged paths fail closed, recheck current authority before mutation, preserve actor attribution, and cannot retain stale or delegated authority. | Canonical action, permission, source, target, confirmation, and audit identifiers stay stable or receive an explicit compatible migration. | Source to sink traces, permission matrix, delegation and route regression evidence. |
| Filesystem and privacy security contract | `SEFAUD-PHASE-002`, `SEFAUD-PHASE-004`, and `SEFAUD-PHASE-007` | Owned paths, links, imports, archives, images, logs, exports, configuration, migration, recovery, and evidence captures enforce the verified security and privacy rules. | Any schema or configuration change is versioned, recoverable, and documented while the platform remains pinned. | Path, parser, migration, redaction, privacy, and recovery regressions. |
| Cross-platform opened-object writer contract | `SEFAUD-PHASE-002`, `SEFAUD-PHASE-005`, and `SEFAUD-PHASE-006` | Linux, macOS, and Windows providers bind validation, append, and flush to the same opened descriptor or handle, reject unsafe link or reparse state and substitution, and fail closed when identity metadata is unavailable. | Provider identifiers and serialized audit behavior remain compatible. Any native ABI, JNA, provider, flag, structure, identity, link, append, or flush change invalidates the affected host evidence. | `EXT-001` manifests, ABI and symbol records, success and adversarial traces, before and after object identity, failure preservation, rotation, shutdown, and restart evidence. |
| Closed dependency alert table | `SEFAUD-PHASE-006` and `SEFAUD-PHASE-007` | Every current alert has a candidate-specific evidence-based disposition. Platform-owned alerts separately close candidate graph, packaged mod, installed runtime, affected-API reachability, authoritative advisory applicability, provenance, and compatible remediation. No applicable critical or high exploitable vulnerability remains. | Dependency remedies preserve `DEC-004`, `DEC-009`, optional status, licenses, provenance, operating-system support, and runtime contracts. | Candidate and platform graphs, installed artifacts, advisory sources, `P001-DEP-###` rows, compatibility tests, `EXT-002`, and remote snapshot. |
| Repaired dependency and artifact manifest | `SEFAUD-PHASE-006` | Exact dependency coordinates, paths, computed artifact checksums, packaged presence, licenses, provenance, artifact entries, mixins, access transformer, and source commit binding are recorded. | A later dependency, resource, plugin, build, or packaging change invalidates this manifest. | Final graph reports, JAR inventory, SHA 256 and SHA 512, license and provenance ledger. |
| Native runtime ownership manifest | `SEFAUD-PHASE-006` and `SEFAUD-PHASE-007` | JNA and JNA Platform remain compile only, exact compatible APIs and native libraries come from pinned NeoForge 21.1.235, canonical Linux proves runtime loading, every changed or exercised macOS or Windows non-client provider path proves its matching runtime, and the mod JAR embeds no duplicate implementation. | Any NeoForge artifact, JNA API use, native load path, classpath, packaging, reachability classification, or executed host result change reopens `EXT-001`, `EXT-002`, Task 009, and Task 012. | Authoritative coordinates and hashes, compile and installed-runtime paths, source-to-artifact reachability, applicable ABI and native-load evidence, JAR class and native-library inventory, and duplicate scan. |
| Evidence invalidation map | Every later phase | Each source, config, schema, protocol, dependency, test harness, or artifact change names the security and supply chain rows that must be rerun. | Invalidation accumulates through later phases and never converts stale proof into current proof. | Component to evidence mapping in the completion packet. |
| Phase integration state | `SEFAUD-PHASE-002` | Phase 001 is merged, checks and review are resolved, candidate branch verification passes, and the signed phase tag identifies the exact integrated commit. | Phase 002 starts only from the updated approved candidate branch. | Pull request, checks, merge commit, candidate verification, and signed annotated tag. |

## Finding and Alert Disposition Rules

### Security finding classification

Each suspected issue receives a stable `P001-FIND-###` identifier before remediation. Severity does not decide whether a confirmed in scope issue is fixed. It controls response order, evidence handling, and disclosure sensitivity.

| Severity | Phase 001 meaning | Required response |
|---|---|---|
| Critical | Reliable arbitrary privileged command or code execution, persistent unauthorized authority, broad secret disclosure, destructive path escape, or equivalent compromise with practical preconditions. | Stop unrelated remediation, preserve sanitized evidence, notify the owner privately, repair first, run the full affected adversarial and compatibility set, and do not publish exploit detail. |
| High | Practical authorization bypass, sensitive data disclosure, integrity compromise, cross actor mutation, or exploitable dependency path with material server impact. | Repair before medium and low findings unless a dependency order requires a prerequisite. Rerun every affected boundary and runtime path. |
| Medium | Bounded security or privacy failure, significant defense gap, unsafe failure mode, or denial of service requiring constrained preconditions. | Repair in this phase and add focused regression. It cannot be deferred merely because it is not part of the final critical or high wording. |
| Low | Narrow but real contract violation with limited impact or difficult preconditions. | Repair in this phase and add focused regression. Record why impact is limited without weakening the invariant. |
| Informational | No security contract violation, but a verified observation, hardening note, or later phase evidence need. | Record as an observation, not a confirmed finding. Route genuine later phase work to its existing requirement without using it to hide an open Phase 001 defect. |

Valid dispositions are `confirmed and repaired`, `not reproducible at frozen revision`, `duplicate of`, `inapplicable with proof`, and `blocked`. A `blocked` mandatory row prevents exit. `Accepted risk`, `historical`, `transitive only`, `default branch only`, `test only`, and `not packaged` are not sufficient dispositions without the required technical evidence. A confirmed authorization bypass, sensitive data leak, unintended authority path, or other `SEFAUD-REQ-002` violation is mandatory remediation at every severity.

### Dependency alert disposition

Each remote alert receives a stable `P001-DEP-###` row that preserves the remote alert identifier. The table must include advisory identifier and authoritative source, severity at capture, affected coordinate and range, every candidate dependency path, resolved configurations, whether the component is direct or transitive, packaged class or metadata presence, affected API, static and runtime reachability, exploit preconditions, candidate applicability, chosen compatible remedy, tests, remote state, and invalidation triggers.

An alert may be `inapplicable with proof` only when exact graph and artifact evidence establishes absence, the vulnerable configuration or API is not used, or the advisory does not apply to the pinned candidate. Compile only, test only, tooling, and default branch differences must still be recorded. An applicable finding must be repaired even when transitive. If a critical or high finding is applicable and exploitable but no compatible remedy exists inside `DEC-004`, Phase 001 is blocked and the owner must decide whether to revise the platform contract. No alert may be dismissed remotely as a substitute for candidate evidence.

A platform-owned alert retains the same `P001-DEP-###` identity but has seven independently recorded gates: resolved candidate graph, packaged mod artifact, installed NeoForge runtime artifact on canonical Linux and any affected or exercised macOS or Windows non-client host-specific path, affected-API source and runtime reachability, authoritative advisory applicability, authoritative provenance, and compatible remediation within `DEC-004`. The provenance gate names authoritative repository origin, coordinates, metadata, license where available, SHA-256, SHA-512, compile path, runtime path, and supplying NeoForge artifact. A row is not closed merely because the dependency is transitive, compile only, loader supplied, absent from the mod JAR, development overridden, or owned upstream. Missing authoritative evidence or unresolved compatible remediation is `blocked` under `EXT-002`.

## Work Packages

| Task ID | Requirement IDs | Work | Inputs and dependencies | Outputs | Affected components or interfaces | Verification |
|---|---|---|---|---|---|---|
| `P001-TASK-001` | `SEFAUD-REQ-002`, `SEFAUD-REQ-003` | Validate Phase 000 integration and completion packet. Freeze the Phase 001 commit, branch, artifact inputs, dependency cache provenance, staging fixtures, evidence location, sensitive evidence routing, Linux, macOS, and Windows provider rows, `EXT-001` and `EXT-002` status, and invalidation baseline. | Phase 000 completion packet, `DEC-004`, `DEC-005`, `DEC-006`, `DEC-009`, `EXT-001`, `EXT-002` | Phase 001 evidence header and a pass, blocked, or stop entry decision that distinguishes unblocked work from exit prerequisites. | Repository identity, Git state, Gradle and platform metadata, Phase 000 inventories, operating-system manifests, native-provider inventory, platform dependency inventory, and evidence manifest. | All identifiers and hashes reconcile. Any dirty or remote divergence is explained without modifying unrelated state. A missing host or prerequisite is recorded as blocked, never excluded or unsupported. |
| `P001-TASK-002` | `SEFAUD-REQ-002` | Expand every trust boundary into actors, assets, entry points, trust transitions, validation, policy, mutation, persistence, observation, error, recovery, and output sinks. Trace protected values and authority from source to final sink. Add normal, denied, stale, forged, replayed, oversized, concurrent, interrupted, provider failure, indirect execution, native ABI, opened-object substitution, link or reparse, platform-runtime supply, and evidence-custody abuse cases. | `P001-TASK-001`, Phase 000 boundary, native-provider, dependency, and data class matrices | Complete threat model, source to sink matrix, attack tree, finding ledger, and test route for every boundary. | All Phase 000 owners; logical server, client, filesystem, native provider, platform dependency, build, evidence, and artifact boundaries. | Independent row count and ownership reconciliation. Missing sinks, provider variants, native calls, runtime suppliers, unbounded transitions, or undocumented authority become findings. |
| `P001-TASK-003` | `SEFAUD-REQ-002` | Trace direct and indirect privileged execution. Prove source classification, feature, permission, explicit deny, wildcard, hierarchy, exemption, target visibility, cooldown, quota, cost, warmup, confirmation, revision, and audit checks at discovery and immediately before mutation. Cover aliases, bundles, profiles, sudo, server context, panel actions, scheduled work, temporary grants, and nested command text. | `P001-TASK-002`; command, permission, source, target, and bypass inventories | Authority trace set, abuse dispositions, findings, and focused regression designs. | `src/main/java/com/enviouse/sef/kernel`; `kernel/command`; `kernel/policy`; `permissions`; `automation`; `sudo`; command registrars; `KernelServices`. | Absent, deny, grant, refresh, revoke, reconnect, stale confirmation, actor and target substitution, recursive and indirect routes, provider failure, and one use grant races fail before unauthorized mutation. |
| `P001-TASK-004` | `SEFAUD-REQ-002` | Audit all enhanced protocol and GUI trust boundaries. Verify bounded decode before allocation or native image work, negotiated feature and protocol state, session identity, monotonic sequence, replay rejection, request rate, panel and control ownership, server projected targets, current revisions, permission recheck, correct thread scheduling, and presentation only client behavior. | `P001-TASK-002`; Phase 000 payload and GUI inventory | Payload and GUI boundary ledger, findings, codec and handler regressions, and runtime abuse workflow. | `src/main/java/com/enviouse/sef/gui/protocol/SefPayloads.java`, `PayloadCodecSupport.java`, `GuiWorkflowPayloads.java`, `SefSessionManager.java`, `SessionRequestGuard.java`, `PanelActionValidator.java`, `SefGuiServer.java`; related tests. | Forged identity, wrong nonce or session, replay, out of order sequence, stale panel, target, config, or policy revision, unknown identifiers, excessive fields and chunks, disconnect, permission loss, and protocol mismatch cause no mutation or private projection. |
| `P001-TASK-005` | `SEFAUD-REQ-002` | Audit configuration, migration, recovery, durable authority, and security-sensitive writer selection as untrusted inputs. Verify type and size bounds, graph validation, secret filtering, optimistic revision, safe watcher behavior, transactional publication, source fingerprinting, exact backup, rollback, unsupported version behavior, recovery mode, provider selection, and time-of-check resistance. Limit nonsecurity integrity depth to Phase 002 while retaining all privilege, disclosure, path, opened-object, and evidence-preservation defects here. | `P001-TASK-002`; Phase 000 configuration, durable owner, filesystem, and native-writer inventories | Security dispositions, repair requirements, regression fixtures, and Phase 002 handoff for nonsecurity integrity rows. | `src/main/java/com/enviouse/sef/config/modules/ModuleConfigService.java`, `ModuleConfigRegistry`; `ConfigurationEventHandler`; `src/main/java/com/enviouse/sef/storage`; audit writer providers; domain repositories and recovery owners identified by Phase 000. | Malformed, oversized, stale, linked, swapped, unsupported newer, corrupt, conflicting backup, failed publication, reload race, wrong provider, missing identity metadata, and recovery input never grants authority, exposes secrets, mutates an unvalidated object, or destroys required evidence. |
| `P001-TASK-006` | `SEFAUD-REQ-002`, `DEC-009`, `EXT-001` | Audit every owned filesystem root, content parser, and platform-native audit writer. Verify containment, normalized names, no traversal, symbolic-link rejection, detectable hard-link handling, Windows reparse controls, safe directory creation, bounded reads and writes, archive and image limits, safe exports, and no host path disclosure. On canonical Linux, verify the actual POSIX ABI, open flags, structure layouts, descriptor identity, type, link count, bounded append, flush, rotation, and post-flush identity. Repeat the applicable POSIX contract on macOS or native ABI, handle, volume and file identity, type, reparse, link-count, append, flush, rotation, and post-flush identity contract on Windows only when that non-client host-specific path changed or was exercised; otherwise close the host row as `NOT_EXERCISED` from frozen reachability evidence. All providers fail closed when metadata or native calls are unavailable. | `P001-TASK-002`; Phase 000 filesystem, archive, image, import, export, recovery, operating-system, and native-provider inventories; `EXT-001` | Filesystem, content, and native-writer boundary dispositions, stable `P001-FIND-###` rows, canonical Linux ABI manifest, conditional host manifests or `NOT_EXERCISED` reachability records, regression fixtures, and recovery proof. | `NativeAuditFileProvider` and all provider implementations; `AtomicFileStore.java`, `StorageExportService.java`; `FancyTagObjectStore.java`, `FancyTagProjectArchive.java`; `FileLogSink.java`; configuration and every other inventoried owned root. | Traversal, absolute paths, symlinks, detectable hard links, reparse points, special or duplicate archive entries, excessive depth, count, ratio, or bytes, malformed images, object and parent swaps, metadata unavailable, wrong type, identity change, partial write, disk failure, flush failure, rotation, shutdown, restart, and export collision fail safely. Linux evidence proves the canonical native contract, with direct macOS or Windows evidence only for a changed or exercised non-client native provider path. |
| `P001-TASK-007` | `SEFAUD-REQ-002`, `SEFAUD-REQ-003`, `DEC-009`, `EXT-001`, `EXT-002` | Audit optional integrations, platform-native API loading, reflection, mixins, and access transformers. Inventory reflective and native class, symbol, structure, and member access; runtime guards and suppliers; failure containment; thread and side ownership; provider cache and refresh; every Mixin target and injection point; every access-widened member; and packaged scope. Prove missing classes or symbols, ABI mismatch, absent provider, outage, malformed response, incompatible state, failed injection, and wrong logical side cannot broaden authority, select an unsafe writer, or hide partial security enforcement. | `P001-TASK-002`; Phase 000 integration, native-provider, platform-dependency, reflection, mixin, and access-transformer inventories | Integration and native boundary dispositions, compatibility findings, tests, and scope justification for every privileged hook and native call. | Native audit provider and JNA API boundary; `src/main/java/com/enviouse/sef/utils/moddeps`; `teleport/compat/FTBTeleportImportService.java`; permission providers; Curios adapters; `sef.mixins.json`; `META-INF/accesstransformer.cfg`; mixin classes under `com.enviouse.sef.vanish.mixin`. | Present, absent, startup failure, runtime outage, removal, stale cache, malformed values, linkage or symbol failure, ABI mismatch, unsupported identity metadata, dedicated server startup, client startup, and injection application preserve least privilege and core availability. Writer uncertainty fails the write closed. |
| `P001-TASK-008` | `SEFAUD-REQ-002` | Trace every sensitive class through command feedback, suggestions, chat, GUI, HUD, audit, command spy, optional file logging, console and exceptions, persistence, exports, generated references, fixtures, and the JAR. Verify redaction before filtering and storage, viewer specific projection, immutable actor attribution, bounded correlation, mandatory audit independence, retention, and safe diagnostic detail. | `P001-TASK-002`; Phase 000 data classes, outputs, logs, exports, and evidence rules | Privacy flow matrix, secret canary results, audit integrity findings, redaction regressions, and sanitized evidence custody record. | `src/main/java/com/enviouse/sef/audit`; `commandlog`; `alts`; `moderation/ConnectionAddressService.java`; social observation and messaging; GUI projections; storage exports; generated references. | Synthetic secret markers, private bodies, hidden identities, raw and linkable addresses, moderation reasons, restricted arguments, host paths, provider metadata, exception text, and evidence identifiers appear only at explicitly authorized sinks. |
| `P001-TASK-009` | `SEFAUD-REQ-003`, `DEC-009`, `EXT-002` | Capture deterministic resolved graphs for buildscript and plugins plus every resolvable main, fallback, test, tooling, packaging, and installed NeoForge runtime configuration on canonical Linux and any affected or exercised macOS or Windows non-client host-specific path. Reconcile all alerts current at execution, including the authoring twelve high, thirteen medium, and one low. For every alert inspect advisory ranges, candidate paths, packaged-mod presence, installed-runtime presence, affected APIs, source and runtime reachability, optional state, licenses, authoritative provenance, checksums, and compatible remediation. For JNA and JNA Platform, prove compile-only declarations, exact NeoForge supplier, binary-compatible APIs, native-library ownership, and absence of a duplicate embedded implementation. | `P001-TASK-001`; Phase 000 dependency, platform, and artifact baseline; refreshed read-only remote snapshot; `EXT-002` | `P001-DEP-###` disposition table with seven separate platform-owned gates, candidate and installed-runtime graph reports, reachability notes, license and provenance ledger, JNA ownership record, and proposed compatible remedy set. | `build.gradle`, `settings.gradle`, `gradle.properties`, checked-in wrapper, generated mod metadata, NeoForge artifacts and metadata, repositories, source sets, optional integrations, test libraries, build plugins, installed runtimes, and candidate JAR inputs. | Repeated resolution at the same commit is semantically identical. Every alert maps to exact candidate and platform evidence. Critical and high inapplicability includes dependency path, configuration, packaged-mod presence, installed-runtime presence, affected API, reachability, advisory basis, provenance, and attempted compatible remediation. Missing authoritative evidence is blocked. |
| `P001-TASK-010` | `SEFAUD-REQ-002`, `SEFAUD-REQ-003`, `DEC-001`, `DEC-004`, `DEC-009` | Implement the smallest compatible repair for every confirmed finding. Order work by critical, high, dependency and native-provider prerequisites, medium, then low. Pair each repair with a focused regression before proceeding. Reconcile overlapping policy, writer, and dependency changes centrally. Native-writer repairs preserve same-opened-object validation and mutation, fail closed on unavailable identity metadata, and do not add path-only fallback. Dependency repairs keep JNA compile only, avoid duplicate runtime embedding, and preserve canonical Linux and any affected macOS or Windows non-client host-specific paths. Add versioned migration, exact recovery copy, rollback, or compatibility documentation when an external contract changes. | Findings and remedy set from Tasks 003 through 009 | Repaired source and declarations, focused tests, per-operating-system provider evidence design, migration and rollback material when applicable, and updated invalidation map. | Only components proven affected by a finding. Platform pins, mandatory operating-system scope, runtime ownership, and future scope remain unchanged. | Each finding fails before the repair or has equivalent decisive evidence, passes after repair, and passes adjacent denial, recovery, cross-platform compatibility, duplicate-runtime, and no-regression cases. All configured and installed-runtime dependencies resolve cleanly. |
| `P001-TASK-011` | `SEFAUD-REQ-002`, `SEFAUD-REQ-003`, `DEC-009`, `EXT-001`, `EXT-002` | Execute the consolidated adversarial, concurrency, interruption, native-writer, privacy, performance, and integration regression matrix against the repaired revision. Run Minecraft Java client behavior through the portable Minecraft Java client fixture and shared server or mod behavior on canonical Linux, without an operating-system client evidence split. Run direct macOS or Windows evidence only for changed or exercised non-client provider, filesystem, server-lifecycle, process-control, dependency-loading, recovery, or packaging paths. Verify authority, redaction, opened-object identity, link or reparse rejection, object-swap resistance, write and flush failure preservation, reload, reconnect, shutdown, restart, and resource bounds. | `P001-TASK-010`; focused regressions; required GameTests; applicable `EXT-001` environment and `EXT-002` inputs | Final adversarial ledger, Minecraft client fixture results, canonical Linux shared-runtime results, affected non-client host traces, privacy and performance results, and downstream invalidation map | Command, payload, GUI, persistence, audit, native provider, dependency, lifecycle, and integration owners from Phase 000 | Every applicable row passes at the required fidelity. Client rows use the Minecraft Java client fixture and shared portable rows use canonical Linux. A changed or exercised non-client host path requires direct evidence on that host. Any confirmed vulnerability or unsafe writer behavior reopens its owner and blocks phase exit. |
| `P001-TASK-012` | `SEFAUD-REQ-002`, `SEFAUD-REQ-003`, `DEC-009`, `EXT-001`, `EXT-002` | From the repaired commit, renew candidate and installed NeoForge runtime graphs and remote snapshots; run focused tests, required GameTests, build, fallback compilation, and dedicated-server and Minecraft Java client fixture workflows. Run native-writer, filesystem, server-lifecycle, dependency-loading, and optional-integration workflows on macOS or Windows only when those non-client paths are changed or exercised. Run code and secret scanning. Inspect repository, mod JAR, installed runtime artifacts, dependency metadata, embedded classes and native libraries, duplicates, resources, reflection surfaces, mixins, access transformer, services, licenses, authoritative provenance, advisories, secrets, private paths, logs, caches, debug files, and source commit binding. Prove JNA and JNA Platform remain compile only, are supplied compatibly by pinned NeoForge, load natively on each exercised provider path, and are not duplicated in the mod. Produce SHA-256 and SHA-512 for candidate and relevant platform artifacts. | `P001-TASK-011`; clean repaired worktree; applicable `EXT-001` runtime evidence; authoritative `EXT-002` inputs | Final security and supply chain verification packet, artifact manifest, platform-runtime provenance ledger, seven-gate platform-alert dispositions, JNA ownership proof, and `EXT-002` ownership and applicability packet. | Gradle build, all changed code and resources, candidate JAR, generated mod metadata, resolved candidate and installed-runtime graphs, NeoForge artifacts, scanners, and Linux, macOS, and Windows fixtures where applicable. | Every required check passes at the repaired commit. Missing provenance, unresolved direct mod-owned advisory, ABI or runtime incompatibility on an exercised path, duplicate runtime, or absent compatible remediation for a direct mod-owned component blocks the phase. An unavailable conditional host or upstream-only platform alert remains explicitly recorded and does not narrow support or block unrelated portable work. Any later source, dependency, resource, harness, runtime, or packaging change invalidates affected packets and returns to the responsible earlier task. |
| `P001-TASK-013` | `SEFAUD-REQ-002`, `SEFAUD-REQ-003`, `DEC-009` | Update current documentation and sanitized evidence from verified results only. Record behavior, configuration, migration, recovery, Linux, macOS, and Windows support, native-writer contract, dependency ownership, JNA compile-only supply, no-duplicate-runtime proof, advisory closure, known blockers, evidence commands, and invalidation changes. Keep exploit detail and sensitive evidence in the approved private route. Mark all Phase 002 through Phase 007 rows invalidated by the repair. A missing host or unresolved advisory is documented as blocked, never unsupported or waived. | `P001-TASK-012` | Reconciled documentation, `EXT-001` and `EXT-002` evidence index, link and drift results, and downstream handoff. | `docs/SECURITY_REVIEW.md`, `docs/SEF2_ACCEPTANCE.md`, `docs/COMPATIBILITY_MATRIX.md`, `docs/RELEASE_WORKFLOW.md`, `DOCUMENTATION.md`, `README.md`, `test.md`, `docs/MIGRATION_GUIDE.md`, and release notes only when affected. | Documentation matches the repaired source, installed runtime, and artifact, consistently names all three supported operating systems, links pass, generated references have no unexplained drift, and no secret, personal data, host path, unsafe exploit content, suppressed alert, or false completion claim is tracked. |
| `P001-TASK-014` | `SEFAUD-REQ-002`, `SEFAUD-REQ-003`, `DEC-009`, `EXT-001`, `EXT-002` | Audit the complete diff and completion packet, including the canonical Linux manifest, any affected host-specific macOS or Windows manifest, native ABI and opened-object rows, platform dependency gate, and documentation claim. Synchronize repository tracking without exposing sensitive details, preflight private independent-review capability, obtain one review when supported or record the optional unsupported disposition, resolve received findings, run invalidated checks, merge through GitHub, verify the integrated candidate branch, and create the signed annotated phase tag. | `P001-TASK-013`; repository phase workflow; applicable `EXT-001` and `EXT-002` evidence | Merged Phase 001 state, signed tag, closed phase tracking, and authorized Phase 002 entry packet. | Git diff, commits, issue and milestone state, pull request, required checks, review, merge commit, candidate branch, tag, external-prerequisite packets. | No unrelated or generated junk is present. Required checks and supported review are resolved. Canonical Linux and every exercised host-specific path pass, and platform-alert gates remain separately classified. Merge uses GitHub and the configured merge method. Integrated candidate hashes and evidence match. Any blocked applicable prerequisite prevents merge and transition. |

### Ordering, Failure Handling, and Safe Parallel Work

Task 001 is an absolute gate. Task 002 establishes stable rows and must complete before specialist review. Tasks 003 through 009 may proceed in bounded parallel work only against the same frozen revision and only when they write separate evidence rows. A reviewer who finds a missing Phase 000 surface stops that row and routes it back to Phase 000 rather than silently extending the baseline.

Task 010 is the only remediation convergence point. Shared files such as `KernelServices`, permission services, protocol handlers, storage primitives, `build.gradle`, and generated metadata must have one change owner at a time. Repairs are atomic by finding where practical. If a repair fails verification, restore the last verified phase branch state through a normal corrective commit or remove only the isolated unmerged change. Never use destructive history rewriting or discard unrelated work.

Tasks 011 through 014 use the final repaired revision. A newly found defect returns to Task 010. A dependency or artifact drift returns to Task 009. A missing inventory row returns to Phase 000. A documentation discrepancy returns to Task 013. Any post verification product change invalidates the exact affected evidence and requires the mapped rerun before integration.

## Architecture and Implementation Boundaries

### Authority and execution

The logical server remains authoritative for identity, source classification, permissions, hierarchy, exemption, targets, policy, revisions, confirmation, execution, persistence, and audit. `CommandExecutionService` and the canonical command policy pipeline remain the required gate for executable catalog actions. Aliases, shortcuts, bundles, command profiles, sudo, panels, GUI actions, scheduled work, and approved integrations must resolve to a canonical action and cannot create an alternate permission or source policy.

Permission and feature decisions made during discovery, preview, suggestion, or GUI projection are not mutation authority. Current feature, permission, provider, hierarchy, exemption, source, target, command tree, configuration, session, panel, and record revisions must be checked immediately before a privileged effect. Temporary authority is immutable, narrow, expiring, single use where declared, cleaned in all terminal paths, and incapable of creating a persistent grant or delegating itself.

### Networking and GUI

Client payloads and client caches are untrusted presentation input. Codecs enforce byte, string, list, page, component, image, chunk, and aggregate bounds before large allocation or native decode. Handlers bind player identity to the connection, negotiate supported features, validate session and monotonic sequence, reject replay and stale state, select only server projected identifiers, and schedule server mutation on the logical server thread. An enhanced, absent, fallback, or incompatible client never changes server authorization semantics.

### Configuration, persistence security, and filesystem ownership

Configuration, migration, recovery, import, archive, image, and persistent documents are untrusted even when located under an owned directory. Normalization alone is insufficient. Each boundary validates root containment, every existing parent, entry type, symbolic links, detectable hard links where the contract requires them, file type, size, encoding, structure, schema, semantic bounds, and revision. Publication preserves the last known good state and recovery evidence.

Phase 001 owns privilege, disclosure, path escape, unsafe parser, and evidence destruction risks at persistence boundaries. Phase 002 owns exhaustive durability and cross store integrity. A Phase 001 repair that changes schema or configuration must still supply a versioned migration, source identity and revision binding, complete staged validation, exact recovery copy, rollback, and forward and backward fixture evidence before integration.

Archives must reject traversal, absolute names, duplicate normalized names, links and special entries, excessive nesting, excessive entries, excessive compressed or expanded bytes, and unsupported content before extraction or publication. Images must be structurally bounded and validated before native decode, with encoded bytes, dimensions, pixel count, frames, metadata, and canonical output constrained. Exports use opaque identifiers or safe fixed names and never disclose host filesystem paths.

### Platform-native audit writer

Every security-sensitive audit append is one opened-object transaction. The provider opens the candidate with operating-system controls that prevent following an unsafe target, obtains type, identity, volume or device, link count, and reparse metadata from that opened descriptor or handle, rejects anything outside the regular owned-object contract, appends only through that descriptor or handle, flushes it, then confirms stable opened-object identity before reporting success. The provider never validates a path and later reopens it for mutation. Failure to load a native symbol, interpret the exact ABI, obtain required identity metadata, reject an unsafe link or reparse state, complete the full append, flush, or confirm stability fails closed and preserves the prior valid audit state.

Linux and macOS may share implementation structure, but canonical Linux receives direct proof for its exact Java 21 architecture, native library symbols, integer widths, structure field order and alignment, open flags, descriptor lifetime, file type, device and inode identity, link count, append behavior, flush primitive, error translation, and rotation behavior. The equivalent macOS POSIX proof is required only if the macOS non-client path changed or was exercised. Windows proof covers native library symbols and calling convention, handle creation flags and sharing, handle lifetime, volume serial and file identity, file type, reparse attributes and tags, link count, append semantics, flush primitive, error translation, rotation behavior, and object stability, and is required only if that non-client path changed or was exercised. Otherwise the host row is `NOT_EXERCISED` with frozen reachability evidence. A unit test that mocks the operating-system name or metadata is useful regression evidence but cannot replace direct `EXT-001` runtime proof for an executed host path.

### Audit, logging, privacy, and evidence

Mandatory security audit remains independent from optional observation and file logging filters. Every privileged attempt has stable action metadata, real and effective actor attribution, source class, result, correlation, and bounded diagnostic context. Audit integrity does not authorize recording sensitive arguments. Redaction happens before filtering, persistence, export, or broad projection. Unknown or malformed roots receive the safer redaction class.

Evidence uses synthetic identities and unique synthetic canary values. Tracked summaries contain hashes, stable IDs, commands, expected and actual classes, and sanitized excerpts only. Raw captures remain in the Phase 000 approved restricted location with bounded retention. If a real credential or personal value is unexpectedly detected, stop inspecting or copying it, quarantine the affected evidence, report only the type and location privately, and let the credential owner perform any required rotation.

### Integrations, reflection, mixins, and access transformers

Optional integrations remain behind runtime presence checks and bounded adapters. Reflection is contained to named optional compatibility surfaces, validates types and return values, catches linkage and reflection failures at the adapter boundary, and cannot grant on failure. Provider caches have explicit revision, refresh, expiry, and outage behavior. Core startup and command fallback remain available when optional integrations are absent.

Every Mixin and access transformer is privileged compatibility surface. The review records target, purpose, logical side, injection or access scope, failure behavior, supported platform, composition risk, test, and packaged presence. Client mixins remain under the client list. Common and dedicated server paths cannot load client classes. A security repair may narrow a hook but cannot broaden platform support or introduce a new Mixin when a current supported API or event can safely express the repair.

### Dependency, license, and provenance

Resolved candidate and installed NeoForge runtime graphs, not declared coordinates alone, define dependency state. The graph capture covers plugin and buildscript dependencies, main compile and runtime classpaths, fallback runtime classpaths, unit and GameTest classpaths, annotation or tooling configurations if present, generated metadata inputs, the actual packaged mod JAR, and the installed runtime artifacts on canonical Linux and any affected or exercised macOS or Windows non-client host-specific path. Configuration names are taken from the live Gradle model rather than assumed. For each component, record coordinate, version, dependency path, repository or authoritative origin, SHA-256 and SHA-512, license evidence, optional or platform-owned status, configurations, packaged-mod presence, installed-runtime presence, affected API, source and runtime reachability, advisory applicability, and compatible remediation.

A dependency remedy prefers the smallest compatible direct version, constraint, exclusion, or upstream selection that removes the applicable vulnerable path without violating `DEC-004`. Optional dependencies remain optional and do not become embedded by accident. A version override is not accepted until clean resolution, compilation, unit tests, GameTests, dedicated server, affected client, optional integration present and absent, and artifact inspection pass.

JNA and JNA Platform remain compile-only declarations. `EXT-002` identifies the exact pinned NeoForge 21.1.235 artifacts that supply their runtime classes and native libraries, while `EXT-001` proves API linkage, binary compatibility, native library loading, and behavior on canonical Linux and each affected host-specific path. The mod JAR must contain no `com.sun.jna` implementation classes, bundled JNA native library, relocated duplicate, or service entry that shadows the platform runtime. A platform-owned alert cannot close until its candidate graph, packaged mod, installed runtime, affected-API reachability, authoritative advisory applicability, provenance, and compatible-remediation gates are all independently passed.

### Concurrency and performance

Security checks and state consumption must be atomic at their owning boundary or protected by current revision and conditional mutation. Race tests cover permission revocation, provider refresh, session close, upload ownership, archive settle windows, config file replacement, migration, export preview and confirmation, log retention, temporary grant use, and timeout cleanup. Exactly one authorized terminal outcome is permitted where operations race.

No repair may add unbounded scans, allocations, decompression, payload buffering, per tick filesystem access, blocking network work, reflection, or log spam to tick, packet, render, command, audit, or persistence hot paths. Bounds are tested at just below, exact, and just above limits. Repeated invalid input is rate limited or bounded and produces summarized audit evidence rather than one unbounded record per attempt.

## Diagnostics and Debugging

**Requirement IDs:** `SEFAUD-REQ-002`, `SEFAUD-REQ-003`
**Task IDs:** `P001-TASK-003`, `P001-TASK-004`, `P001-TASK-005`, `P001-TASK-006`, `P001-TASK-007`, `P001-TASK-008`, `P001-TASK-009`, `P001-TASK-010`, `P001-TASK-011`, `P001-TASK-012`, `P001-TASK-013`
**Controls:** Inherit the Phase 000 server-authoritative controls `/sef debug on <category> [target] [seconds] [sample_limit]`, `/sef debug status`, and `/sef debug off`; the owned dedicated-server console uses the same commands without the leading slash. Only the console or a source with `sef.commands.sef.debug` may operate them. Phase 001 uses the bounded categories `authority`, `protocol`, `filesystem`, `native_writer`, `privacy`, `dependency`, and `integration`; an unknown category, ambiguous or unauthorized target, missing target, or remote client request fails without enabling capture. `off` is idempotent, and timeout, target removal, disconnect, reload, server stop, and restart clear applicable capture state. If the controls or permission are absent at entry, Phase 000 is incomplete and Phase 001 runtime capture does not proceed.
**Signals:** Emit diagnostics contract version 2 structured records with `capture_id`, `schema_version`, `sequence`, `correlation_id`, candidate commit and artifact hashes, logical side, category, component, canonical action or operation, pseudonymous actor and effective actor, expected and observed revisions, decision, stable reason code, duration in milliseconds, payload bytes, queue depth, dropped and truncated counters, protocol state, storage state, writer state, dependency coordinate and owner when relevant, and sanitized failure class. The signal table below defines sources, units, and falsifiable observations.
**Collection procedure:** Follow the numbered Phase 001 collection runbook below. It binds one frozen candidate and disposable fixture, selects the least costly real path, enables one category and explicit target for a bounded window, performs one declared normal or adversarial stimulus set, joins events by `capture_id` and `correlation_id`, disables capture, retains only the sanitized support packet, and completes registered cleanup on every used host.
**Headless verification:** On `node-1` or the Linux laptop, first inspect the requested Gradle task graph and prove it starts no client, renderer, or display. Use focused JUnit tests, `./gradlew test`, `./gradlew runGameTestServer`, `./gradlew runAuditWriterProbe`, `./gradlew build compileFallbackRuntimeJava`, and `./gradlew generateAuditDependencyManifest --no-configuration-cache -PsefAuditCandidateCommit=<full-commit>` as their mapped rows require. Server-only permission, payload rejection, opened-object writer, dependency loading, redaction, audit, recovery, and shutdown checks use synthetic actors, controlled console input, bounded polling of logs or reports, and authoritative before-and-after assertions. These paths prove only server, filesystem, dependency, and non-rendering behavior and cannot close client receipt, negotiation, reconnect, client-only classloading, or presentation claims.
**Client verification:** The residual client claims are matching enhanced-client negotiation, fallback and incompatible-protocol behavior, client receipt of authorized projections, absence of private projection after denial or revocation, reconnect and session clearing, and client-only Mixin and classloading compatibility. Run these once through the portable Minecraft Java client fixture on the owner Linux laptop against the same canonical Linux candidate server and artifact. Use client and server logs for protocol state, connection, and correlation; use a targeted visual only when presentation itself is the assertion. No macOS or Windows Minecraft client is launched or required. A missing laptop client capability leaves only these client rows unverified while independent headless work continues.
**Client audio isolation:** Before launch, use a disposable client directory and set the pinned 1.21.1 master audio option to zero without touching a personal instance. After launch, bind the exact owned Hyprland window address, class, title, and PID through `hyprctl clients -j`; verify that process belongs to the candidate and uses the discrete RTX renderer; correlate only that PID and verified descendants to its PipeWire or PulseAudio application stream; apply `wpctl set-mute <discovered-node-id> 1` or `pactl set-sink-input-mute <discovered-input-id> 1`; and verify the application stream reports muted before acceptance actions. Keep a bounded owned watcher and repeat correlation and mute verification after resource reload, device change, reconnect, restart, or every stream recreation. If identity, renderer, or mute cannot be proved, stop that owned client and leave its rows unverified. Teardown stops the client and watcher, verifies their processes and streams are gone, and removes only their disposable audio, routing, and instance state. Never mute a default sink, microphone, system-wide output, unrelated application, or user-owned client.
**Budgets and privacy:** Diagnostics are off by default. Default capture duration is 60 seconds and maximum duration is 300 seconds; default sample limit is 1,000 and maximum is 10,000 events; maximum output is 4 MiB; maximum accepted rate is 100 events per second per capture. Disabled mode performs no expensive formatting, traversal, packet transmission, or filesystem work and has no material hot-path allocation. Enabled mode constructs records lazily, bounds per-tick work and buffers, increments truthful dropped and truncated counters, and stops or safely truncates at a limit without blocking the tick, render, packet, or writer thread. Redact credentials, tokens, private messages, raw or linkable addresses, hidden identities, restricted arguments, artwork bytes, native path details, private absolute paths, and unrelated data before any sink. Diagnostics never grant authority, mutate policy or gameplay, persist enabled state, or automatically upload evidence.
**Regression and support:** Test enable, status, disable, idempotent disable, console and permission authorization, unknown category, absent or removed target, timeout, reload, disconnect, stop, restart, sequence and correlation, schema fields, rate and output limits, dropped and truncated counters, sanitizer behavior, disabled-mode overhead, bounded enabled overhead, writer failure, and unchanged gameplay and durable results. Every repaired finding adds its expected normal and failure events plus an assertion that diagnostics do not alter the outcome. `P001-TASK-013` updates the `Security diagnostic capture` procedure in `DOCUMENTATION.md` and links it from `README.md`; the support packet contains version and loader, candidate hashes, minimal sanitized configuration, exact reproduction, expected and actual result, capture window and identifiers, bounded server and relevant client excerpts, test summary, disposition, and cleanup result.

### Phase 001 Diagnostic Signals

| Signal or record | Source and unit | Expected observation | Failure indicator |
|---|---|---|---|
| Capture identity and bounds | Shared diagnostic service; opaque ids, schema integer, monotonic sequence, seconds, event count, bytes | One unique `capture_id`, schema version 2, stable `correlation_id` across an operation, current candidate hashes, and truthful remaining duration, sample, rate, and byte budgets | Missing identity, reused id after reset, nonmonotonic sequence, stale candidate, silent truncation, or capture beyond a declared limit |
| Authority decision | Command policy, permission service, delegation, sudo, alias, bundle, profile, scheduler, or panel owner; stable ids and revisions | Pseudonymous real and effective actor, source class, action id, expected and observed revisions, `allowed` or `rejected`, and a stable reason before mutation | Missing recheck, source or target confusion, grant on provider failure, stale state accepted, actor attribution lost, or a denial followed by mutation |
| Protocol decision | Session, codec, request guard, panel validator, and server handler; direction, sequence, payload bytes, protocol state | Bounded payload, authenticated sender context, current session and revision, ordered acknowledgment or explicit rejection, and no private response on denial | Allocation before bound check, replay accepted, stale or cross-player id accepted, wrong-thread mutation, private projection, or uncorrelated client and server state |
| Opened-object writer transition | `SecurityAuditService` and `NativeAuditFileProvider`; provider id, operation id, object-identity digest, type, link or reparse state, bytes, flush boolean, duration | Canonical Linux reports one descriptor-backed regular object from validation through append and flush with the same before-and-after identity; an executed macOS or Windows path reports its matching native contract | Reopen by path, wrong type, unsafe link or reparse state, missing identity treated as success, changed object, partial append, failed flush reported as success, or leaked host path |
| Dependency and runtime ownership result | Gradle evidence task and runtime loader; coordinate, version, owner, dependency path, hash, reachability and advisory enums | JNA coordinates remain compile only, runtime ownership resolves through pinned NeoForge, the installed runtime supplies compatible APIs, the candidate JAR reports no duplicate native runtime, and every alert has separate gate states | Unknown owner represented as closure, graph or hash drift, affected API reachable without remedy, duplicate class or native library, or platform ownership used to suppress an alert |
| Privacy and custody result | Central sanitizer, audit, logging, export, exception, and evidence writers; data-class and sink ids, counts | Synthetic canaries occur only in authorized sinks; broad records contain redacted or pseudonymous values and bounded failure classes | Credential, private message, raw address, hidden identity, restricted argument, artwork bytes, private path, or raw exception content reaches a broad or tracked sink |
| Resource and cleanup result | Diagnostic service, queues, writers, process registry, and cleanup registry; milliseconds, events per second, bytes, depth, process and path counts | Bounded overhead, explicit drops or truncation, zero later events after disable, all owned processes exited, and all registered disposable paths removed after evidence retention | Tick or render blocking, unbounded allocation or queue growth, events after reset, missing process identity, retained runtime, or deletion outside registered ownership |

### Diagnostic Collection Runbook

1. Select the exact `SEFAUD-REQ-002` or `SEFAUD-REQ-003` row and owning `P001-TASK-###`. Record the full candidate commit, candidate JAR SHA-256 and SHA-512, Java 21, Minecraft 1.21.1, NeoForge 21.1.235, relevant configuration, expected result, host, logical side, runtime directory, evidence destination, synthetic identities, and every test-owned path, process, stream, watcher, and service before creating or starting it. Use a fresh unique disposable root. If a path or laptop instance is not yet known, discover it and verify identity before launch rather than inventing it.
2. Choose the lowest evidence rung that proves the claim. Use a focused unit or static check for isolated logic, a verified client-free GameTest or protocol harness for server paths, a no-GUI dedicated server for loader, lifecycle, dependency, console, filesystem, and writer behavior, and the Linux laptop client only for the residual client claims named above. Before any server launch, create or update only that disposable runtime's `eula.txt`, remove contradictory assignments, read back one effective `eula=true`, and keep the file outside tracked source.
3. Confirm the server reaches its candidate-specific ready state and the authorized console is controllable. Run `sef debug on <category> [target] [seconds] [sample_limit]` at the server console, or `/sef debug on ...` through an authorized player only when that source is itself part of the test. Record the returned `capture_id`, output location, expiry, sample and byte budgets, and pre-stimulus state. If the command, target, or output cannot be verified, stop the capture or server and leave the row unverified.
4. Execute exactly the declared normal, denial, stale, forged, replay, oversized, concurrent, interrupted, provider-failure, writer-failure, redaction, or dependency stimulus through its real entry point. Do not grant broader permission, force a state mutation, use console to bypass the path under test, or substitute mocked host names for native behavior. Record authoritative state before and after and use bounded waits tied to a named event or deadline.
5. Run `sef debug status`, then join the bounded diagnostic records, audit events, server log, client log when applicable, filesystem identities, dependency manifest, and state assertions by `capture_id`, `correlation_id`, operation id, candidate commit, and artifact hashes. Verify every required field, reason code, bound, expected and actual decision, dropped or truncated counter, redaction rule, and absence of unauthorized mutation. A screenshot or server log cannot substitute for an opened-object, dependency, or client-only assertion.
6. Run `sef debug off` twice and verify idempotence, disabled status, zero later matching events, and reset on the applicable timeout, reload, disconnect, stop, and restart cases. Retain the minimal sanitized support packet at the Phase 000 approved evidence destination. Do not upload automatically, retain raw secrets, or copy an entire runtime, world, home directory, or unbounded log.
7. On every exit path, including failure, timeout, cancellation, or launch failure, gracefully stop only the exact owned processes and wait for exit; stop each owned audio watcher and route; verify client streams disappeared; preserve only the required sanitized evidence; re-resolve every registered disposable target without following symlinks; remove only those test-created files, worlds, logs, crash reports, configurations, dependency copies, build outputs, and runtimes; and verify absence on every used host. Record test result and cleanup result separately. Any remaining owned process or disposable path keeps the workflow cleanup-incomplete and must be reconciled before another run.

## Runtime Host, Client, and Cleanup Contract

| Workload | Host and runtime | Required identity and result | Why a client is or is not needed |
|---|---|---|---|
| Source review, graph capture, unit tests, artifact scans, and verified server-only GameTests | `node-1` or the Linux laptop in a fresh disposable checkout or worktree using the checked-in wrapper and Java 21 | Record checkout path, commit, task graph, candidate hashes, Gradle and Java versions, command, report path, result, and cleanup. On `node-1`, prove no task dependency starts a client, renderer, or display. | No client is needed because assertions inspect code, dependency graphs, packaged bytes, server-authoritative decisions, or deterministic server state. These checks do not prove client behavior. |
| Canonical shared server, native writer, reload, shutdown, restart, and representative security workflow | Owner Linux laptop in a discovered isolated server runtime bound to the same candidate artifact | Record active host, filesystem, runtime path, loopback or existing authorized private endpoint, `eula=true` readback, ready event, console ownership, writer provider, installed runtime hashes, exact stimulus, expected and actual result, and cleanup | A client is not needed for pure console, writer, dependency-loading, filesystem, or server-state rows. Use the server console and bounded diagnostics without requiring EnVy to join. |
| Matching enhanced, no-SEF fallback, incompatible protocol, reconnect, and private-projection security workflows | Canonical server and all required disposable 1.21.1 client instances on the owner Linux laptop, using the repository's discovered `runClient` or `runFallbackClient` game-directory and quick-play properties only after their live definitions are verified | Match commit, JAR hashes, NeoForge, configuration, endpoint, client logs, server join identity, world-entry state, renderer, exact owned window and stream mute, correlation ids, disconnect and reconnect state, result, and cleanup | A real client is required only for negotiation, actual client receipt, reconnect, client-only classloading, and presentation boundaries. This one Linux fixture proves the portable Minecraft Java client contract. There is no macOS or Windows client evidence. |
| Changed or exercised macOS or Windows non-client filesystem, native-writer, server-lifecycle, process-control, dependency-loading, recovery, or packaging path | Matching disposable host runtime for the exact reachable path | Record reachability trigger, host and filesystem manifest, commit and artifact hashes, installed runtime, native ABI where relevant, command, result, evidence, and cleanup | No Minecraft client is used. If the frozen source and artifact prove the path was not changed or exercised, close it as `NOT_EXERCISED`; otherwise missing direct host evidence keeps only that conditional row open. |

For a required client run, first discover and verify the laptop checkout, launcher properties, isolated server and client directories, and candidate hashes. Use an isolated loopback or already authorized private endpoint without public exposure, firewall changes, credential copying, or authentication weakening. Configure and read back `eula=true`, start the no-GUI canonical laptop server, confirm the exact candidate ready event, then launch each disposable client with its prelaunch master output at zero. Apply the Hyprland window and PID binding, discrete-renderer check, per-application stream mute, and watcher procedure before connection assertions. Verify the intended synthetic player in both server evidence and client world state. Use the console only for deterministic setup that does not bypass the behavior or permission under test. A title screen or server-list entry is not a join. Recheck mute after reconnect or stream recreation, then stop and clean the exact clients, watchers, streams, server, runtimes, and generated state.

Singleplayer is not part of Phase 001. It may be added only for a later explicitly identified singleplayer or integrated-server defect with its requirement id and reproduction rationale; missing connectivity, desktop control, login, or launcher support is not an exception. No Minecraft client may run on `node-1`, through a virtual display, offscreen renderer, software renderer, X11 forwarding, VNC, or an integrated GPU fallback.

Every Phase 001 test and audit, including read-only scans and Gradle verification, registers cleanup before execution. Prefer a disposable checkout and task output root so build products, dependency manifests, native probe state, worlds, logs, crash reports, and temporary configuration can be removed after the last evidence consumer. Preserve source changes, tracked fixtures, required sanitized evidence, shared dependency caches, personal instances, user worlds, unrelated worktrees, and unrelated processes. Never use a blanket `git clean`, broad process kill, broad glob, repository-root deletion, workspace-root deletion, or symlink-following cleanup. A read-only audit that creates no file or process records that fact. A passed test with unverified teardown remains cleanup-incomplete.

## Security Scope Traceability

| Mandatory audit concern | Owning tasks | Required proof | Blocking condition |
|---|---|---|---|
| Sensitive-data leak and evidence custody | `P001-TASK-002`, `P001-TASK-008`, `P001-TASK-011`, `P001-TASK-012`, `P001-TASK-013` | Source-to-sink matrix, synthetic canaries, redaction and viewer projection tests, repository and JAR scans, sanitized custody ledger | Any unauthorized sink, unsafe raw capture, secret, personal data, or private host path remains. |
| Backdoor-like and indirect authority routes | `P001-TASK-002`, `P001-TASK-003`, `P001-TASK-010`, `P001-TASK-011` | Attack tree, canonical-action traces, actor and effective-actor attribution, preview-to-mutation revalidation, focused exploit-shape regression | Any alias, bundle, profile, sudo, GUI, scheduler, provider, lifecycle, or stored-text route bypasses current authority. |
| Permission, delegation, hierarchy, exemption, and revocation | `P001-TASK-003`, `P001-TASK-010`, `P001-TASK-011` | Absent, deny, grant, wildcard, inheritance, refresh, revoke, outage, race, replay, reconnect, and single-use evidence | Any stale, failed-provider, alternate-source, or concurrent path produces an unauthorized effect. |
| Path, opened-object identity, link, and reparse safety | `P001-TASK-005`, `P001-TASK-006`, `P001-TASK-010`, `P001-TASK-011`, `P001-TASK-012` | Root and parent checks, direct canonical Linux ABI proof, and direct macOS or Windows ABI proof when those non-client provider paths are changed or exercised, with same-opened-object identity, symlink, hard-link, reparse, swap, append, flush, rotation, and failure-preservation evidence | Any writer reopens by path, metadata is unavailable, object identity changes, unsafe link state passes, or any affected host row is missing or failed. |
| Archive, image, configuration, migration, and parser bounds | `P001-TASK-004`, `P001-TASK-005`, `P001-TASK-006`, `P001-TASK-010`, `P001-TASK-011` | Boundary and fuzz corpora, duplicate and special entries, traversal, depth, count, ratio, bytes, dimensions, pixels, frames, schema, revision, rollback, and recovery | Any input reaches unbounded allocation, native decode, publication, mutation, or evidence destruction before complete validation. |
| Reflection, optional providers, native API, mixins, access transformers, and logical side | `P001-TASK-007`, `P001-TASK-010`, `P001-TASK-011`, `P001-TASK-012` | Presence and absence, linkage and symbol failure, ABI mismatch, outage, malformed response, injection audit, dedicated-server classloading, client classification, packaged scope | Failure broadens authority, hides partial enforcement, loads client code on server, or selects an unsafe native writer. |
| Artifact, native-runtime ownership, and secret hygiene | `P001-TASK-009`, `P001-TASK-012`, `P001-TASK-014` | Source binding, JAR entries, duplicate classes and native libraries, mixins, access transformer, services, resources, licenses, hashes, secrets, paths, caches, and debug-file scans | Unexpected content, duplicate JNA runtime, missing provenance, stale hash, or unrelated change remains. |
| Candidate and platform-owned dependency risk | `P001-TASK-009`, `P001-TASK-010`, `P001-TASK-012`, `P001-TASK-014` | Stable `P001-DEP-###` rows and separate candidate graph, packaged mod, installed runtime, reachability, authoritative advisory, provenance, and compatible-remediation gates | Any critical or high applicable alert remains, any gate is unresolved, or remediation requires an unauthorized pin or scope change. |

## Failure, Recovery, and Edge Cases

| Scenario | Detection | Required behavior | Recovery or rollback | Regression proof |
|---|---|---|---|---|
| Phase 000 row is missing or stale | Source or runtime exposes a boundary absent from the frozen inventory, or hashes and counts disagree. | Stop the affected review and reopen Phase 000. Do not assign an ad hoc untracked row. | Correct and reintegrate the baseline, then restart Phase 001 from Task 001. | Inventory drift fixture and exact row count reconciliation. |
| Authorization is revoked after discovery or preview | Permission, provider, policy, hierarchy, target, feature, or revision differs immediately before mutation. | Reject with safe feedback, consume no cost or grant incorrectly, perform no privileged effect, and record bounded audit metadata. | Refresh current state and create a new request. Old confirmation or grant is invalid. | Revocation race across command, GUI, alias, bundle, profile, sudo, and scheduled routes. |
| Source or target confusion | Actor, effective actor, source type, target, or canonical action differs across indirection. | Fail closed and preserve true attribution. No alternate source gains a bypass. | Submit a new request through an allowed route. | Player, console, RCON, command block, function, scheduled, GUI, alias, bundle, sudo, and server profile substitution tests where declared. |
| Delegated grant races replay or cleanup | Two consumers, expiry, disconnect, provider refresh, or exception compete. | At most one valid execution occurs. The grant cannot survive or mint persistent authority. Cleanup runs on every terminal path. | Expire or revoke the grant and require a new reviewed preview. | Concurrent consume, expiry boundary, exception, reconnect, and replay test. |
| Malformed or oversized payload | Codec or handler detects an invalid field, aggregate size, identifier, feature, session, or sequence. | Reject before large allocation, native decode, state lookup that leaks identity, or mutation. Bound repeated abuse and audit output. | Negotiate a new session or use command fallback. | Boundary and fuzz corpus for every privileged payload family. |
| Stale or replayed GUI action | Session, sequence, panel, control, target, config, permission, or record revision differs. | Drop or reject without mutation or private response. | Reopen or refresh from server projected state. | Replay, out of order, stale revision, cross player identifier, disconnect, and protocol mismatch workflows. |
| Path traversal or link substitution | Normalized path escapes root, parent or target is a link or unexpected type, link count violates contract, or file changes during settle. | Reject before read, write, extraction, decode, export, backup, or recovery replacement. Preserve existing state and evidence. | Remove the unsafe synthetic fixture and retry with a validated regular path. | Traversal, absolute path, symbolic link, detectable hard link, parent swap, and race fixture. |
| Native writer identity or ABI evidence is unavailable | Provider cannot load the exact symbol, validate a structure layout, obtain required opened-object type or identity, read link or reparse state, or prove object stability on an executed host path. | Fail the audit write closed with bounded sanitized health information. Do not reopen by path, select a generic writer, call the operating system unsupported, or report audit success. Preserve prior valid state. | Repair the provider within the pinned runtime boundary, restore a compatible authoritative runtime artifact when allowed, and rerun the complete applicable host row. | Canonical Linux symbol, flags, layout, descriptor, device, inode, type, link-count, append, flush, rotation, and identity tests; equivalent macOS or Windows ABI and opened-object tests only when that non-client path changed or was exercised. |
| Opened object changes or write or flush fails | Before-and-after descriptor or handle identity differs, append is partial, flush returns failure, rotation targets an unsafe object, or injected storage failure interrupts the transaction. | Never report success. Preserve prior valid records and bounded recovery evidence. Do not continue through another path or object. | Resolve the disposable fixture, reopen only through the full validated transaction, and retry a new operation. | Object and parent swap, partial write, write denial, disk-full simulation, flush failure, rotation race, shutdown, and restart on canonical Linux and each affected or exercised macOS or Windows non-client host-specific path. |
| Archive or image bomb | Entry count, depth, compression ratio, expanded size, encoded bytes, dimensions, pixels, frames, or structure exceeds bounds. | Abort before unbounded allocation, extraction, or native decode. Publish nothing. | Delete the disposable candidate and retain only sanitized result metadata. | Just below, exact, just above, malformed, duplicate, truncated, and decompression fixtures. |
| Configuration or migration fails midway | Validation, source fingerprint, revision, backup, staged publication, or recovery marker fails. | Keep previous known good authority, block unsafe mutation, retain exact recovery material, and report sanitized cause. | Restore validated backup or correct input and rerun the whole transaction. | Source swap, conflicting backup, read only, malformed, unsupported newer, failure injection, forward migration, and rollback tests. |
| Optional provider is absent or fails | Presence guard, reflective linkage, timeout, malformed response, refresh, or runtime removal signals failure. | Preserve documented fallback or explicitly disable only provider owned capability. Never grant on error or stale cache. | Restore a compatible provider and refresh, or continue through the verified fallback. | Present, absent, outage, stale, malformed, refresh, removal, and restart matrix. |
| Mixin or access transformer no longer applies | Loader reports target or injection failure, member shape differs, or dedicated server loads client code. | Fail startup clearly for required incompatible hooks. Never continue with partially enforced security behavior. | Restore the verified pinned artifact or repair the narrow hook within the pin. | Dedicated server and client startup, injection audit, class loading inspection, and packaged resource comparison. |
| Audit or optional file sink fails | Queue, disk, rotation, retention, writer health, or export reports failure. | Mandatory audit health remains visible and bounded. Optional sink failure never reports success or disables security checks. Sensitive data remains redacted. | Repair the disposable sink, resolve incomplete markers, and retry only safe operations. | Queue full, write denied, disk full simulation, rotation, shutdown marker, recovery, and secret canary inspection. |
| Dependency graph cannot resolve reproducibly | Repository, metadata, checksum, constraint, exclusion, or cache state produces a different or failed graph. | Mark graph and affected alerts blocked. Do not build a release claim from stale cached output. | Restore repository availability or a verified compatible declaration and recapture all affected graphs. | Clean resolution twice at the same commit plus dependency insight for every changed or alerted component. |
| Host-specific environment is unavailable | `EXT-001` lacks a disposable Minecraft Java client fixture environment or a macOS or Windows environment for a changed or exercised non-client server, filesystem, native writer, shutdown, or restart path. | Continue Phase 001 work independent of the missing host. Mark only affected rows open and preserve the operating system as supported. | Obtain the missing disposable environment or repository-owned runner and execute the exact-artifact evidence contract when the non-client path is exercised. | Environment-manifest completeness and cross-platform result reconciliation that rejects mocked, stale, or single-host substitutions. |
| Alert is branch or configuration specific | Remote alert points at the default branch or a dependency absent from some candidate configurations. | Preserve the remote fact and record candidate applicability with exact graph, configuration, package, API, and reachability evidence. | Reevaluate after any branch, graph, packaging, or advisory change. | Candidate versus default branch graph comparison and JAR inspection. |
| Platform-owned dependency provenance or advisory is unresolved | Exact NeoForge supplier, installed artifact, hash, license or provenance, affected API, advisory applicability, or compatible remediation cannot be established. | Keep the `P001-DEP-###` row and `EXT-002` provenance record open. Do not dismiss it because it is transitive, loader supplied, compile only, or absent from the mod JAR. Block completion only when the mod directly declares, packages, or reaches an applicable vulnerable component. | Obtain authoritative metadata and advisory evidence or implement a compatible remedy within `DEC-004`, then rerun all six gates and affected host workflows. | Candidate graph, packaged mod, installed runtime, reachability, authoritative advisory, provenance, and compatible-remediation reconciliation on canonical Linux and any affected macOS or Windows non-client host-specific paths. |
| JNA runtime is missing, incompatible, or duplicated | Runtime class or symbol loading fails, API shape differs, native library is not supplied by pinned NeoForge, or the mod embeds or shadows another JNA implementation. | Fail the affected native operation and Phase 001 gate closed. Do not add an embedded runtime or development-only override as silent closure. | Restore the exact compatible platform-supplied runtime within the pin or block for an owner decision if no compatible remedy exists. | Compile-only declaration check, binary API linkage, native loading and writer workflows on canonical Linux and each affected or exercised macOS or Windows non-client host-specific path, plus mod-JAR duplicate class and native-library scan. |
| Compatible dependency remedy does not exist | An applicable critical or high exploitable path remains under `DEC-004`. | Block phase completion. Do not suppress the alert, weaken tests, or change the platform pin. | Request an explicit owner decision and authorized plan revision. | Complete attempted remedy ledger, advisory evidence, and failing compatibility results. |
| Repair changes schema or public contract | Diff changes serialized fields, config, wire shape, permission, action, or public route. | Prefer compatibility. If unavoidable, add the smallest versioned migration, recovery copy, rollback, and documentation. | Restore prior signed state and validated data snapshot if acceptance fails. | Old and new fixture compatibility, migration, rollback, and restart tests. |
| Sensitive value appears in evidence | Secret scan or reviewer detects a real credential, personal value, private host path, or unsafe raw payload. | Stop copying or displaying it. Quarantine the evidence and report only sanitized metadata privately. | Credential owner rotates if required. Recreate evidence with synthetic data and rescan. | Evidence custody audit and clean tracked diff and artifact scan. |
| A later repair invalidates earlier evidence | Commit, graph, config, schema, protocol, Mixin, test harness, or artifact hash changes. | Mark mapped rows stale immediately. No stale row may remain passed. | Rerun focused invalidated evidence, then repeat Tasks 011 and 012 after the last product change. | Evidence manifest commit and artifact hash reconciliation. |

## Verification Matrix

| Requirement or task | Static or unit | Integration | Real workflow or runtime | Negative and recovery | Evidence artifact |
|---|---|---|---|---|---|
| `P001-TASK-001` | Manifest, hash, prerequisite, and operating-system schema validation | Phase 000 packet and `DEC-009` cross reference | Clean candidate checkout identity check plus Minecraft Java client fixture, canonical Linux, and affected non-client host environment and runtime manifest validation | Dirty state, wrong branch, missing host or fixture, unresolved `EXT-001` or `EXT-002`, and stale packet stop or blocked tests | Phase 001 evidence header, external-prerequisite state, and entry gate result |
| `P001-TASK-002` | Boundary ownership, duplicate, sink, and data class checks | Cross component source to sink review | Representative traces correlated through runtime audit | Missing sink, hidden route, and stale inventory injection | Threat model, attack tree, boundary matrix, finding ledger |
| `P001-TASK-003` | `PermissionServiceTest`, `DynamicPermissionServiceTest`, `LuckPermsDynamicPermissionTest`, `DelegatedPermissionScopeTest`, policy and compiler tests | Kernel command contract, sudo, alias, bundle, profile, and panel integration tests | Disposable multi actor command, provider refresh, and delegated execution workflows | Deny, revoke, hierarchy, exemption, source and target substitution, stale confirmation, replay, recursion, provider failure, and concurrent grant use | Authority traces, state assertions, audit correlation, focused test reports |
| `P001-TASK-004` | `SefPayloadsCodecTest`, `GuiWorkflowPayloadsCodecTest`, `SefSessionNegotiationTest`, `SessionRequestGuardTest`, `PanelActionValidatorTest` | GUI protocol, workflow, projection, and server handler tests | Matching enhanced, fallback, incompatible protocol, disconnect, reconnect, and mixed client security workflows | Forged identity, wrong session, replay, out of order, stale revision, unknown IDs, oversized fields, rapid abuse, and permission loss | Codec corpus, session traces, denial and no mutation assertions |
| `P001-TASK-005` | `ModuleConfigServiceTest`, registry, storage, migration, and recovery focused tests | Transactional reload, migration, backup, recovery, and authority invalidation | Disposable server reload, failed migration, restart, and rollback | Malformed, oversized, linked, swapped, stale, unsupported, conflicting, read only, and interrupted fixtures | Config and migration matrix, before and after hashes, recovery record |
| `P001-TASK-006` | `AtomicFileStoreTest`, `FileLogSinkTest`, `FancyTagServiceTest`, native-provider, archive, and object-store focused tests | Import, canonicalization, publication, export, logging, opened-object identity, backup, rotation, and restore workflows | Direct canonical Linux POSIX descriptor workflow, plus direct macOS POSIX or Windows handle workflows only for a changed or exercised non-client provider path | Traversal, absolute path, symlink, detectable hard link, reparse point, parent and object swap, missing identity metadata, wrong type, identity change, partial write, flush failure, archive or image bomb, duplicate, disk failure, and collision | Fixture manifest, Linux ABI record, conditional host record or `NOT_EXERCISED` reachability record, before and after identity, bounded result, file hashes, append and flush traces, path and disclosure scan, `EXT-001` packet |
| `P001-TASK-007` | Provider adapter, native API, reflection, permission bridge, Mixin policy, and class-reference tests | Optional integration present and absent, provider refresh, native runtime linkage, and hook composition | Dedicated server and matching client startup through the Minecraft Java client fixture, with macOS or Windows native loading only for changed or exercised non-client provider paths | Missing class or symbol, linkage or ABI error, malformed value, stale cache, outage, removal, wrong side, missing native API, unsupported metadata, and failed injection | Integration matrix, loader and native-load logs, ABI ledger, classloading scan, Mixin and access-transformer ledger |
| `P001-TASK-008` | `AuditServiceTest`, `CommandRedactionPolicyTest`, `FileLogSinkTest`, `AltAddressPrivacyTest`, observation and export tests | Correlated command, GUI, observation, logging, privacy, and export flows | Multi actor secret canary, hidden identity, address, private message, retention, and log failure workflows | Unknown roots, control characters, wrapper nesting, restricted args, revoked observer, failed sink, unsafe exception, and evidence leak | Sanitized sink matrix, canary absence report, audit health and custody ledger |
| `P001-TASK-009` | Candidate and installed-runtime Gradle graph reports, dependency insight, binary API analysis, and advisory applicability for every alert | Changed and compile-only dependency build, GameTest, optional integration, packaging, and runtime-load checks | Dedicated server and Minecraft Java client fixture startup, with macOS or Windows only for changed or exercised non-client native loading or remedy behavior | Missing package, packaged-mod absence, installed-runtime absence, unreachable API, duplicate classes or native libraries, incompatible version, unavailable repository, stale alert, unresolved advisory, missing provenance, and no compatible remediation | Candidate and platform graphs, seven-gate `P001-DEP-###` table, licenses, authoritative provenance, installed artifact hashes, JAR and runtime reachability maps, JNA ownership record, `EXT-002` packet |
| `P001-TASK-010` | Focused regression for every `P001-FIND-###` and changed dependency | Adjacent boundary and compatibility suite | Real failure workflow for every finding whose failure depends on game, network, filesystem, provider, or loader state | Original exploit or failure shape, boundary values, rollback, old data, and no regression cases | Repair commit mapping, before and after proof, migration and rollback packet |
| `P001-TASK-011` | Consolidated affected unit suite and native-provider contract tests | Required GameTests and cross-boundary security and writer suites | Dedicated server, client, provider, filesystem, reload, reconnect, interruption, audit append, flush, rotation, shutdown, and restart on Linux; macOS and Windows only for changed or exercised non-client provider paths | Forgery, revocation, races, replay, bombs, outages, recovery, symlink, hard link, reparse point, object swap, missing identity, write and flush failure, repeated abuse, and resource bounds | Final adversarial ledger, per-host ABI and opened-object traces, `EXT-001` packet, performance comparison |
| `P001-TASK-012` | Read-only code scan, secret scan, repository scan, candidate and platform graph renewal, advisory review, JAR and installed-runtime inventory | Operating-system wrapper tests, required GameTests, build, fallback compilation, native API binary compatibility, and affected dependency checks | Dedicated server, Minecraft Java client fixture, fallback, incompatible protocol, optional integration, and native writer workflows; macOS and Windows only for changed or exercised non-client paths | Secrets, private paths, logs, caches, duplicate entries, JNA classes or native libraries, unintended embeds, missing licenses or provenance, unresolved advisories, Mixin or access-transformer drift | Scan and build reports, candidate and installed-runtime graphs, mod JAR and platform inventories, SHA-256, SHA-512, JNA ownership, seven-gate alert ledger, closed `EXT-002` packet |
| `P001-TASK-013` | Documentation link, terminology, support-claim, prerequisite-status, and generated-reference drift checks | Evidence-to-requirement, finding, operating-system, and dependency-gate reconciliation | Replay documented security, native-writer, recovery, compatibility, and dependency procedures against the repaired artifact and pinned runtime | Stale count, false completion, secret in docs, unavailable feature claim, unsupported-Windows claim, suppressed platform alert, and lower-fidelity substitution | Updated docs, `EXT-001` and `EXT-002` sanitized evidence index, downstream invalidation map |
| `P001-TASK-014` | Complete diff, artifact, prerequisite, and commit audit | Required pull request checks plus private independent-review capability and result disposition | Integrated candidate branch verification tied to the canonical Linux packet, any affected host-specific packets, and platform dependency closure | Missing evidence for an exercised host path, unresolved direct mod-owned advisory or provenance, duplicate runtime, unresolved review, failed check, merge mismatch, unsigned tag, or stale evidence blocks transition | Pull request, checks, review, merge commit, verified candidate commit and artifact hashes, signed tag, prerequisite closure records |

### Fixtures, Environments, and Rerun Order

Use disposable synthetic identities for owner, senior staff, junior staff, ordinary players, offline players, vanished targets, equal and higher ranks, exempt targets, console, RCON, command block, function, scheduler, server profile, and integration sources. Permission fixtures cover absent, exact grant, exact deny, inherited or wildcard grant, refresh, revoke, provider outage, reconnect, and stale cache. Never grant broad permission to avoid constructing a precise row.

Filesystem fixtures use a freshly created isolated root whose exact path and disposable identity are checked before each destructive action. Canonical Linux includes normal regular files, symbolic links, detectable hard links, parent and target swaps, changed device or inode identity, wrong file types, unsupported identity metadata, read-only files, partial write, flush failure, collision, interrupted publication, rotation, and recovery copies. Repeat the equivalent POSIX corpus on macOS or the handle, link, junction, reparse, volume, and file-identity corpus on Windows only when the corresponding non-client provider path changed or was exercised. Otherwise retain a `NOT_EXERCISED` reachability record, not a fabricated fixture result. Each executed fixture records filesystem type and capability. Unsupported fixture creation is distinct from unsupported product behavior and keeps a required conditional row open until an appropriate disposable host is available. Archive and image corpora are synthetic and bounded so failure tests do not exhaust the host. Store only fixture generators or small safe fixtures in tests. Large or deliberately malformed captures remain in the approved evidence location.

For `EXT-001`, run the exact repaired artifact through the Minecraft Java client fixture and on a disposable canonical Linux environment. Add disposable macOS or Windows environments only when changed or exercised non-client host-specific paths require them. Each executed environment manifest records operating-system version, architecture, filesystem, Java 21 vendor and version, Minecraft 1.21.1, NeoForge 21.1.235, Gradle invocation, installed runtime coordinates and hashes, fixture identity, and artifact SHA-256 and SHA-512. Native-writer evidence records loaded library and symbols, ABI assumptions, structure layouts, flags, identity fields, link or reparse data, opened descriptor or handle lifetime, before and after object identity, append result, flush result, rotation, shutdown, restart, and failure preservation. A mocked operating-system name, unit-only native facade, container that does not expose the required host ABI, or one host standing in for another cannot satisfy an exercised host-specific packet.

Dependency capture records the Gradle version from the checked-in wrapper, Java 21 runtime, repositories, offline or online mode, configuration names discovered from the live model, exact commands, and output hashes. At minimum, capture every resolvable compile, runtime, fallback runtime, test, tooling, packaging, and installed NeoForge runtime configuration plus build environment, platform artifacts, and packaged JAR inputs on canonical Linux and any affected macOS or Windows non-client host-specific paths. Use dependency insight for every alert and every changed coordinate. Never assume compile only means absent from the target server; confirm mod packaging, authoritative NeoForge supply, installed runtime state, affected API reachability, native loading, and binary compatibility. For JNA and JNA Platform, retain the exact compile-only declaration, runtime supplier, class and native-library inventory, and duplicate scan.

After each repair, run its focused unit or static regression, then adjacent integration tests. After the final repair, run the complete affected unit suite, required GameTests, build, fallback compilation, dedicated server and Minecraft Java client fixture workflows, provider and protocol matrices, native-writer rows on Linux and on macOS or Windows only when their non-client paths are exercised, scanners, candidate and installed-runtime graph renewal, advisory closure, and mod and platform artifact inspection. A failure reopens the responsible finding or alert. After any subsequent product, provider, dependency, runtime, harness, or packaging change, rerun every mapped row and then repeat the consolidated Task 011 and Task 012 gates.

Expected results are binary and evidence based. A required row is `pass`, `fail`, or `blocked`. `Blocked` is not passed. A lower-fidelity test cannot replace a real provider, loader, operating system, native ABI, opened-object, multiplayer, filesystem, migration, recovery, dependency, or artifact workflow required by the row. Unknown conditional `EXT-001` or `EXT-002` evidence leaves affected rows open while unblocked work proceeds.

## Documentation, Operations, and Release

- Update `docs/SECURITY_REVIEW.md` with the frozen and repaired review range, complete trust boundaries, stable sanitized finding dispositions, canonical Linux and affected or exercised macOS and Windows non-client native-writer proof, dependency closure summary, scanners, runtime proof, limitations, `EXT-001` and `EXT-002` status, and invalidation rules. Do not erase historical findings or expose exploit details that would endanger unpatched users.
- Update `docs/SEF2_ACCEPTANCE.md` with current Phase 001 evidence and honest remaining gaps. Missing evidence for an exercised host, native ABI proof, platform provenance, or a direct mod-owned advisory remains open. Upstream-only platform-owned alerts are recorded as not mod resolvable after reachability proof. Do not mark later phase matrices complete.
- Update `docs/COMPATIBILITY_MATRIX.md` when a dependency remedy or integration verification changes supported behavior, while preserving `DEC-004` and the `DEC-009` requirement that Linux, macOS, and Windows remain supported.
- Update `DOCUMENTATION.md` and `README.md` only when verified operator behavior, configuration, permissions, privacy, recovery, dependencies, setup, or limitations change.
- Update `test.md` when a security, provider, payload, filesystem, native-writer, archive, image, privacy, or dependency procedure changes. Record exact canonical Linux ABI, opened-object, append, flush, failure, rotation, shutdown, restart, runtime-dependency, and artifact steps. Add macOS or Windows non-client steps only for a changed or exercised host-specific path, and record `NOT_EXERCISED` reachability otherwise. Preserve the highest-fidelity staging requirement and one Linux Minecraft Java client contract.
- Update `docs/MIGRATION_GUIDE.md` for any versioned configuration or data repair and include preflight, backup, forward migration, failure behavior, rollback, and recovery verification.
- Update `docs/RELEASE_WORKFLOW.md` with exact candidate and installed-runtime graph, JNA ownership, duplicate-runtime, scanning, license, authoritative provenance, advisory, and mod and platform artifact inspection steps when those commands or expected outputs change.
- Update release notes only for actual user or operator visible security, dependency, configuration, compatibility, or recovery changes. Do not publish a release.
- Regenerate command, permission, configuration, or other tracked references only when a repair changes their live source. Zero unexplained drift is required.
- Store raw security logs, packet captures, malformed fixtures, screenshots, world snapshots, graph output, and detailed exploit reproduction outside tracked source in the approved sanitized evidence location. Tracked evidence summaries must be sufficient to reproduce the result without exposing secrets or personal data.
- If a severe finding requires restricted disclosure, GitHub issues and pull requests carry only a neutral sanitized summary, affected invariant, test status, and closure reference. Detailed exploit preconditions and payloads remain in the approved private evidence route.
- Repository integration follows the sequential phase branch and pull request workflow. All Git and GitHub text follows repository authorship and formatting rules. No direct update to the protected default branch, alert dismissal, release publication, or deployment occurs.
- No production monitoring change is needed because this plan does not deploy. Any new diagnostic or health signal added by remediation must be bounded, permission filtered, redacted, documented, and covered by tests.
- Every affected compatibility, installation, security, troubleshooting, acceptance, and release document must identify Linux, macOS, and Windows as supported. `EXT-001` or `EXT-002` uncertainty is written as a blocker and never as an unsupported-Windows claim, operating-system exclusion, platform-alert suppression, or Phase 001 pass.

## Risks and Evidence Invalidation

| Risk | Prevention | Detection | Recovery | Evidence invalidated | Reverification |
|---|---|---|---|---|---|
| Indirect execution path is missed | Derive routes from the live dispatcher, catalog, callbacks, services, and Phase 000 inventory; trace aliases, bundles, profiles, GUI, sudo, scheduler, and integrations. | Boundary ownership and source to sink completeness checks; independent review. | Reopen the threat model and every dependent authority row. | Permission, command, GUI, audit, and integration evidence. | Repeat Tasks 002, 003, 008, 011, and 012 for the affected path. |
| Client or cached state is treated as authority | Enforce server projected identifiers and immediate pre mutation revalidation. | Forged, stale, replay, disconnect, and permission loss tests. | Close the session or surface, discard stale state, repair handler, and rerun. | Payload, GUI, authority, privacy, and integration evidence. | Repeat Tasks 004, 003 where applicable, 011, and 012. |
| Security fix changes behavior outside scope | Keep repair mapped to one finding and inspect blast radius and diff. | Adjacent contract tests, generated reference drift, and documentation comparison. | Split or remove unrelated change through a normal corrective commit. | Repair, compatibility, command, UI, or persistence evidence touched by the drift. | Focused and adjacent suite plus downstream handoff updates. |
| Dependency remedy silently changes platform, native-runtime ownership, operating-system support, or optional status | Compare pins, dependency attributes, transitivity, generated metadata, compile-only declarations, installed-runtime supply, and packaged entries before and after. | Candidate and installed-runtime graph diff, mod metadata and JAR inspection, JNA duplicate scan, canonical Linux native loading, and macOS or Windows native loading when host-specific paths are changed or exercised, plus optional present and absent tests. | Revert the incompatible declaration and try another compatible remedy; block if none exists. | All dependency, integration, native-writer, build, runtime, and artifact evidence. | Repeat Tasks 007 and 009 through 012 plus affected `EXT-001` and `EXT-002` rows. |
| Alert advisory or remote state changes during execution | Capture advisory identifiers and timestamps, then refresh before exit. | Remote snapshot diff. | Reopen affected `P001-DEP-###` rows. | Alert closure and potentially graph or artifact evidence. | Repeat advisory analysis, reachability, remedy, and affected runtime tests. |
| Test-only or loader-supplied component is misclassified | Record every candidate configuration, platform owner, installed runtime provider, affected API, and host result, not only packaged classes. | Compare resolved candidate and installed-runtime graphs, generated metadata, authoritative NeoForge artifacts, loader runtime, mod JAR, and runtime inventories. | Correct the seven-gate disposition and rerun affected compatibility and host environments. | Dependency and supply chain closure for that component. | Repeat Tasks 007, 009, 011, and 012 plus `EXT-002`. |
| Link, reparse, or object swap defeats check-then-use logic | Use provider-specific or native open primitives and bind type, identity, link or reparse validation, append, flush, and post-flush stability to the same descriptor or handle on canonical Linux and each affected or exercised macOS or Windows non-client host-specific path. No path-only fallback is permitted. | Concurrent parent and target swaps, symlink, detectable hard-link, reparse-point, identity-change, rotation, write-failure, and flush-failure tests on each executed provider host. | Reject ambiguous state, preserve current files, repair the platform provider, and rerun every affected host row. | Filesystem, native-writer, config, migration, import, export, logging, recovery, and platform-runtime evidence. | Repeat Tasks 005 through 007, 011, and 012 plus affected `EXT-001` rows. |
| A required host-specific environment is missing | Preflight `EXT-001` before execution and keep Linux, macOS, and Windows support rows explicit even when availability is unknown. | Environment-manifest completeness and exact-artifact reconciliation. | Continue unblocked work, keep only affected Phase 001 rows open, and obtain the missing disposable host when its non-client path is exercised. Never narrow support or call Windows unsupported. | Native-writer, runtime dependency, server, Minecraft Java client fixture, shutdown, restart, and any non-client host-specific security evidence. | Run the complete missing non-client host contract when that path is exercised, then rerun Tasks 011 through 014 as affected. |
| Platform-owned alert is collapsed into mod packaging status | Require separate candidate graph, packaged mod, installed runtime, affected-API reachability, authoritative advisory applicability, provenance, and compatible-remediation gates. | `P001-DEP-###` schema validation, authoritative artifact comparison, advisory refresh, and reviewer reconciliation. | Restore all gates and classify an upstream-only nonreachable alert as platform-owned and not mod resolvable. Keep Phase 001 blocked only for direct mod-owned exposure or missing authoritative evidence needed to establish ownership. | Dependency, native runtime, build, server, client, artifact, and documentation evidence. | Repeat Tasks 009, 012, 013, and 014 plus affected host runtime tests. |
| Compile-only JNA is absent, binary incompatible, or duplicated | Pin compile-time APIs to the declared compile-only dependency, identify the exact NeoForge runtime supplier, inspect binary API use, and prohibit embedded duplicates. | Minecraft Java client fixture and macOS or Windows non-client class and native-library loading, API linkage, mod JAR and installed-runtime scans, and duplicate inventory. | Repair within the pinned NeoForge boundary or keep the phase blocked for an owner decision. Do not embed a second runtime silently. | Native writer, dependency, dedicated-server, client, packaging, provenance, and advisory evidence. | Repeat Tasks 006, 007, 009, 011, and 012 plus `EXT-001` and `EXT-002`. |
| Archive or image test harms the host | Apply small configured bounds and disposable fixture isolation before running malformed cases. | Resource monitoring and fixture preflight. | Terminate the fixture process, discard isolated data, restore baseline, and reduce harness limits without weakening product limits. | Parser performance and denial of service evidence. | Rerun with safe bounded generators and record environment limits. |
| Redaction occurs after filtering or persistence | Centralize classification and redact before any broad sink. | Unique synthetic canary search across memory, logs, audit, exports, exceptions, docs, and JAR. | Quarantine unsafe evidence, repair ordering, clear disposable sinks, and recreate proof. | Privacy, audit, logging, export, and evidence custody results. | Repeat Task 008, affected runtime workflows, scans, and Task 012. |
| Reflection or Mixin failure is swallowed | Bound failures at adapter or loader boundary and require actionable sanitized diagnostics. | Absent class, linkage error, required injection, and dedicated server class loading tests. | Restore compatible optional artifact or narrow repair inside pins. | Integration, class loading, runtime, and artifact evidence. | Repeat Task 007, runtime startup, and artifact inspection. |
| Concurrent revocation or cleanup permits one extra effect | Bind revisions and use atomic conditional consumption at mutation. | Deterministic race harness and repeated stress. | Repair ownership and cleanup, invalidate affected sessions or grants, and retest. | Authority, payload, provider, audit, and performance evidence. | Repeat Tasks 003 or 004, 011, and correlated audit proof. |
| Security evidence leaks sensitive details | Synthetic data, restricted raw evidence, sanitized summaries, secret and path scans. | Precommit, documentation, evidence, repository, and JAR scan. | Quarantine, remove from tracked change before commit, rotate only through the credential owner if necessary, and recreate evidence. | Documentation, evidence, diff, and artifact scans. | Repeat Task 008 custody checks and Tasks 012 through 014. |
| Phase 002 changes a repaired filesystem boundary | Explicit downstream invalidation map and stable finding IDs. | Diff and evidence manifest comparison at Phase 002. | Reopen affected Phase 001 rows before Phase 002 exits. | Filesystem, recovery, privacy, and artifact evidence. | Repeat focused Phase 001 tests and later full Phase 006 matrix. |
| Final product change occurs after the artifact inspection | Bind every proof to commit and artifact hashes. | Completion packet hash reconciliation. | Mark affected rows stale and return to the responsible task. | All component, graph, runtime, documentation, and artifact proof touched by the change. | Rerun mapped rows, then Tasks 011 through 014. |

## Phase Completion Packet

Phase 001 may close only when the completion packet outside the protected plan set contains all of the following:

1. The integrated Phase 000 dependency proof, signed phase tag, frozen Phase 001 starting commit, branch, ancestry, dirty state, platform versions, wrapper version, and sanitized fixture and evidence manifest.
2. The complete threat model, attack tree, data class flow map, trust boundary matrix, and source to sink traces with no missing, duplicate, or unowned row.
3. The complete `P001-FIND-###` ledger with severity, invariant, source, sink, preconditions, impact, disposition, repair commit, focused regression, adjacent regression, disclosure route, and invalidation scope. Every confirmed finding is repaired.
4. Authority evidence covering permissions, explicit denies, wildcard and inheritance, hierarchy, exemptions, all declared sources and targets, delegation, aliases, bundles, profiles, sudo, scheduled work, GUI actions, stale revisions, provider failure, and true actor attribution.
5. Protocol evidence covering codec bounds, negotiation, sessions, sequence, replay, stale state, server projected choices, rate limits, disconnect, reconnect, incompatible protocol, matching enhanced client, fallback client, and mixed client security behavior.
6. Configuration, migration, recovery, filesystem, archive, image, import, export, object store, logging, and evidence custody results, including traversal, symlink, detectable hard-link, Windows reparse, parent and object swap, malformed, oversized, interrupted, rollback, privacy, and host path disclosure cases.
7. Optional integration, native API, reflection, Mixin, access transformer, dedicated-server classloading, client classification, provider present and absent, outage, malformed, removal, missing symbol, ABI mismatch, unsupported identity metadata, failed injection, and compatibility evidence.
8. Audit, redaction, logging, privacy, secret canary, exception, export, retention, hidden identity, address, private message, and restricted argument proof with sanitized results.
9. The resolved Gradle build, main compile, main runtime, fallback, test, tooling, packaging, and installed NeoForge runtime graph reports discovered from the live model on canonical Linux and any affected or exercised macOS or Windows non-client host-specific path, with coordinates, dependency paths, configurations, authoritative repositories, SHA-256, SHA-512, licenses, provenance, optional or platform-owned state, packaged-mod presence, installed-runtime presence, and runtime supplier.
10. The complete `P001-DEP-###` table for every alert current at execution, explicitly reconciling the authoring count of twelve high, thirteen medium, and one low. Every row includes advisory basis, affected range, graph path, configuration, candidate applicability, packaged-mod artifact, installed-runtime artifact, affected API, source and runtime reachability, exploit precondition, authoritative advisory applicability, provenance, compatible remediation, verification, remote state, and invalidation. Platform-owned gates remain separate.
11. Focused unit and integration results for every repair; required GameTest results; adversarial, concurrency, interruption, provider, native-writer, privacy, and resource-bound results; and exact commands, Java and platform environment, expected result, actual result, and disposition. Direct canonical Linux and affected or exercised macOS or Windows non-client host-specific results prove native ABI, same-opened-object identity, link or reparse rejection, object-swap resistance, append, flush, rotation, shutdown, restart, and preservation after failure.
12. Final clean security and supply chain checks for the phase: successful operating-system-appropriate builds and fallback compilation, affected dedicated-server and client workflows, native loading, code scan, secret scan, remote alert snapshot, complete repository diff review, mod-JAR and installed-runtime entry and duplicate-class and native-library inspection, dependency metadata, licenses, authoritative provenance, advisories, mixins, access transformer, resource scan, and SHA-256 and SHA-512.
13. Versioned migration, exact recovery copy, rollback instructions, old and new fixture evidence, and compatibility documentation for any repair that changes configuration, persistent schema, protocol, public route, permission, identifier, or other external contract. Record `not applicable` with evidence when no contract changed.
14. Updated `docs/SECURITY_REVIEW.md` and every other affected current document, plus link checks, generated reference drift results, sanitized evidence index, and a map of all later phase evidence invalidated by Phase 001 changes.
15. A complete final diff audit showing no unrelated changes, secrets, personal data, host specific private paths, runtime logs, caches, build output, unsafe raw findings, unexpected generated files, or accidental platform changes.
16. Phase branch commits authored and signed under repository rules, linked issue and milestone state where applicable, one phase pull request, all required checks, private independent-review capability and result disposition, resolved supported-review threads, GitHub merge commit, verified resulting candidate branch commit, and signed annotated Phase 001 tag.
17. An explicit exit statement that no known authorization bypass, sensitive data leak, backdoor like route, applicable critical or high exploitable vulnerability, or other confirmed mandatory Phase 001 finding remains, and that no release, publication, deployment, production mutation, remote alert dismissal, operating-system scope reduction, or unsupported-Windows claim occurred.
18. The applicable `EXT-001` evidence packet for the same candidate commit and artifact hashes, with sanitized canonical Linux environment manifest and provider-specific runtime proof plus macOS or Windows manifests when changed or exercised host-specific paths require them. Missing, partial, failed, mocked, or stale evidence prevents the affected host-specific item from passing, never changes platform support.
19. The `EXT-002` evidence packet with authoritative NeoForge artifact metadata and hashes, exact JNA and JNA Platform runtime supply and binary compatibility, successful native loading on canonical Linux and any affected macOS or Windows non-client host-specific paths, no duplicate native runtime in the mod JAR, and separate platform-owned advisory and compatible-remediation dispositions. Unresolved authoritative evidence or direct mod-owned exposure prevents the affected dependency item and Phase 001 from passing; an upstream-only nonreachable alert is recorded as not mod resolvable.

## Next Transition

After every Phase 001 row and exit criterion pass, with `EXT-001` conditional rows complete for canonical Linux and any affected host-specific path and `EXT-002` ownership, reachability, and direct mod-owned remediation dispositions recorded, the Phase 001 pull request may merge. Verify through GitHub that the merge is complete, fetch the authoritative remote, confirm the resulting candidate branch contains the merge commit, rerun the required integrated branch checks, and create and push the signed annotated Phase 001 tag. Do not merge, create, or start the Phase 002 branch while a required path is unavailable or failed, a direct mod-owned applicable vulnerability or provenance gate is unresolved, the pull request is open, checks are pending or failed, review findings remain unresolved, auto merge is only queued, the integrated commit is unverified, or the signed tag is missing.

Once every integration gate passes, `SEFAUD-PHASE-002` begins from that verified integrated candidate commit. Its first action is to consume the closed filesystem and persistence security boundary matrix, repaired primitives, schema or configuration migrations, recovery material, dependency and artifact manifest, and evidence invalidation map. Phase 002 then executes its exhaustive durable owner and cross store integrity audit without reopening platform scope or treating Phase 001 security proof as a substitute for persistence fault, concurrency, migration, rollback, and recovery evidence.
